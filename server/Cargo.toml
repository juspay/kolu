[package]
name = "kolu-server"
version.workspace = true
edition = "2021"
description = "kolu server"

[dependencies]
kolu-common = { path = "../common" }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["fs"] }
