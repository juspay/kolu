## [blocker] #1 D6's "treat undecodable frames as skew → recycle" cannot satisfy the upgrade-window acceptance suite, because that suite does not merely observe a supervisor recycling — it makes CROSS-VERSION RPC CALLS with the CURRENT-build client against PREVIOUS-RELEASE daemons and asserts they succeed. Under an oRPC→ndjson break those calls can never complete, so the tests fail at the probe, long before any skew policy runs.

Evidence: packages/padi/src/upgradeWindow/previousRelease.e2e.test.ts:311-327 (current `@kolu/surface/links/unix-socket` dials the PREVIOUS kaval and awaits `surface.system.heartbeat({})`); :571-584 (current `unixSocketLink<PadiDaemonContract>` dials the PREVIOUS padi and awaits `surface.control.core.hello()`); :600-604 (current client drives the previous padi's `surface.padi.lifecycle.recycleKaval`). The readiness helper just retries until a deadline and then throws — packages/surface-daemon/src/upgradeWindowPreviousRelease.testlib.ts:38-59 (`socket never ready at ${socketPath}`). ci/mod.just:253-330 runs exactly this file under KOLU_UPGRADE_WINDOW_REQUIRE=1, and refuses to skip.

Fix: Add an explicit D6 sub-decision for the harness, not just the daemon: either (a) the previous-release probes/dials in `newReadsOld`/`oldReadsNew` are rewritten to assert an UNDIALABLE peer (transport-level refusal → recycle), with the version-tag/store-inequality guards kept, or (b) the suite pins a previous release that already speaks ndjson (i.e. land a transport-only release first, then the semantic break). State which, and write the replacement assertions in the plan before W4 starts.

---

## [blocker] #2 The migration breaks the one channel the codebase declares must never break. `controlCore` is the version-AGNOSTIC fragment a supervisor reads BEFORE it knows whether the versioned surface is compatible; D6's lever (bump PADI_SURFACE_VERSION / PTY_HOST_CONTRACT_VERSION) is inert, because version negotiation happens INSIDE the protocol being replaced — you cannot read a peer's version over a wire you can no longer parse.

Evidence: packages/surface-daemon/src/controlCore.ts:1-14 — "This channel never versions: a supervisor must be able to ask a resident daemon who it is, and drain it, before the versioned application surface is known to be compatible… The frozen fragment's own wire version. Never bump this value." The compatibility gate reads a value obtained over that wire: packages/padi/src/dial.ts:169-180 (`assertPadiSurfaceCompatible(runningSurfaceVersion)` on `hello.surfaceVersion`).

Fix: Make the frozen fragment's TRANSPORT frozen too, or admit a flag day. Concretely: either serve the control-core fragment on the legacy base64+newline framing alongside the new ndjson listener for exactly one release (a documented, dated transition, not a permanent fallback), or state in D6 that this is a hard flag day, bump CONTROL_CORE_VERSION despite the comment, and delete/replace the "never versions" contract in controlCore.ts and the invariants docs in the same commit.

---

## [blocker] #3 "Undecodable frame ⇒ skew ⇒ recycle" contradicts the supervisor's existing, deliberately-designed refusal contract, and implementing it would re-introduce a recorded regression plus a kill-the-innocent hazard. Today a probe that connects but cannot handshake is NOT skew — it is `probe-failed` ⇒ UNCONVERGED ⇒ refuse. That arm is what protects a foreign process holding the socket from being SIGTERMed.

Evidence: packages/surface-daemon-supervisor/src/probeDaemonIdentity.ts:228-229 — "Curried endpoint probe. Returns `null` only for ECONNREFUSED/ENOENT; any other dial or frozen-handshake failure throws." packages/surface-daemon-supervisor/src/convergence/converge.ts:216-220 (throw ⇒ `{kind:"failed"}`) and :250-262 (`probeFailedOutcome` ⇒ "convergence: UNCONVERGED"). packages/surface-daemon-supervisor/src/convergence/bindResult.ts:8 records the prior regression: "Never catch-to-null — that collapsed probe-failed into identity-unverifiable." packages/surface-daemon-supervisor/src/endpoint.ts:246-248 — SocketSquatterForeignError: "refusing to kill a process that is not a verified daemon of this endpoint." And padi's disposition is not recycle at all: packages/server/src/padi/padiConvergence.ts:57 `onContractSkew: { kind: "drain-newer-else-refuse" }` — where `drain` is itself an RPC (`surface.control.core.drain()`, probeDaemonIdentity.ts:148) over the same broken wire.

Fix: Do not widen `probe-failed` into the skew arm. Add a THIRD, narrowly-typed observation — e.g. `{kind:"unspeakable-protocol"}` — raised only by an explicit protocol-negotiation failure (a decode error on the first frame from a peer whose gate file we own and whose pid we verified), and give it its own disposition per policy: kaval `recycle`, padi `refuse` (it cannot be drained, so it must not be killed). Spell out in D6 that padi's `drain-newer-else-refuse` degenerates to `refuse` across the break, and what the operator-facing message is.

---

## [major] #4 D5's claim that `createLiveSignal`/heartbeat "stay (they are transport-agnostic)" is false. Only `heartbeat.ts` is agnostic. `createLiveSignal` takes a concrete socket object, BUILDS THE oRPC LINK ITSELF over it, derives status from the socket's own open/close events, and recovers a half-open socket by calling `ws.reconnect()`. Effect's `Socket.layerWebSocket` + `RpcClient.layerProtocolSocket` expose none of those three affordances to a caller, so half-open detection — the whole reason this primitive exists — has no implementation path in the plan.

Evidence: packages/surface/src/solid/liveSignal.ts:199-232 — `createLiveSignal(ws: WatchableSocket, …)`, `ws.addEventListener("open"/"close", …)`, `const link = websocketLink<C>(ws as unknown as WebSocket)` (line 228), and the watchdog action `ws.reconnect()`; the header at :150-179 makes the link and the probe channel deliberately the same socket ("there is no separate, fabricatable probe target"). Mirror on the app side: packages/surface-app/src/connect.ts:159-165 `HeartbeatSocket = { readyState; OPEN; reconnect }`.

Fix: Promote this to its own D-decision: define how a `WatchableSocket`-equivalent is obtained from Effect's socket layer (open/close observability + an imperative force-reconnect), or replace the watchdog's recovery action with an Effect-native equivalent (e.g. interrupting the protocol fiber so `retryTransientErrors` re-dials) and re-derive the `LIVE_SIGNAL_HANDLES` brand and `surfaceClient`'s refusal of bare links on top of it. Budget it in W2/W5, not as "stays as-is".

---

## [major] #5 The stale-tab retirement mechanism has no Effect home, and `retryTransientErrors: true` actively converts it into a reconnect storm. Today a server-initiated stale close is made TERMINAL by poisoning the socket's `send`; with connection-level infinite retry, close-code 4001 is just another transient close, so the client reconnects forever, re-presenting the same stale `pid` and being closed again.

Evidence: packages/surface-app/src/lifecycle.ts:51-59 — `retireSocket` calls `ws.close()` then replaces `ws.send` with a thrower producing `deadTransportError(SURFACE_TRANSPORT_RETIRED, …)`. packages/surface-app/src/connect.ts:99-102 — retire fires on `event.code === restartCloseCode` (STALE_PROCESS_CLOSE_CODE); "other close codes are ordinary transient drops partysocket reconnects through". The non-retry fence that depends on that error shape: packages/surface/src/client.ts:44-47, 85-90 (`isDeadTransportError`).

Fix: D5 must specify a terminal-close classifier for the Effect protocol layer: a close-code predicate that STOPS the retry schedule and fails every in-flight and future call with the `SurfaceTransportRetired` tagged error from D4. Name the seam (a custom `Schedule`/`while` predicate on the socket layer, or a wrapper protocol layer) and add a test that a 4001 close produces exactly one close and zero re-dials.

---

## [major] #6 D5 accounts for the CSWSH origin gate but omits the two other things that sit between the WS upgrade and RPC dispatch on the server: the per-socket stale-tab gate and the server-side liveness reaper. Both are order-sensitive by design (gate → enrol → dispatch), and `RpcServer.layerHttp({protocol:"websocket"})` owns the upgrade, so the plan has no seam for either.

Evidence: packages/server/src/index.ts:1168-1211 — `acceptSurfaceSocket({server: wss, liveProcessId: serverProcessId, …})`, then `acceptor.accept(ws, url, () => wsRpcHandler.upgrade(ws, {context: …}))`; the comment states the gate "closes a tab bound to a PREVIOUS instance BEFORE oRPC upgrades the socket". packages/surface-app/src/server.ts:597-640 — the seam exists precisely because "forgetting `register` leaks a half-open browser as a server-side zombie holding stream subscriptions open forever; doing the steps out of order reintroduces kolu#1231's crash". Client half: packages/surface-app/src/connect.ts:141-152 — the URL is a THUNK so the echoed `pid` is re-read on EVERY reconnect.

Fix: Add to D5: (a) how a pre-dispatch per-socket gate is expressed under `RpcServer.layerHttp` (likely a custom upgrade route that gates then hands the socket to the RPC layer, rather than `layerHttp`'s turnkey path), (b) how the ws-level ping/terminate reaper (`startWsHeartbeat`) survives, and (c) how the client re-evaluates its connect URL (pid echo) on every re-dial under `Socket.layerWebSocket`.

---

## [major] #7 D3/D7 state the ordering requirement as its current MECHANISM ("one microtask per yield") rather than as the invariant, and in doing so miss the opposite constraint pinned by the same e2e scenario: event handlers must add NO wrapper layer, because an extra async hop put the stream's "complete" frame ahead of its last yielded value. A Stream port that uniformly "preserves one microtask per yield" breaks half of what kill.feature pins.

Evidence: packages/surface/src/server.ts:1086-1098 and :1145-1155 — `publisherChannel`/`iterateUntilAborted` MUST add the hop ("a list-update publish racing a per-terminal exit publish can deliver the list message first… Regression-pinned by Kolu's `kill.feature`"). packages/surface/src/server.ts:546-567 — `eventHandlers` MUST NOT wrap: "The extra wrap layer would put oRPC's wire one async tick behind a single-yield-then-return source… the consumer's first iteration sees `done: true`, and the yielded value is dropped. Pinned by `kill.feature` "Natural PTY exit removes terminal"."

Fix: Restate D3/D7's requirement as two invariants and test them directly, independent of implementation: (1) cross-channel delivery order at the client equals publish order when two channels publish in the same tick; (2) a single-yield-then-complete source delivers its value before end-of-stream. Add both as framework-level unit tests in W2 (before the e2e in W7), since Effect's fiber scheduler gives no microtask-level control to "preserve".

---

## [major] #8 D5 drops `ClientRetryPlugin` for connection-level `retryTransientErrors`, but the retry hook the codebase actually depends on is PER-STREAM-CALL and supersession-aware: `onRetry` fires before each re-subscribe so the consumer can clear derived state that the fresh snapshot will replace. A connection-level retry cannot express it.

Evidence: packages/surface/src/client.ts:156-186 (`onRetry` merged into the per-call retry context). Consumers: packages/client/src/terminal/Terminal.tsx:597 `onRetry: resetIfLive` — guarded by an attempt gate so "a superseded attempt's retry must not wipe the successor's screen" (:586-600, :437-448); packages/padi/src/watch.ts:403 `onRetry: disarmIdle`; packages/surface/src/solid/surfaceClient.ts:1377-1382; packages/surface-app/src/lifecycle.ts:42.

Fix: D5 must state where per-subscription retry lives in the Effect port: the retry belongs INSIDE the per-stream `Stream` (e.g. `Stream.retry` with a tap that runs the consumer's reset) rather than only in the protocol layer, and `SurfaceClientStreamOpts.onRetry` must keep its per-attempt identity. Note the same requirement is what preserves the "reconnect snapshot leads a fresh stream" invariant D3 claims to keep.

---

## [major] #9 The plan gives the daemon boot no headroom for the 30s frozen-hello deadline that a foreign-protocol peer will now hit on every probe, and the drain exit-oracle has NO deadline at all and explicitly treats "listener that cannot complete hello" as not-proof-of-exit — so during the upgrade window it spins until an external abort.

Evidence: packages/surface-daemon-supervisor/src/probeDaemonIdentity.ts:20 `CONTROL_CORE_HELLO_TIMEOUT_MS = 30_000`, applied at :60-81 (`withHelloDeadline`); :196-207 `awaitHelloGone` calls `client.surface.control.core.hello()` with no deadline and comments "A listener that cannot complete hello is not proof of process exit." The e2e's padi readiness budget is 90s total (packages/padi/src/upgradeWindow/previousRelease.e2e.test.ts:380).

Fix: Specify in D6 that the unspeakable-protocol classification is made at the TRANSPORT/first-frame decode, not by waiting out the hello deadline, and that `awaitHelloGone` gains an explicit bound for that case. Otherwise state and raise the affected budgets and prove the boot still converges inside the 90s the e2e allows.

---

## [minor] #10 `frontDaemonOverStdio`'s "contract-blind byte splice" property does not survive by assertion — it survives only if the stdio client framing and the unix-socket server framing remain BYTE-IDENTICAL. D5 puts three different Effect protocol layers on the three legs (`layerProtocolStdio` agent-side, `layerProtocolSocket` parent-side, `NodeSocketServer.layer` for the daemon socket) and asserts identity without evidence. Relatedly, the base64 codec exists for BINARY message safety, not only for embedded newlines — the plan's justification only answers the newline half.

Evidence: packages/surface-daemon/src/frontDaemonOverStdio.ts:22-31 — "**No decode:** a unix socket served by `serveOverUnixSocket` and a client's `stdioLink` carry the *same* `@kolu/surface` peer framing (base64+newline), so the client talks to the socket-served router straight through this pipe." packages/surface/src/links/stdio-codec.ts:9-17 — "The underlying peer codec emits `string | ArrayBufferLike | Uint8Array` per message… 1. Binary safety — message bytes can include `\n`, NUL, etc."

Fix: Add an explicit W2 gate: a test that bytes produced by the stdio protocol layer are accepted verbatim by the socket protocol layer (and vice versa), i.e. the splice round-trips. If they differ, `frontDaemonOverStdio` must become framing-aware and the plan's "that property survives" line must be deleted. Also confirm no surface member carries a non-string binary payload under ndjson.

---

## [minor] #11 The upgrade-window e2e also grounds a shared-artifact inventory against the LIVE disk, before AND after recycle, failing on any unregistered non-log file. D9's rewrite of `daemonMain` into a Layer graph is exactly the kind of change that can add a new on-disk artifact (lock/socket/marker), and the plan does not list this inventory as a thing to keep in sync.

Evidence: packages/padi/src/upgradeWindow/previousRelease.e2e.test.ts:426-434 and :475-483 — `unknownProtocolFilesOnDisk(SHARED_ARTIFACTS, RUNTIME_ROOT, stateRoot)` expected `[]`, with `unknownSharedFileMessage` as the failure text; the header at :9-11 calls out "unknown non-log files fail the suite".

Fix: Add to D9/W4: any new daemon-lifecycle artifact must be registered in `packages/padi/src/upgradeWindow/sharedArtifacts.testlib.ts` in the same commit that creates it.

---

## [blocker] #12 D5 is wrong that Effect's socket reconnect replaces `ClientRetryPlugin`+`STREAM_RETRY`. With `RpcClient.layerProtocolSocket({retryTransientErrors:true})`, the close of an ESTABLISHED socket kills every in-flight stream and unary call; the transport reconnects but nothing re-issues the requests. `retryTransientErrors` only suppresses the failure broadcast when the reason is `SocketOpenError` (a socket that never opened) — a live socket closing yields `SocketCloseError`, so `ClientProtocolError` is broadcast and `clearEntries(Exit.fail(...))` fails every entry. D3's "reconnect snapshot leads a fresh stream" invariant and `createSubscription`'s change-iff-fired law rest on today's TRANSPARENT re-subscribe, which does not exist here.

Evidence: node_modules/effect/dist/unstable/rpc/RpcClient.js:648-660 (`SocketCloseError({code:1000})` on close; `if (options?.retryTransientErrors && ... reason._tag === "SocketOpenError") return Effect.void;` else broadcast `ClientProtocolError`), :396-401 + :55-64 (`ClientProtocolError` → `clearEntries` fails every entry), :670-680 (`Effect.retry(defaultRetryPolicy)` reconnects the socket only). Empirically reproduced in /tmp/.../scratchpad/probe: a client built exactly per D5 logged `STREAM DIED: Cause([Fail(~effect/rpc/RpcClientError: SocketCloseError: 1005)])` when the server was killed, and never resumed after the server came back. Contrast packages/surface/src/client.ts:130 `STREAM_RETRY = {retry: Infinity, retryDelay: o => o.lastEventRetry ?? 1000, shouldRetry: shouldNotRetryORPCError}` and client.ts:41.

Fix: Amend D5/D3: the surface client must own an explicit per-subscription retry fence INSIDE the Stream, before it reaches the Solid bridge — `Stream.retry` with a schedule, gated on a predicate that retries `RpcClientError` and `SurfaceRelayTransportLost` forever and never retries a declared (D4) error, i.e. a direct port of `shouldNotRetryORPCError`. Also record that `lastEventRetry` (the server-suggested SSE backoff) has no Effect RPC counterpart, so the schedule becomes fixed/app-supplied, and add an acceptance test that a mid-stream socket drop yields exactly one fresh snapshot with no error surfaced to `createSubscription`.

---

## [major] #13 D2 does not say which SIDE of the schema the client face accepts, and Effect RPC's answer inverts today's documented behaviour: the generated client takes the make-in/Type side, not the Encoded side. Consequence: every field carrying a decoding default becomes a REQUIRED argument at the call site, where today `.default()` makes it omittable. This directly invalidates the type-level spec the repo pins.

Evidence: node_modules/effect/dist/unstable/rpc/RpcClient.d.ts:48 and :66 pass `Rpc.PayloadConstructor<...>`, defined at Rpc.d.ts:296 as `_Payload["~type.make.in"]`. Compiled probe against effect@4.0.0-beta.102: payload `Schema.Struct({name, finishedIds: Array(String).pipe(withDecodingDefaultKey(Effect.succeed([])))})`, call `client.p({name:"x"})` → `TS2741: Property 'finishedIds' is missing ... but required in type 'ReadonlyMakeIn<...>'` (the Encoded literal `{name:"x"}` compiles fine). Today: packages/surface/src/solid/surfaceClient.ts types `BoundProcedure`'s arg as `z.input<In>` and result as `z.output<Out>`, pinned by packages/surface/src/solid/boundProcedure.test-d.ts — which recon/zod.md:115 calls "the specification for the trickiest inference behaviour".

Fix: State in D2 that the spec-derived face exposes the ENCODED side for inputs, and make that true by one of: (a) pairing every `withDecodingDefault*` with `Schema.withConstructorDefault` so make-in stays omittable, or (b) having the hand-built face encode payloads itself before handing them to the flat dispatch fn. Port `boundProcedure.test-d.ts` as a type-level gate in W2 rather than deleting it.

---

## [major] #14 D8's invariant list is incomplete: `Schema.toJsonSchemaDocument` in beta.102 differs from zod's `z.toJSONSchema` in five measured ways, two of which are outright MCP-host breaks, not deliberate snapshot churn. It also has none of the five options the current glue pins (`io`, `unrepresentable`, `reused`, `cycles`, `target`) — its whole option surface is `{additionalProperties, generateDescriptions, includeAnnotationKey}`.

Evidence: Measured side by side (effect@4.0.0-beta.102 vs zod 4.3.6 in packages/surface-mcp): (1) object — zod emits no `additionalProperties`, Effect emits `additionalProperties:false` on EVERY object, closing every tool input; (2) `Schema.Number` → `anyOf:[{type:"number"},{type:"string",enum:["Infinity","-Infinity","NaN"]}]` vs zod `{"type":"number"}` (`Schema.Finite`→`{type:"number"}`, `Schema.Int`→`{type:"integer"}` are the faithful mappings); (3) `Schema.Void`/`Schema.Undefined` → `{"type":"null"}` vs zod `{}`, so `enforceObject` (packages/surface-mcp/src/jsonschema.ts:200-217) turns a no-arg tool into `{type:"object",properties:{value:{type:"null"}},required:["value"]}` — hosts must now send `value:null`; (4) the `default` keyword is dropped — packages/surface-mcp/src/jsonschema.test.ts:26 asserts `props.strict` matches `{type:"boolean", default:true}`; (5) `$defs` live on `JsonSchema.Document.definitions` while `$ref`s point at `#/$defs/<name>`, so `collectDefs` (jsonschema.ts:161-175, which reads `$defs`/`definitions` off the schema object) resolves nothing. Options surface: node_modules/effect/dist/Schema.d.ts:9973-10040.

Fix: Extend D8's invariant list to name all five and their fixes: pass `additionalProperties:true` (or per-node annotation) to keep tool inputs open; use `Schema.Finite`/`Schema.Int` for every numeric MCP-facing field; special-case `Schema.Void`/`Undefined` in `inputSchema` to return `emptyObjectSchema()` before `enforceObject`; re-emit `default` via a `default` annotation on defaulted fields; rewrite `collectDefs` to read `Document.definitions` while matching `#/$defs/<name>` pointers. Keep jsonschema.test.ts's assertions as the gate rather than re-snapshotting them away.

---

## [major] #15 D5's plan for per-caller context — "an `RpcMiddleware` that provides a `CurrentViewer` service from the HTTP request" — cannot supply `viewerAddress`. Effect's websocket RPC protocol forwards only the upgrade request's HEADERS into each RPC message, and middleware sees only `{client, requestId, rpc, payload, headers}`. `forwardedFor` (a header) survives; `req.socket.remoteAddress` does not.

Evidence: packages/server/src/index.ts:1194-1195 `viewerAddress: req.socket.remoteAddress, forwardedFor: req.headers["x-forwarded-for"]`, consumed at packages/server/src/router.ts:123-129. node_modules/effect/dist/unstable/rpc/RpcServer.js:596 `yield* onSocket(socket, Object.entries(request.headers))` and :955-959 `message.headers = headers.concat(message.headers)` — headers only. node_modules/effect/dist/unstable/rpc/RpcMiddleware.d.ts:44-50 and :95-101 — the middleware options carry no request and no remote address. (`HttpServerRequest.remoteAddress` exists at unstable/http/HttpServerRequest.d.ts:78 but is not in the middleware's reach.)

Fix: Amend D5: do not use the `layerProtocolWebsocket` convenience for the browser leg. Hand-wire `RpcServer.makeProtocolWithHttpEffectWebsocket` (or `toHttpEffectWebsocket`) and wrap its `httpEffect` to read `HttpServerRequest.remoteAddress` and inject it as a synthetic header before `onSocket`, so the `CurrentViewer` middleware can read it — or decide explicitly to drop `viewerAddress` and record the behaviour change in `hosts/viewer`.

---

## [major] #16 D1/D2 assume a dynamically-assembled `RpcGroup` behaves like today's contract walk. It serves fine at runtime, but it carries ZERO type-level safety AND — unlike `defineSurface` today — silently drops colliding tags instead of throwing. `RpcGroup.make`/`merge` are plain `Map.set`, last writer wins.

Evidence: Compiled + ran against effect@4.0.0-beta.102: `RpcGroup.make(...arr)` with `arr: Array<Rpc.Any>` gives `RpcGroup<Rpc.Any>`; `group.toLayer({})` — an EMPTY handler object — compiles with no error, as does `toLayer({"totally/unknown/tag": ...})`; `Rpc.PayloadConstructor<Rpc.ExtractTag<Rpc.Any, Tag>>` resolves to `never`, so the flat dispatch fn is uncallable and returns `never` without casts (`TS2345: Argument of type 'any' is not assignable to parameter of type 'never'`). Runtime path verified end-to-end (dynamic group → `layerHttp` websocket + ndjson → flat client stream + unary, both OK). Collision behaviour: node_modules/effect/dist/unstable/rpc/RpcGroup.js `make = (...rpcs) => makeProto({requests: new Map(rpcs.map(rpc => [rpc._tag, rpc]))})` and `merge(...groups){ ... requests.set(tag, rpc) }`. Today packages/surface/src/define.ts:989-1000 `claim()` THROWS on a duplicate (key, verb), and packages/surface/src/define.test.ts:29-34 asserts wire paths via `StandardRPCMatcher`.

Fix: Add to D1: the spec walk must carry the `claim` duplicate-throw forward itself (slash-joining makes the tag namespace flat, so `member="conn/get"` now collides with `member="conn", verb="get"`), and every `make`/`merge`/`prefix` must be followed by an assertion that `group.requests.size` equals the expected tag count. Note explicitly that sibling composition by `merge` collides the three reserved `system/*` tags across siblings — use `prefix` (or per-sibling tag minting), not `merge`. Replace the deleted `StandardRPCMatcher` path tests with tests asserting the exact `group.requests` key set.

---

## [major] #17 The zod→Schema mapping for `.optional()` and `.default()` is not byte-neutral, and the plan never names which of the four Effect constructs each zod idiom maps to. `Schema.optional` JSON-encodes an explicit `undefined` as `null` (zod omits the key), and NEITHER default variant reproduces zod `.default()`: `withDecodingDefaultKey` REJECTS an explicit `undefined`, while `withDecodingDefault` accepts it but widens the Encoded side to `T | undefined` and encodes it as `null`.

Evidence: Measured against effect@4.0.0-beta.102: `encodeUnknownSync(toCodecJson(Struct({a:String, b:optional(String)})))({a:"x", b:undefined})` → `{"a":"x","b":null}` (with the key absent → `{"a":"x"}`). `decodeUnknownSync(Struct({id, finishedIds: Array(String).pipe(withDecodingDefaultKey(Effect.succeed([])))}))({id:"x", finishedIds: undefined})` → THREW `SchemaError(Expected array, got undefined at ["finishedIds"])`, while the missing key correctly yields `[]`. `withDecodingDefault` JSON schema for a `NullOr(String)` field: `anyOf:[{anyOf:[string,null]},{type:"null"}]`. This lands squarely on recon/zod.md's hit list: the conf store (packages/server/src/state.ts `PersistedStateSchema`), the user-exportable `SavedSessionSchema` (packages/client/src/sessionTransfer.ts), the tolerant seed path (packages/padi/src/terminalEndpoint/local.ts), and the six rolling-deploy `.default([])`/`.default(null)` sites.

Fix: Add a mapping table to the plan (not just "write a round-trip test"): zod `.optional()` → `Schema.optionalKey` for every WIRE/DISK field (never `Schema.optional`, which round-trips `undefined` through `null`); zod `.default(v)` → `withDecodingDefaultKey` for wire fields (encoded stays `T`, key optional) with an explicit note that it is stricter than zod on in-memory `undefined`, so any in-process `.parse()` caller (migration ladder, `backfillSavedSession`) must strip `undefined` keys first. Require a byte-level fixture test per hit-list format asserting the encoded JSON string, not just decode-equality.

---

## [minor] #18 There is no `signal` anywhere in Effect RPC — neither in per-call client options nor in handler options — yet three AbortSignal-shaped functions are PUBLIC `@kolu/surface` exports consumed by drishti/odu, and the whole server-side `deps.source(input, signal)` contract is AbortSignal-based. D3 addresses only the UI-edge fiber.

Evidence: node_modules/effect/dist/unstable/rpc/RpcClient.d.ts:48-56 (unary `{headers, context, discard}`) and :66-74 (streaming `{asQueue, streamBufferSize, headers, context}`) — no `signal`. Rpc.d.ts:387-391 `ToHandlerFn` options are `{client, requestId, headers, rpc}` — no `signal`. packages/surface/src/server.ts references `signal` 101 times and EXPORTS `isAbortReason` (:1128), `iterateUntilAborted` (:1145), `superviseTerminalSource` (:1659); recon/surface.md §5 lists all three under the public "Serving" API. packages/surface/src/client.ts:175 `unenrolledStreamCall(proc, input, {signal})` is likewise public.

Fix: Add a D-decision for the AbortSignal→interruption boundary: name the replacement signature for `StreamSpec.source`/`EventSpec.source` (fiber-interruptible `Stream` vs `(input, signal)`), decide whether `isAbortReason`/`iterateUntilAborted`/`superviseTerminalSource` keep AbortSignal shapes (bridged at the edge) or change signature, and flag the latter as a public API break in the deferred drishti/odu follow-up list rather than discovering it in W4.

---

## [blocker] #19 D6's wire break makes `ci::upgrade-window` structurally unpassable, and no amount of skew-path design fixes it. The suite's *fixture* — not its assertions — dials previous-release daemons with the CURRENT client library before any version negotiation happens: `newReadsOld` boots the previous-release kaval and then probes it via a dynamic `import("@kolu/surface/links/unix-socket")` + `surface.system.heartbeat({})`, retrying until it succeeds. Once the unix-socket framing changes from the oRPC peer protocol to Effect ndjson (D5), the current link can never complete that handshake against a v-previous binary. The plan treats this suite as 'the acceptance criteria — read them FIRST', which reads as if the suite is a spec to satisfy; it is partly a harness that must itself be rewritten. `ci::upgrade-window` runs on BOTH platforms under `KOLU_UPGRADE_WINDOW_REQUIRE=1`, so it cannot be skipped or same-versioned away.

Evidence: packages/padi/src/upgradeWindow/previousRelease.e2e.test.ts:311-325 (`waitForSocket(kavalSocket, …)` → `link({socketPath})` → `surface.system.heartbeat({})` against `window.bin`, the previous-release kaval built by ci/mod.just:308); ci/mod.just:253,325-331 (`KOLU_UPGRADE_WINDOW_REQUIRE=1`, both platforms); .agency/effect-migration/PLAN.md:93-99 (D6)

Fix: Amend D6 with an explicit fixture-rewrite item: the previous-release readiness probe must become transport-neutral (a bare `net.connect()` on the socket path, or polling the kaval gate file that :330-337 already reads), and the 'compatible contract → adopt' arm at :383-386 must be re-stated as permanently the recycle arm. Do this in W4 alongside the transport change, not at W7 when CI first runs it. Note in the plan that `oldReadsNew`'s reverse arm (:523-534) is unaffected because it dials the CURRENT kaval.

---

## [blocker] #20 The plan pins the Effect stack to the byte but never names the compiler, and this repo typechecks with TypeScript 7.0.2 (the native tsgo-based `tsc`), not TS 5.x. Effect 4 beta's type-level machinery — variance-annotated `Effect<A,E,R>`, `Context.Service` self-types, RpcGroup/Schema mapped-and-conditional inference — is the single most demanding workload for a compiler reimplementation, and the plan's own D2 already anticipates TS2590 blowups. Because the repo-wide typecheck gate is deliberately deferred to W4, a compiler incompatibility would surface only after `surface`, `surface-map`, `surface-remote`, `surface-daemon`, `surface-app`, `surface-mcp` and every app schema have already been rewritten — three waves of unwindable work.

Evidence: packages/surface/package.json devDependencies `"typescript": "^7.0.2"` (same in every package); pnpm-lock.yaml:5764 resolves `typescript@7.0.2` (5.9.3 also present, for other consumers); every package's `typecheck` script is `tsc --noEmit`; nix/pnpm-typecheck.nix runs `pnpm typecheck` as a required input of `packages.default`, so `ci::nix` inherits it; .agency/effect-migration/PLAN.md:136-141 (W3 'repo-wide typecheck may be red until W4')

Fix: Add a W1 acceptance step before any framework rewrite: a throwaway spike file that exercises the four hardest shapes at once — an `RpcGroup` with a streaming member, `Schema.TaggedErrorClass`, `Context.Service` + `Layer`, and the D2 `SurfaceTypes<S>` conditional-type face — typechecked with the repo's own `tsc` (typescript@7.0.2) and with `skipLibCheck` in its committed state. Record the result in the plan, plus the written fallback (drop the workspace to `typescript@5.9.3`, already in the lockfile) and what that costs, before W2 starts.

---

## [major] #21 W1's 'superset up front to minimize FOD-hash churn' has already shipped an incomplete superset, and W1's stated gate ('typecheck stays green') cannot detect it — no source imports effect yet, so the gate is vacuously true. Two concrete holes are already committed: `@kolu/surface-daemon-supervisor` got `@effect/platform-node` but NOT `effect` (it is in padi's dependency closure, so the first `import { Effect } from "effect"` in its runtime source is an `undeclared` violation in padi's dep-edge guard, not just a resolution error); and `@kolu/surface` — the browser-facing framework whose `src/links/websocket.ts` is documented as 'the browser-facing member of the family' — got `@effect/platform-node` and NOT `@effect/platform-browser`, the inverse of what D5 needs. Under pnpm's isolated node_modules each missing edge is a `tsc` failure whose fix is a package.json edit → lockfile change → fresh FOD hash, i.e. exactly the churn the wave exists to prevent.

Evidence: commit 51b2e215e; packages/surface-daemon-supervisor/package.json dependencies = {@effect/platform-node, @kolu/surface, @kolu/surface-daemon, osfacts-client, ts-pattern} (no `effect`); packages/surface/package.json has `@effect/platform-node` + `effect`, no `@effect/platform-browser`, while packages/client and packages/surface-app got `@effect/platform-browser`; packages/surface/src/links/websocket.ts:1-16 header; packages/daemon-test-gate/src/runtimeDepEdges.testlib.ts:355-372 (`owner.dependencies?.[pkgName] === undefined` → `undeclared`), consumed by packages/padi/src/daemonBoot/buildId.closure.test.ts; no .npmrc, so node-linker is isolated

Fix: Add a W1 acceptance step that actually proves the superset: drop a one-line canary import (`effect`, plus `@effect/platform-node` or `@effect/platform-browser` per tier) into `src/` of every member the plan will touch, run `pnpm typecheck` + `pnpm --filter @kolu/padi test:unit src/daemonBoot/buildId.closure.test.ts`, then delete the canaries — one lockfile edit, one FOD hash. Fix the two known holes now, and decide the tier for the members in padi's closure that currently have no effect dep at all but are D9 service targets: @kolu/serve-dir, kolu-pty, kolu-io, @kolu/log, @kolu/heap-diag, @kolu/terminal-protocol.

---

## [major] #22 Pinning the stack via the pnpm `catalog:` protocol breaks the framework packages for their pinned consumers, which is a hard install failure rather than the 'deferred review' the plan books it as. `catalog:` is workspace-local; drishti and odu do not consume `@kolu/surface*` through kolu's pnpm workspace, they vendor the package DIRECTORY from a content-addressed pin and install its `dependencies` from their own workspace. A `"effect": "catalog:"` spec in `packages/surface/package.json` therefore resolves against *their* catalog and errors when they have none. The repo already has the right mechanism for 'one version everywhere, visible to transitive resolution' — root `pnpm.overrides` — and uses it for exactly this purpose (`hono`, `@xterm/xterm`, `@xterm/addon-webgl` are pinned there).

Evidence: packages/surface/package.json, packages/surface-daemon/package.json, packages/surface-remote/package.json, packages/surface-map/package.json, packages/surface-app/package.json all now carry `"effect": "catalog:"` (commit 51b2e215e); packages/surface-daemon/nix/workspace-closure.nix:46-54 documents the pinned-consumer shape ('a PINNED consumer … spells a pinned edge however its manifest spells a pin (a version, a `file:`, a catalog entry)'); package.json:19-39 `pnpm.overrides`; .claude/rules/surface.md (drishti + odu are production consumers); .agency/effect-migration/PLAN.md:20-22 (locked decision 3)

Fix: Spell literal exact versions in the seven `@kolu/surface*` package.json files (and add `effect`/`@effect/*` to root `pnpm.overrides` so kolu still gets one version everywhere), keeping `catalog:` only for kolu-private members. If catalog is kept, the plan must state in the same breath that drishti and odu each need a mirrored catalog block before their next pin bump, and that becomes a `breaks-at-bump` ledger line on odu#43 per .claude/rules/surface.md.

---

## [major] #23 The plan's standing rule 5 names `nix/workspace.nix:178` as the only Nix file an agent must touch, but `default.nix`'s per-daemon `stableLeaves` lists are equally load-bearing and fail eval — loudly and repo-wide — the moment a dependency edge leaves a daemon's closure. `depClosure` asserts every `stop` entry is present in the un-stopped closure. D2 dissolves surface-map's link machinery ('keyInjectingLink becomes a dispatch wrapper', 'leafAt becomes tag concatenation'), D5 deletes the peer/stdio-codec machinery, and W6 purges every `@orpc/*` dep — any of which can drop `@kolu/surface-map` or `@kolu/surface-remote` from `@kolu/padi`'s `dependencies`, or `@kolu/surface-daemon`/`@kolu/shell-quote` from `kaval`'s. When that happens `nix eval` throws and every nix-building CI node reds at once (ci::nix, smoke, e2e, home-manager, agent-flake-nix, upgrade-window, the three example flakes) with a message about 'stop entries' that names no wave.

Evidence: packages/surface-daemon/nix/workspace-closure.nix:168-172 (`stale = lib.subtractLists full stop` + `assertMsg (stale == [])`); default.nix:284-302 (padi stableLeaves: @kolu/surface, @kolu/surface-remote, @kolu/surface-map, @kolu/xterm-kit, @kolu/heap-diag); default.nix:210-253 (kaval stableLeaves incl. @kolu/surface-daemon, @kolu/shell-quote, osfacts-client); .agency/effect-migration/PLAN.md:163 (rule 5)

Fix: Rewrite standing rule 5 to cover both files: 'After ANY change to a package.json `dependencies` block, refresh nix/workspace.nix:178 AND re-check default.nix stableLeaves for both daemons — a removed edge that drops a leaf out of the closure is a hard nix eval error.' Add a W6 checklist line: after the @orpc purge, run `nix eval .#padi.drvPath` and `nix eval .#kaval.drvPath` before commit. Also note in W1 that adding `effect` to package.jsons already flips both PADI_BUILD_ID and KAVAL_BUILD_ID (package.json is hashed into behavioralFileset — workspace-closure.nix:188-193), so the kaval currency nudge fires for every user on this release.

---

## [major] #24 The e2e-governance node is pinned to specific padi UNIT tests by file path AND exact test title, and W4 rewrites exactly those areas. `validateCollectedTests` collects each ledger-referenced file and throws if the named test isn't in the collected set. Three padi unit files are cited as active coverage: `session/reconcile.test.ts`, `session/session.test.ts`, `terminalEndpoint/sleepWake.test.ts` — all inside the session/terminal-endpoint machinery that W4's Layer-graph and singleton→Service rewrite touches. Plan standing rule 4 only forbids renaming e2e *scenarios*, so an agent doing a legitimate padi refactor has no warning. `ci::e2e-governance` depends only on `install`, so it reds independently of the nix gate and will not be masked by the W2–W3 red window.

Evidence: packages/tests/governance/ledger.ts:179-193 (`validateCollectedTests` → `collected.has(reference.test)` → throw); packages/tests/coverage-ledger.yaml:749,923,952 (the three padi files, each with a `test:` title and `lane: unit`); ci/mod.just:223-224 (`e2e-governance: install`); .agency/effect-migration/PLAN.md:161-162 (rule 4)

Fix: Extend standing rule 4: 'The file paths AND `it(...)` titles of packages/padi/src/session/reconcile.test.ts, packages/padi/src/session/session.test.ts and packages/padi/src/terminalEndpoint/sleepWake.test.ts are frozen by packages/tests/coverage-ledger.yaml — renaming, moving, or merging one requires a new ledger row in the same commit.' Add a W4 pre-flight: `just --no-deps test-e2e-governance` after the padi rewrite, before moving to W5.

---

## [minor] #25 Standing rule 6 promises a lint gate that structurally cannot fire on the code this migration produces. `noFloatingPromises` and `noMisusedPromises` are Promise-typed rules; an `Effect<A,E,R>` that is constructed and never run — the canonical Effect footgun, and precisely the failure mode at the `runFork`/`runPromise` edges the rule cites — is not a Promise and is invisible to Biome and to `tsc` alike. So the plan believes it has mechanical enforcement of D10 ('every such edge is a real boundary, not a convenience hatch') when it has none; after W6 the biome lane's Promise rules will be near-dead code over the migrated tiers.

Evidence: biome.jsonc:91-101 (`noFloatingPromises`/`noMisusedPromises` under `nursery`, type-aware via the `project` domain at :86-88); .agency/effect-migration/PLAN.md:164-165 (rule 6: 'no floating promises … at runFork edges'); PLAN.md:120-123 (D10)

Fix: Drop the false assurance from rule 6 and replace it with a real one the repo already knows how to build: a lint-pin plus a unit test enumerating the sanctioned `Effect.runFork`/`runPromise`/`runSync` call sites — the same shape as the `@preact/signals-core` `noRestrictedImports` ban plus its two-file override (biome.jsonc:148-155, 263-278), which is how the reactor boundary is held by construction rather than by review.

---

## [minor] #26 The `define.ts` biome override is invalidated by W2 and the plan's only mention of overrides ('a scoped biome override needs a written reason') points the wrong way — the problem is an EXISTING override whose reason expires. It is scoped to exactly one path and justified by two things the migration deletes: the `(contract as any).surface` walk-by-string in `composeSurfaceContracts` (which D1 replaces with RpcGroup prefixing) and 'every value flows through Zod schemas at the edges' (locked decision 2 removes zod entirely). Either the file survives carrying a rationale that is now false, or D2's spec-walking conditional-type machinery lands in new files the override does not cover and `ci::biome --error-on-warnings` reds on `noExplicitAny` — discovered at W6 ('biome clean'), after the framework is already rewritten.

Evidence: biome.jsonc:178-197 (override `includes: ["packages/surface/src/define.ts"]`, rationale naming `composeSurfaceContracts` and Zod); ci/mod.just:361-362 (`biome lint --error-on-warnings .`); .agency/effect-migration/PLAN.md:42-46 (D1), :47-55 (D2), :17 (zod fully replaced), :146 (W6 'biome clean')

Fix: Make re-scoping and re-justifying this override an explicit W2 deliverable, not W6 cleanup: either delete it (if Effect Schema's inference removes the need for `any`), or restate the reason in Effect terms and widen `includes` to whatever files D2's spec walker actually lands in. Run `biome lint --error-on-warnings packages/surface` as part of W2's stated gate.