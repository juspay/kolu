[serve]
port = 5173

[[proxy]]
rewrite = "/api"
backend = "http://localhost:7681/api"

# Trunk handles WS upgrade automatically — use http:// not ws://
[[proxy]]
rewrite = "/ws"
backend = "http://localhost:7681/ws"
