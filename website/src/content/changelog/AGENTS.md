# AGENTS.md
<!-- Generated by APM CLI from distributed .apm/ primitives -->
<!-- Build ID: 8430460e4293 -->

## Files matching `website/src/content/changelog/**`

## Changelog entries

- **A `<Change>` in `changelog/unreleased.mdx` is your trigger, not your finish line.** A changelog entry is the most reliable signal that user-facing behaviour changed — so writing one is the moment to ask *which docs page did this just make stale?* A change can invalidate a page it never opens: a "known gap / for now / currently shows X" sentence you just closed, a table row, a flag you renamed. Grep `website/src/content/docs/` for the behaviour you're changing and fix the page **in the same PR** — the failure mode is not a missing doc, it's an existing sentence quietly turned into a lie. (Real slip: PR #1730 landed the honest-connect UI and wrote a perfect changelog entry, but left `remote-hosts.mdx` telling users the opposite; #1746 fixed the page and this glob.)
- **Write each `<Change>` for the reader coming from the LAST RELEASE — and iterate by EDITING it, not appending.** Two rules, one audience: the person upgrading from the previous release, not someone who watched yesterday's commits. *(1) Release-relative, not commit-relative* — describe what changed **since the last release**, in their terms; never narrate internal iteration ("fixes the regression the previous fix introduced"), and never leak internal phase/campaign vocabulary (SR8, W5) into the entry text — the PR refs carry that archaeology. The **`kind` is release-relative too**: you cannot *fix* (or *change*) what the last release never shipped, so a `<Change>` touching a feature that is **itself still unreleased** describes it as `added` and folds into that feature's existing `added` entry (extend its prose, add your PR to its refs) — never a standalone `fixed`/`changed` note the upgrading reader has no prior state to make sense of. (Real slip: PR #1876 first logged its kaval contract-skew handling as its own `fixed` entry, but kaval ships for the **first time** this release — so it folded into the kaval `added` entry instead.) *(2) Iterating on an unreleased entry EDITS it* — when your PR revises a feature or fix that already has an unreleased `<Change>`, rewrite that entry so its prose stays net-true and **add your PR to its refs** (`prs={[…]}` renders a chip per PR); appending a *second* entry is only for a change with **no** existing unreleased entry. Otherwise one release's worth of iteration leaves N entries where the reader needs ONE — the drift a release then has to reconcile by hand. Mechanically, keep each note under the matching product-area `###` heading, whose label links to the same docs page, and put its editorial type on the entry as the required `kind="added|changed|fixed|heads-up"` prop; categories organize the page, while `kind` powers the per-entry pill and filters.
