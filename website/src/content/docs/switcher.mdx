---
title: The Switcher
description: "One search box for workspaces, hosts, and commands. How root ranking works, which chords deep-link into scoped views, and what stays separate."
order: 48
section: Use Kolu
koluHero:
  eyebrow: one surface
  headline: One box that finds {everything}.
---

import Aside from "../../components/docs/Aside.astro";
import CardGrid from "../../components/docs/CardGrid.astro";
import LinkCard from "../../components/docs/LinkCard.astro";

The **switcher** is kolu's unified search surface. Press
<kbd>Cmd/Ctrl</kbd> + <kbd>K</kbd> and type — a branch, a repo, a host, a
command — without first deciding which drawer to open. Three older pickers
(command palette, workspace switcher, host switcher) share one index and one
muscle memory; the extra chords are deep links into scoped views of the same
surface, not separate apps.

## What opens at the root

At the empty root you see three bands:

1. **Recent** — the three most-recent live terminals (workspaces), so "go back
   to what I was doing" is often row one.
2. **Hosts** — every machine in the pool (only when more than local is
   configured), each with its connection status.
3. **Commands** — the familiar palette sections (New terminal, active-terminal
   actions, canvas, UI, help).

Type to search **across every kind at once**. Matching is multi-token: every
whitespace-separated word must appear somewhere in the row's corpus (repo,
branch, intent, agent, cwd, host label, command name…). Token order does not
matter — `kolu auth` finds the same row as `auth kolu`.

## How results rank

With a non-empty query, hits are ordered by **kind first**, not by how close the
string match is:

| Priority | Kind | Tie-break |
| --- | --- | --- |
| 1 | Workspaces (live terminals) | Recency — most recently active first |
| 2 | Hosts | Registration / pool order |
| 3 | Commands | Existing section order (workspaces → active terminal → canvas → UI → help) |

So a branch name outranks a host that happens to share a substring, and both
outrank a command. There are **no prefix sigils** (`>` commands, `@` hosts) —
scoping is the existing drill-in path (below), not a mode character.

## Deep-link chords

| Chord | Opens |
| --- | --- |
| <kbd>Mod</kbd> + <kbd>K</kbd> | Root — search everything |
| <kbd>Mod</kbd> + <kbd>Shift</kbd> + <kbd>K</kbd> | Scoped **workspace grid** (agent-state columns + repo facets) |
| <kbd>Mod</kbd> + <kbd>Shift</kbd> + <kbd>H</kbd> | Scoped **host list** (same rows as root hosts, plus host verbs) |

**Mod** is <kbd>Cmd</kbd> on macOS and <kbd>Ctrl</kbd> on Linux and Windows. The
full chord table lives under [Keyboard shortcuts](/keyboard-shortcuts).

## Scoped views

Deep links do not leave the switcher — they **drill in**. Breadcrumbs show the
path; **Backspace on an empty query** (or the breadcrumb) pops one level back to
root. Escape closes the whole surface.

### Workspaces (⌘⇧K)

The scoped workspace body is the **column grid**: Idle · Awaiting · Working ·
No agent, with a repo facet sidebar. Filter still uses the same multi-token
matcher over the full terminal corpus. Selecting a card activates that terminal
and pans the canvas to it — the same verb a root workspace row uses.

Use the grid when you are **browsing** ("who needs me?") rather than jumping to
a name you already know.

### Hosts (⌘⇧H)

The scoped host list is the pool as plain rows (connection status, active
marker). Selecting a host re-keys the canvas to that machine with no reload —
same as clicking a chip in the [host strip](/remote-hosts). The chrome-bar chips
stay: they are **ambient status** (dots, awaiting pills), not a second switcher.

## Reading a row

Every result kind paints the same four slots so a mixed list stays scannable:

1. **Lead** — agent-state pip (workspace), connection dot (host), or glyph
   (command).
2. **Identity** — repo colour + branch; `user@host`; or the command name.
   Matched tokens bold in place.
3. **Context** — one truncating line: intent / foreground, host status, or
   command description.
4. **Right rail** — recency, keybind chip, drill chevron, and a kind tag
   (`ws` / `host` / `cmd`) **only at the mixed root** — not inside a scoped
   view where every row is already the same kind.

## What this is not

- The **dock** is still the always-visible navigator down the left edge —
  glanceable without opening anything. The switcher is the jump / search path.
- The **host strip** is still ambient multi-host status; ⌘K does not replace it.
- Sleep, restore, and session memory live under [Sessions](/sessions); the
  switcher only **finds** terminals that already exist on the active host.

<Aside type="note" title="Mobile">
  On a phone the same chords and palette open from the chrome sheet; the
  spatial canvas is gone, but search and host switch still reach every live
  terminal and pool member. See [Mobile](/mobile).
</Aside>

## Next

<CardGrid>
  <LinkCard title="Keyboard shortcuts" href="/keyboard-shortcuts" description="every global chord, including the three switcher doors" />
  <LinkCard title="The Dock" href="/dock" description="the always-on terminal list the switcher jumps into" />
  <LinkCard title="Remote hosts" href="/remote-hosts" description="the host strip, pool membership, and per-host canvases" />
  <LinkCard title="First five minutes" href="/first-five-minutes" description="open a repo and launch an agent from the palette" />
</CardGrid>
