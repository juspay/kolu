---
title: The Code Tab
description: "The Code tab is a file browser scoped to the active terminal's directory: a Pierre tree with git-status tinting, Local/Branch/All views with live diffs, rendered Markdown, inline HTML/SVG/PDF/image/video previews, file comments — and plain-directory browsing when there's no git repo at all."
order: 46
section: Use Kolu
koluHero:
  eyebrow: read the repo
  headline: The files, {beside the work}.
---

import Aside from "../../components/docs/Aside.astro";
import LinkCard from "../../components/docs/LinkCard.astro";
import CardGrid from "../../components/docs/CardGrid.astro";

The **Code tab** lives in the [right panel](/right-panel) and is a file browser
scoped to the active terminal's repo — a Pierre file tree with git-status tinting
over a syntax-highlighted source view. It is how you read what an agent is
working on without leaving the terminal beside it.

## Browse and jump

- Click a `path/to/file.ts:42` reference in terminal output to open that file at
  that line; a **folder** path (`packages/client/`) is clickable too and reveals
  the directory in the tree.
- Rhai scripts (`.rhai`) are recognized and syntax-highlighted in both source
  and diff views.
- Big files and diffs highlight on a background worker, so opening a 50k-line
  lockfile paints immediately and never freezes scrolling.

## Your git changes — live

A segmented control switches between three views: **All files** (the whole repo),
**Local** (uncommitted), and **Branch** (vs the base). The Local and Branch
segments carry a **change-count badge**, so you can see you have, say, 3
uncommitted files and 12 changes versus your branch base without switching into
either.

**All files** shows what git tracks plus anything untracked, and leaves out what
your `.gitignore` hides — so the tree is your project, not your `node_modules`.
When you do want to see the hidden ones, the **eye toggle** beside the filter box
brings them back as dimmed rows, and a fully-ignored directory arrives as a
single collapsed entry — `node_modules/` is one row, not forty thousand files.
**Expand that row and kolu reads the one level beneath it from disk**, so you can
walk down into `node_modules/solid-js/` a click at a time. Each directory you
open costs one directory read; the ones you never open cost nothing. An ignored
file listed in its own right opens and renders like any other. The toggle
remembers itself per browser, and appears only in **All files**, since the diff
views list changed files where an ignored path can't turn up.

One caveat worth knowing: nothing about an ignored file updates live. kolu
deliberately doesn't watch inside ignored directories — recursively watching
`node_modules` is what exhausts a machine's file-watch budget — so neither the
dimmed rows nor the contents of an ignored file you have open will repaint when
they change on disk. Collapse a directory and open it again to re-read what is
inside it, and reselect a file to re-read its contents.

The changed-file list and its diffs repaint **live** the instant a file changes
on disk — a payload-free repo-change pulse re-running `git status`, with no
polling — and the same loop works when the Code tab is pointed at
[another machine](/remote-hosts), so your status and diffs stay live over the
ssh hop too.

## Not a repo? Still a browser

The tree browser never needed git — only the diff views do. In a terminal whose
working directory is **not a git repository**, the Code tab shows that directory
as a single collapsed root node, and nothing is listed or watched until you
click it. Click the node and the tree opens on the directory's first level, one
lazily-read level per expanded folder — the same click-to-descend walk the
ignored-directory rows use, so browsing even your home directory costs a
handful of directory reads, never a recursive crawl. Unlike the ignored rows,
each folder you have open here **is** watched — non-recursively, one cheap
handle per open folder — so the levels you're looking at repaint live. The
file you have open rides its parent folder's watch: saves from editors (which
write-and-rename) repaint live, while a rare in-place write under heavy load
can wait for the next filesystem event — reselect the file to re-read it
immediately.

The git surfaces — the Local/Branch views, status tinting, the scope switcher,
the ignored-files eye — stay hidden there: they genuinely need a repo. `cd`
into a git repo and the tab snaps back to its usual repo-rooted self; `cd`
somewhere else non-git and the collapsed root returns, waiting for a click.
Nothing is persisted and there is no allowlist to maintain — the click is the
approval, per terminal, per session.

## Markdown and artifacts

- **Markdown** (`.md`, `.markdown`, and **`.mdx`**) renders as a reading
  document with a Source/Rendered toggle that keeps both views alive (flipping
  is instant and holds your scroll position). A `---` YAML front-matter block
  renders as a table; a footnote marker opens its definition in a popover where
  your eye already is. MDX is previewed as Markdown — component tags are not
  executed and are stripped by the sanitizer (use Source for the raw MDX).
- Agent-generated **`.html`** and **`.svg`** artifacts preview inline in a
  sandboxed iframe; **`.pdf`** opens in the browser's native viewer; images and
  **video** render as themselves. Everything live-reloads on change, and an
  external link in an HTML preview opens in a new tab.

## File comments

Select text in source, a branch diff, or a rendered artifact and pin a quoted
note to it. The comment tray flushes to Markdown you paste straight back into the
agent — a way to review a change and hand the agent your notes in its own words.

## Next

<CardGrid>
  <LinkCard title="The Right Panel" href="/right-panel" description="the panel that hosts the Code tab, and the Inspector beside it" />
  <LinkCard title="Remote Hosts" href="/remote-hosts" description="the Code tab reads the machine you're connected to" />
  <LinkCard title="Clipboard & Files" href="/clipboard" description="drop a file and its path lands on the agent's input line" />
</CardGrid>
