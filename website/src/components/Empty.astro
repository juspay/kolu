---
/**
 * An intentionally empty Starlight component override. Used to drop stock UI we
 * replace elsewhere — currently Starlight's `ThemeSelect`: the shared NavBar
 * already carries the one theme toggle, and Starlight's select (rendered in the
 * mobile menu footer) otherwise runs a client script on load that stores the
 * "auto" theme and forces the OS colour scheme, fighting our dark-by-default.
 */
---
