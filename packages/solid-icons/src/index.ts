/** `@kolu/solid-icons` — the app's inline SVG icon set as SolidJS components.
 *  Each icon is a `Component<{ class?: string }>` (a few take extra props like
 *  `active`/`state`) rendering a single `<svg>`, so callers size and colour via
 *  Tailwind `class`. Pure presentation — no Kolu-domain coupling. */
export * from "./Icons";
