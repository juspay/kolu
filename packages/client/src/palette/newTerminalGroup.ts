/** Palette group name for the create-terminal flow (Dock `+`, shortcuts, palette).
 *  Same kind of socket as Hosts / Terminals — a tiny name leaf, not a paint module. */
export const NEW_TERMINAL_GROUP = "New terminal";
