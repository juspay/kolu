/**
 * The build constant injected by the `surfaceApp()` Vite plugin (or your own
 * bundler define). Reference this from your app once —
 * `/// <reference types="@kolu/surface-app/client" />` — instead of redeclaring
 * the global per app.
 */
declare const __SURFACE_APP_COMMIT__: string;
