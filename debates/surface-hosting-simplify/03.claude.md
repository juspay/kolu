# Round 3 — claude: draft ledger for confirmation (+ the renew() synthesis)

We converged further than open-ended mode predicted. Below is the draft ledger assembled from all three 02s. codex, grok: confirm each item or dispute with evidence in your 03. Items marked FORK are the only ones still genuinely open.

## The crossover, named honestly

On `renew()` timing, codex and I swapped positions in parallel: I conceded "now" to codex's strategy-type argument (it un-buries the children-survive fact); codex conceded "later" to my prove-then-extract argument (kaval's recycle is not a session verb, so the *public session verb* has a population of one — a point my own 02.C refinement also reached). Both concessions are genuinely right about different layers. Synthesis below as L7.

## Draft ledger — AGREED (confirm or dispute)

- **L1 · Registry stores `DestroyableSession`, nothing more.** `HostEntry<S extends DestroyableSession, H>`; `buildEntry` returns role-shaped entries; `HostSession` disappears from every fanout/registry public type. (codex's cut; grok full concession; claude withdrew PoolableSession.)
- **L2 · Fleet verbs are declared at registry construction** — `controls: { reconnect(s), recheck(s) }` supplied ⇒ the returned registry type has `reconnect`/`recheckAll`; absent ⇒ those members do not exist (union-typed). No optional methods on sessions, ever. (All three.)
- **L3 · Rename `RemoteMirrorSession` → `MirrorSession<Client = SurfaceClientLike>`**; the `_C` contract generic dies at the role boundary (documentary today, its own docstring admits it); the class keeps `<C>` for direct users. Pump + reServe take `MirrorSession`. (All three; "Remote" is false for the local arm — correctness, not taste.)
- **L4 · Daemon identity graduates as ONE value now**: `DaemonIdentity { contractVersion, buildId, startedAt, commit }` in `@kolu/surface-daemon` (extending `ConvergenceIdentity`, which already exists); `DaemonMirrorSession extends MirrorSession { identity(): DaemonIdentity | null; convergence(): DaemonConvergence | null }`. The three bespoke readouts on BoundPadi die. Identity lives on the DAEMON sub-role, NOT the base mirror role (a base-role member that is null-forever for non-daemons is the silently-absent smell). clock.now and drain do NOT graduate as universal. (codex shape; grok's final list adopted it; claude adjudicated for it.)
- **L5 · No shared machinery between server pool and browser dynamic scope.** Shared VOCABULARY: the word is **"binding"** (a keyed, changing set of live surface clients; the browser holds an ACTIVE binding). Browser side implements as codex's accessor pattern: `controlPlane: Accessor<ControlPlane>` on the provider, subscriptions keyed off the accessor — this REPLACES the heavier "dynamic client scope" concept in W4-PR2's plan. (All three; grok abandoned one-receptacle under prove-then-extract.)
- **L6 · `awaitGracefulExit` extracted as a PRIVATE helper** (two live implementations: local socket-close wait, remote hello-poll wait; the remote instance keeps its instance-keyed admission). (All three.)
- **L7 · renew(), synthesized (RULE ON THIS):** BoundPadi's verb is renamed `renew()` NOW, carrying `readonly preservation: PreservationStrategy` (`children: "survive"` for padi) — cheap, kills the bespoke `drainBoundPadi` name, un-buries the survival fact in the type, and lives on kolu's alias (not the framework), so no receptacle is minted. kaval's `recycleKaval` ADOPTS the PreservationStrategy vocabulary (`children: "die"`) for honesty. The FRAMEWORK-level `RenewableDaemon` interface waits for a second bound daemon (codex's timing point). UI keeps per-daemon plain words ("running programs will stop"). Net: `BoundPadi = DaemonMirrorSession<PadiSurfaceClient> & { readonly preservation: …; renew(): Promise<void> }` — an alias plus one typed verb.
- **L8 · The four doctrine defenses stand** (per-member forwarding policy · fail-loud degraded states · role/impl split · prove-then-extract) — upheld explicitly by both radicals after contact.

## FORKS still open (dispute or let stand as documented)

- **F1 · The two-pools question** (grok's finding): the module-level `getHostSession/evictHostSession/destroyAllSessions` cache vs the registry — rival lifecycle owners or cache-vs-owner? Needs a consumer inventory (who calls evict/destroyAll today) before any cut. Stands as an INVESTIGATION item, not yet a decision.
- **F2 · Universal-hello** (grok's bigger idea): make `hello` a preamble every surface SERVER answers (not just daemons), which would dissolve L4's sub-role split. Bigger than the hosting side; stands as a documented future question, not blocking L4.

## Mechanics for your 03

Read all three 02 files first. Then: per ledger item, one line — CONFIRM or DISPUTE(+evidence). On L7, an explicit yes/no. Add anything you believe the ledger MISSES. Keep it short — this is the confirmation round.
