# Round 3: codex confirmation

L1: CONFIRM - registry code needs `destroy()` for ownership; mirror methods belong to pump/reServe, not `HostEntry`.

L2: CONFIRM - controls declared at construction, with typed return shape; no `reconnect?()` / `recheck?()` on sessions.

L3: CONFIRM - `RemoteMirrorSession` is false for the local arm, and the role boundary should not carry the current documentary `_C`.

L4: CONFIRM - daemon identity belongs on `DaemonMirrorSession`, not base `MirrorSession`; `clock.now` and `drain` do not graduate.

L5: CONFIRM - server registry and browser active binding share vocabulary, not machinery; `Accessor<ControlPlane>` is the small browser cut.

L6: CONFIRM - private `awaitGracefulExit` is proved by local socket-close wait plus remote hello-poll wait.

L7: CONFIRM, YES - `BoundPadi.renew()` now, `preservation.children = "survive"` in the alias; kaval adopts `children = "die"` vocabulary; framework `RenewableDaemon` waits.

L8: CONFIRM - all four doctrine defenses stand, with role/impl and prove-then-extract strengthened by the registry cut.

F1: CONFIRM AS INVESTIGATION - my decision remains cache-vs-owner, not rival pools; current repo inventory shows `evictHostSession` has no production call, while `destroyAllSessions` is used by the example monitor.

F2: CONFIRM AS FUTURE QUESTION - universal server `hello` could collapse the daemon sub-role later, but it is bigger than hosting and does not block L4.

Missed: L4 should state the field mapping explicitly before implementation: current code has both convergence currency (`buildId`/`staleKey`) and navigable commit (`commit`/`navigableCommit`); do not silently merge those meanings.
