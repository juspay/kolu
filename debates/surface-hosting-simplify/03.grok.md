# Round 3 — grok: confirmation

Read: 02.claude.md, 02.codex.md, 03.claude.md (and quick greps on evict/destroyAll + prior code grounding).

L1: CONFIRM (codex's DestroyableSession cut is the smallest true role the registry body actually uses; hostFanout.ts:404/431/457 only ever call destroy; pump owns the rest).

L2: CONFIRM (declared at construction via controls; no `?()` on any session; matches F4 precedent and kills silent no-op).

L3: CONFIRM (rename to MirrorSession<Client=...> + kill `_C` at role boundary; variance test and hostSession.ts:206-231 confirm `_C` was documentary only).

L4: CONFIRM (DaemonMirrorSession sub-role + single DaemonIdentity; identity on base would be null-forever for non-daemons; clock.now/drain stay per-daemon).

L5: CONFIRM (vocabulary "binding" only; machinery split by prove-then-extract; codex accessor pattern for browser is the minimal delta).

L6: CONFIRM (private helper; two live impls already exist).

L7: YES (middle path is the ratifiable synthesis: BoundPadi alias gets `renew()` + `preservation` NOW; framework RenewableDaemon waits for second bound daemon. Kills bespoke name without minting receptacle for one).

L8: CONFIRM (all four doctrines survive and are strengthened by the narrower registry role).

## F1 consumer inventory (quick grep + code)

- `evictHostSession`: only in hostSessionPool.test.ts (2 test cases); README + hostSession.ts:970 comment claim "use from buildHostRegistry.remove" but hostFanout.ts remove does direct `entry.session.destroy()` (no evict call); no production calls anywhere. (Docs/tests only.)
- `destroyAllSessions`: exported; called on shutdown in packages/surface/example/remote-process-monitor/src/server/main.ts:171; README says "Call on parent shutdown"; no calls in kolu server, remotePadiBinding, or registry paths (they destroy held sessions directly). Module pool + registry destroy are separate today.

F2: let stand (universal-hello is bigger than this scope).

Short confirmation per instructions. No re-argument. All items cross-checked against cited files (hostFanout.ts, hostSession.ts, remotePadiBinding.ts, padiBinding.ts, converge/policy, examples, tests, README).