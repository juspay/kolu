node_modules/
dist/
result
.direnv/
# APM: generated metadata + per-user state (not vendored)
apm_modules/
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
test-results/
packages/tests/reports/
packages/client/public/fonts
.worktrees
.logs/
.ci/
/.do-results.json
.kolu-dev/
.ralph-logs/
# Talk-mode HTML artifacts (single-source-of-truth for in-session
# design discussions — see `.claude/skills/talk`). Live alongside
# the repo so the runner can render them, but never committed.
talk-*.html
