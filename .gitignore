node_modules/
dist/
# …except the self-contained Atlas output, committed for in-app Code-tab preview.
!/docs/atlas/dist/
!/docs/atlas/dist/**
result
.direnv/
# APM: generated metadata + per-user state (not vendored)
apm_modules/
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
test-results/
packages/tests/reports/
packages/client/public/fonts
.worktrees
.logs/
.ci/
/.do-results.json
.kolu-dev/
.ralph-logs/
# /agent-debate skill: per-worktree scratch (verdicts, rebuttals, transcript)
.agent-debate/
# /lens-debate skill: per-worktree scratch (commit-message files)
.lens-debate/
# /dev-server skill: per-worktree scratch (remembered dev-server ports)
.dev-server/
# /be review gauntlet: per-worktree scratch (reviewer comment artifacts)
.be-review-comments/
.gauntlet/
# /self-improve skill: per-worktree scratch (mined transcript signals)
.self-improve/
.be-review-state/
bridge-data.js

# Rust build output for the port-scan helper (its own crate, own flake).
packages/port-scan/target/
