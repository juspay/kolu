**claude** — Conceded all three: gated the resumable display/count on the real wake test, moved the MRU side-effects ahead of the fold no-op early-return, and refreshed three stale comments; full unit suite + `just check` green.

- `F1` **fixed** — Real regression, conceded. `resumableCommand` (`packages/integrations/anyagent/src/schemas.ts:86`) tested `kind` independently of the wake path, so a `legacyMostRecent` record for a detection-only agent (`aider`/`goose`/…, in `STABLE_FLAGS` but not `AGENT_RESUME`) or an `exact` target whose id fails its shell-safe shape gate got counted/shown as resumable while `resumeFormFor` (`agent-cli.ts:338`) returns `null` and wake lands on a bare shell. Fixed by GATING on the single authority: `if (target === undefined || resumeFormFor(target) === null) return null;` then the existing exhaustive switch returns the raw display command. The two projections can no longer drift (one now delegates to the other). schemas.ts imports `resumeFormFor` from the sibling `./agent-cli.ts` — both browser-safe (agent-cli pulls only the pure `@kolu/shell-quote` + `string-argv`; the client already bundles it via `anyagent/cli`), and the `agent-cli → schemas` edge is `import type` only, so no runtime cycle and no node-only deps dragged into the "browser-safe schemas" layer. Added a `resumableCommand` describe block (6 cases — happy `exact`/`legacy`, plus the three null cases that now match `resumeFormFor`) in `agent-cli.test.ts`, and a broken-id exclusion case in `restoreModel.test.ts`. Also fixed an unrealistic fixture there: `exactTarget` used `sessionId: "ses-A"` (not a claude UUID, so it correctly failed the new gate) → now a valid UUID, since claude session ids ARE UUIDs and the test is about the sleeping/sub/quit filtering, not the id gate.

- `F2` **fixed** — Real regression, conceded. Verified against the base: the OLD command sensor (`sensors.ts:419` at d60c7643d) fired `sink.trackRecentAgent?.(normalized)` for every `!replayed` mark, OUTSIDE the `record.meta.lastAgentCommand !== normalized` gate — i.e. a re-launch of the same agent command still refreshed the recent-agents MRU `lastSeen`. The new emit path (`packages/server/src/terminalEndpoint/local.ts`) returned early on `current === before` (the fold dedups an unchanged `lastAgentCommand` to `cur`, `fold.ts:138`) BEFORE reaching `trackRecentAgent`, dropping that bump. Fixed by moving both cross-terminal MRU side-effects (`trackRecentRepo`/`trackRecentAgent`) ABOVE the early return — they key off the OBSERVATION, not the fold delta. Behavior-neutral for git (a git observation always allocates a new `observed`, so it never hit the early return) and for new/non-replayed commands; only restores the same-command refresh. No unit seam exists for the private `emit` closure (it's wired to pty-host taps — `grep` found zero tests touching it), so this rests on the fold-dedup contract test (`fold.test.ts`) plus the base-vs-branch behavior diff above rather than a new emit-level test.

- `F3` **fixed** — Stale comments, conceded; all three sites describe the OLD frozen-`pr`/awareness-recency model that `surface.ts` itself says is GONE (`surface.ts:221,291,350`: "the old frozen-`pr`-on-the-sleeping-arm special case is gone … `pr` is restore-relevant now"). (1) `terminals.ts:82` — dropped "a sleeping record's frozen `pr` wins"; the spread order is justified now by "authored names no observed field, so it never clobbers the observation" + the sleeping arm keeping the restore-relevant projection (`pr` included) and stripping the live half, matching `composeTerminalMetadata`. (2) `DormantTileBody.tsx:44` — `cwd`, `git.branch`, AND `pr` ALL ride the persisted `PersistedObservation` base; removed "the snapshot the sleeping arm froze off the live overlay". (3) `HostGroup.tsx:5` — the `awareness` collection is the memoryless `Observation` (agent state + git, NO recency — `lastActivityAt` is kolu's remembered memory, confirmed absent from `ObservationSchema`); and the row-order comment (`:279`) said "needs-you first, then most-recent, then id" but `compareFleetEntries` (`fleet.ts:147-157`) sorts "needs-you first, then id" (urgency alone — pulam has no recency), so corrected to match.

commit: `8f704b1b082a43805c2fbe3ad173ac569e04a938`
