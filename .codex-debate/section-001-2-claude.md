**claude** — Fixed all five findings: two real lifetime bugs (F1 in-flight-query abort, F2 daemon anchor now retained per host), one ergonomics tightening (F3), and doc/comment accuracy (F4, F5).

- `F1` **fixed** — Confirmed and load-bearing. `createPolledQuery`'s inactive branch (`packages/client/src/right-panel/createPolledQuery.ts:229`) returned before aborting `controller`, so a `runQuery` already dispatched when a host is backgrounded kept running; on resolve it wrote the now-background host's retained store. Concretely cross-host in `BrowseFileDispatcher`'s binary branch, which reads `activeHost()` AFTER its `filePreviewTag` await — a late resolve stamps host-B's URL with host-A's tag into A's store. Fix: abort + null `controller` on pause (held value/`pending`/`shownKey` stay frozen; the resolve early-returns on `ctl.signal.aborted`). Added a deferred-resolve test (`createPolledQuery.test.ts`) that pins it — verified it FAILS without the fix ("A#LATE") and passes with it.
- `F2` **fixed** — Confirmed. The reanchor premise in `useDaemonStatus.ts` (old `daemonStatusPendingWindow`) rested on "daemonStatus rides `useEntry(activeHost)`, which RE-KEYS on every switch" — which W9 invalidated by RETAINING daemonStatus per host. So A→B→A gave A a fresh 30s anchor though A's sub never restarted, letting a repeatedly-revisited wedged local host dodge the "kaval didn't start" ceiling forever. Fix: the anchor now lives with the sub's lifetime — a `daemonPendingAnchorMs` stamped ONCE at `createHostWire` (`hostScope/createHostWire.ts`), retained across switch-away, discarded on membership exit, fresh only on re-add. `daemonStatusPendingTimedOut` reads `activeScope()?.wire.daemonPendingAnchorMs` (floors the removal race to `false`). Simplified `pendingWindow.ts` (`isPendingTimedOut(pending, anchorMs, now, timeout)`; removed the now-wrong `reanchorPendingWindow`/`PendingWindow`) and updated its header + tests. Added a `perHostWire.test.ts` case proving A retains its deadline across A→B→A and re-anchors only on a genuine re-add.
- `F3` **fixed** — Agreed. `perHostPolledQuery` (`right-panel/perHostPolledQuery.ts`) already hardwires ownership to `padiMap`/`activeHost`, and both callers passed the derivable `live: () => padiMap.live()` and `pulseHost: activeHost`. Now `active`, `live`, and `pulseHost` are all injected inside `perHostPolledQuery` and Omitted from the accepted config, so the incoherent "ownership/liveness/host from three authorities" config is unrepresentable. Updated both callers (`BrowseFileDispatcher.tsx`, `createRepoPolledQuery.ts`) and the two test call sites; dropped the now-unused `padiMap`/`activeHost` imports.
- `F4` **fixed** — Agreed. `website/src/content/docs/remote-hosts.mdx` claimed inactive hosts keep terminals "live in your browser" and that memory "scales with connected hosts and their terminals" — contradicting this PR's boundary (`createHostWire` header: xterm/WebGL/attach stay active-host-only; only subs/metadata/session/query state are retained). Rewrote the paragraph to distinguish host-side running terminals + retained per-host state from the active-host-only renderer that re-attaches on switch, and reframed memory scaling to the state actually retained. Fixed the identical claim in the linked `troubleshooting.mdx#kolu-is-using-a-lot-of-memory` for a coherent story.
- `F5` **fixed** — Agreed; swept the stale relocation comments: `wire.ts` header (recentRepos/savedSession no longer defined there — moved to `activeWire`), `useTerminalStore.ts` header (`terminalListSub` now in `hostScope/activeWire.ts`), `useDaemonStatus.ts` `activeEntryConnected` doc, `daemonPresentation.ts`, `KavalInfoDialog.tsx`, and the `PadiInfoDialog.tsx` classification-table row — all now point to the retained `activeScope().wire.daemonStatus` path instead of `useEntry(activeHost)`. Left the many OTHER `useEntry(activeHost)` comments untouched — `connection`/`rpc`/`processMemory`/`status`/`hostInventory`/`terminalExit` genuinely still ride `useEntry` (deliberate: connection cell stays active-host-only).

`just fmt` + `just check` (typecheck + biome) green; affected vitest suites 105 passing.
