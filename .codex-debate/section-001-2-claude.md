**claude** — Fixed both findings: collapsed the host-body/count/tally gates onto one `bodyReady` (`hostBodyReady`) memo so they can't disagree on a sub error, and brought the surface-client docs in line with the new whole-handle API.

- `F1` **fixed** — Real inconsistency. The body `<SurfaceGate ready={hostBodyReady}>` hides rows when any sub errors (`h.live && !subs.some(error)`), but `reportCounts` (`HostGroup.tsx:257`) and the header terminal count (`HostGroup.tsx:296`) gated only on `connected()` = `app.health().live`. So on a live-link sub error the body showed the error card while the fleet strip kept tallying stale `entries()` and the header still read "N terminals" — exactly the stale-beside-an-error lie. The code comments even claimed these rode "the SAME effectiveHealth gate as the body," which was false. Fix: replaced the `connected` memo with `const bodyReady = createMemo(() => hostBodyReady(app.health()))` (`HostGroup.tsx:220`) and pointed both the `reportCounts` effect and the terminal-count `<Show>` at it, so the three now share one predicate by construction. Updated the surrounding comments (incl. the gate block at ~311 and a stale test comment at `HostGroup.test.tsx:255`). Added a regression test — "a live SUB ERROR drops the terminal count AND the reported tally" — which holds the keys non-empty and a live link, blips a sub error, and asserts the "N terminals" count disappears (red on the pre-fix code) and returns on heal. All 8 HostGroup tests green; `just check` (typecheck + biome) green.
- `F2` **fixed** — Docs taught the retired `surfaceClient(surface, transport.link, { live: transport.live })` / `surfaceClients(link, …, { live })` shape; the branch's new signatures are `surfaceClient(surface, transport)` and `surfaceClients(transport, entries)` — the whole branded `LiveSignalHandle` is passed, there is no `{ live }` seam (`surfaceClient.ts:351-371`, `:798-814`). Updated every stale site: the `.apm/instructions/solidjs.instructions.md` source (regenerated `.claude/rules/solidjs.md`, `packages/client/src/AGENTS.md`, `apm.lock.yaml` via `just ai::apm`, not hand-edited — `.claude/` is generated), the `websocket.ts` JSDoc example (`:59`) plus its three `{ live }` prose references (`:28`, `:38`, `:78`), `packages/surface/README.md` (`:636`, `:705`, the `:999`/`:1005` API-reference block), and `packages/surface-app/README.md:282-285`. Docs/JSDoc only — no API or runtime delta — so no drishti PR is owed for this round's edits.

commit: `6cb12ba184c744dd8e075b01bb94b5a1f5f13061`
