---
title: R-4 — Local PTY daemon (@kolu/pty-host)
description: 'The redo plan for R-4 — local PTY daemonization. A1/A2 landed; the first Phase B (#1275) was built whole, verified live, and discarded for architecture. The redo: one rule (package boundary = process boundary = staleKey hash), a host-count-agnostic endpoint core, and three PRs — the daemon binary inert, the door with survival off, then survival — each complete w.r.t. the hazards it opens. Carries the #1034 postmortem and its hard constraints.'
kind: feature
status: accepted
maturity: budding
parents: [remote-terminals]
updated: 2026-06-11
---

import D2 from "../../components/D2.astro";
import Callout from "../../components/Callout.astro";
import Pill from "../../components/Pill.astro";
import PrLink from "../../components/PrLink.astro";
import Issue from "../../components/Issue.astro";
import Commit from "../../components/Commit.astro";

**The R-4 plan of record** — local PTY daemonization + `@kolu/pty-host`. Split out of the parent [remote-terminals](remote-terminals.html) plan on 2026-05-30; the parent keeps R-1/R-1.5/R-1.6 (shipped) and R-2/R-3 (post-R-4), and now also owns the **multi-host direction** (1 local + N ssh-remote pty-hosts, host switching in the ChromeBar) that this plan must not foreclose. **Current state: A1 (<PrLink pr={1055} />) and A2 (<PrLink pr={1063} />) are on master. The first Phase B build (<PrLink pr={1275} />) shipped the full feature as one 40-commit PR, was verified live in production, and was then deliberately discarded: functional, but the architecture was discovered in review rather than designed.** This note is the redo plan — same user-facing functionality, a designed architecture, and a three-PR split sized so each PR can be executed end-to-end by a single agent session.

Companions: the [`srv · pty` ChromeBar rail](pty-daemon-chrome-bar.html) (A2's deliverable, shipped) and [kolu-tui](pty-daemon-tui.html) (Phases 0–2 shipped — the CLI that dials the pty-host unix socket, today served by the in-process server, after B2 by the daemon).

## The architecture — one rule, then a module map

<D2
  caption="The redo's module map. @kolu/pty-host is exactly the code that runs in the daemon process — the package boundary is the process boundary is the staleKey hash set, zero file-level exceptions. The server-side supervisor is a directory seam (endpoint · driver · restart · reconcile), keyed by hostId from day one so R-2's ssh driver is an additive sibling."
  code={`direction: down
daemon: "@kolu/pty-host — THE DAEMON PROCESS (hashed: staleKey covers every file)" {
  entry: "daemonMain — pid-gate acquire · own root · env · serve loop"
  core: "createPtyHost + taps + ptyHostSurface + serveOverUnixSocket"
}
server: "kolu-server — restarts every deploy" {
  sup: "src/ptyHost/ — the supervisor (a directory, promotable to a package at R-2)" {
    endpoint: "endpoint.ts — status owner, map keyed by hostId"
    driver: "localDriver.ts — spawn · waitForPidGone · pid-gate read"
    restart: "restart.ts — composed sequence, steps non-optional in the type"
    reconcile: "reconcile.ts — pure: list x saved -> adopt | restore | orphans"
  }
  backend: "terminalBackend/local.ts — wireTerminal: spawn | adopt as siblings"
  providers: "providers.ts — provider DAG, FRESH each deploy"
}
client: "client" {
  rail: "IdentityRail — SRV / DAEMON columns + per-host status"
}
tui: "kolu-tui"
server.sup.driver -> daemon: "spawns + supervises (systemd-run / detached)"
server.sup.endpoint -> daemon: "unixSocketLink + contract handshake"
server.backend -> daemon: "PTY RPCs + raw taps"
client.rail -> server.sup.endpoint: "derives from the status collection"
tui -> daemon: "dials the same socket"
`}
/>

> **The one rule everything follows from: the package boundary IS the process boundary IS the staleKey hash set.** `@kolu/pty-host` contains exactly the code that executes inside the daemon — the PTY primitive, the wire contract, the taps, the socket serving, *and the process entry* (pid-gate acquisition, the daemon's own root/shellDir, env application, exit handling). The staleKey is "the nix hash of this package dir" with **zero file-level exceptions**, and `buildId.closure.test.ts` walks the import graph **from the daemon entry** (not `index.ts`) — the closure test now answers the exact question the staleKey asks: *what would a restart gain?* Everything that supervises from outside (spawn, wait, handle, restart, reconcile) lives in `packages/server` — un-hashed, correctly, because changing the supervisor never changes what a restart would gain.

The remaining axes, most stable → most volatile (the boundaries that own them):

| Axis | Rate | Boundary that owns it |
| --- | --- | --- |
| Everything in the daemon process | rarely | `@kolu/pty-host` — hashed whole; the entry travels with the package, so R-2's remote daemon gets single-instance arbitration for free. |
| Wire contract + compat + identity | per wire change | `ptyHostSurface` + `PTY_HOST_CONTRACT_VERSION` + `PtyHostIdentity`, in-package (unchanged from A1/A2). |
| Identity values: staleKey vs navigableCommit | key: per wire change · commit: per deploy | `buildId.ts` (unchanged). Currency is **derived at the read site** (`staleKey !== currentBuildId()`), never stored, never frozen onto a connection. |
| Endpoint status (the one health owner) | per endpoint-model change | `server/src/ptyHost/endpoint.ts` — emits `{state, identity, startedAt}` on **every** transition; a **map keyed by hostId** (a map of one, today: `local`). Everything else — buildInfo, rail, degraded canvas — derives by subscription. |
| Local reach mechanics | per platform quirk | `localDriver.ts` — `systemd-run --user` with per-spawn unique unit names + explicit `--setenv` env forwarding (Linux) / detached+unref (macOS); `waitForPidGone` (ESRCH poll, load-aware ceiling); pid-gate *read* side. R-2 adds `sshDriver` as a sibling (HostSession reach + `provisionAgent`). |
| Recovery sequence | per recovery policy | `restart.ts` — one composed `capture → save → drain → waitForGone → respawn → reattach`, all steps **non-optional in the type**, serialized (concurrent callers observe `restarting`), parameterized over driver ops. The forced and user paths are the same function. |
| Reconciliation + adoption | per persisted field | `reconcile.ts` (a pure function: `daemon.list() × savedSession → {adopt, restoreCard, orphanExtras}`) + `wireTerminal` in `local.ts` with **adopt and spawn as siblings**. Adoption consumes the **whole `SavedTerminal` record as a unit** — never field-by-field reconstruction — and a schema-level round-trip test iterates the schema's keys. |
| Health → UI projection | per UX tweak | A per-host status collection on the surface; rail + DegradedCanvas + restart dialog subscribe. User-facing name: **“kolu daemon”**. |

There is deliberately **no `@kolu/pty-host-daemon` package**. The supervisor has no second consumer — R-2's ssh driver is a *sibling behind the endpoint concept*, not a consumer of the local driver's code. A directory seam gives the same review isolation; promote it to a package only when R-2 produces a real second tenant.

### Multi-host readiness (the shapes, not the feature)

The [parent note](remote-terminals.html) owns the direction: 1 local + N ssh-remote pty-hosts, host switching in the ChromeBar, reattach to all of them. Phase B ships exactly one endpoint but must be host-count-agnostic in its **shapes**: the endpoint map is keyed by hostId; daemon status is a per-host collection (not a singleton cell); adoption and the saved session join on `(host, id)` — terminal records already carry `location` from R-1; and the daemon-side code is one hashed package that runs identically on a remote host. None of this costs more than the singleton version; retrofitting any of it later costs another #1275.

## What this makes impossible by construction

The production failures — four from #1034, four more paid for during #1275 — each become a failing test at the phase that owns the concept:

| Production failure | Killed at | How |
| --- | --- | --- |
| Mis-scoped staleness key (wire change didn't flip it) | A2 ✅ | Closure-scoped key + import-walk guard (now re-rooted at the daemon entry in B1). |
| Over-prompting (key nudged on every deploy) | A2 ✅ | Server-only change leaves the key bit-identical — a falsifiable test. |
| Data-loss restart (kill-then-pray, #1034) | B3 | One composed restart, snapshot-before-kill a **type-level fact**; forced + user paths are one function. |
| Empty-canvas lie (dead daemon → “no terminals”) | B2 | Honest dead/degraded state ships **with the door**, before any survival promise exists. |
| Lossy adoption (#1275: splits un-nested, then agent-resume lost *and autosaved*, poisoning cold restores) | B3 | Adoption consumes the whole persisted record; a schema-key-iterating round-trip test closes the *class*, not the instance. |
| Lazy adopt-on-spawn (#1275: orphan shells respawned into the daemon — duplicated terminals) | B3 | Eager boot-time reconciliation is the only adopt path; orphans are reaped and logged, non-survivors go to the restore card — never silently dropped. |
| Identity gone stale after restart (#1275: one-shot read, then manual republish at enumerated call sites) | B2 | One status owner emitting on every transition; everything derives by subscription. |
| Contract-skew crash-loop masked by launchd as an “App updated” loop (the zest field report on <PrLink pr={1275} />) | B2 | Version checked over the socket on every connect, **never** an import-time throw; skew at boot → controlled recycle (B2) / composed restart (B3). |

## The three PRs — each an executable brief

No feature flags — each PR ships complete to master and leaves kolu working. The hazard rule, refined from the first attempt: **each PR must be complete w.r.t. the hazards *it* opens** — and the door can open with the survival promise off, which empties its hazard set by policy. Each brief below is written to be executed end-to-end by a single agent session; the **Traps** lists are load-bearing — they encode the exact failure modes already paid for.

### B1 — the daemon binary, inert <Pill variant="todo">next</Pill>

**Goal:** `@kolu/pty-host` contains everything that runs in the daemon process, runnable by hand. **Zero changes to `packages/server`.**

Deliverables:

- `packages/pty-host/src/daemonMain.ts` — the process entry, a plain sequence: acquire the pid-gate **atomically** (write pid to a temp file, `link(2)` it into place; on `EEXIST` read the gate, liveness-probe `kill(pid,0)`, exit 0 if a live daemon holds it, unlink-and-retry if stale) → compute the daemon's **own** root + shellDir with an in-package helper (do *not* import the server's `koluRoot`) → serve via `servePtyHost` + `servePtyHostOverUnixSocket` at `getPtyHostSocketPath()` → emit one structured boot log line → on SIGTERM: close the socket, release the gate, exit. PTY env derives from the daemon's **own** `process.env` (the `cleanEnv`/`koluIdentityEnv`/`prepareShellInit` calls already live daemon-side in `servePtyHost`'s spawn handler).
- The pid-gate **acquire** side as an in-package module (the read side comes in B2, server-side).
- `buildId.closure.test.ts` re-rooted at the daemon entry; every file in the package hashed and reachable-or-test — zero exceptions.
- A bin entry for the daemon (the `kolu-pty-host` binary; nix wiring per the new-package checklist: `default.nix` fileset, lockfile, vitest).
- Tests: gate-race choreography (A acquires → B exits 0 while A lives → A dies → C acquires → D exits 0); double-bind hits the socket's `already-served` refusal as a second tripwire; `system.version` roundtrip over the socket.

Traps: no import-time throws anywhere; no top-level await; nothing in the package may import from `packages/server`; hand-testing against a running kolu must use a socket-path override (the default path is served by the in-process server until B2 — `serveOverUnixSocket`'s probe will refuse politely, which is itself a test).

Verify: hand-run the binary on an override socket; a server-only change leaves `KOLU_PTY_HOST_BUILD_ID` bit-identical across two nix builds; touching `daemonMain.ts` flips it (it now runs in the daemon — that's correct, not a regression).

### B2 — the door, with survival off <Pill variant="todo">the one-way door</Pill>

**Goal:** flip the topology — the server becomes a client of a daemon it spawns — while keeping user-facing semantics **byte-identical to today**: the boot policy is *always recycle* (connect-if-survivor → kill → `waitForPidGone` → spawn fresh → connect). No survivors exist, so no survival hazard can open: no orphans, no skew older than one boot, nothing for a restart to pray over. Every production deploy now exercises kill → wait-for-real-exit → respawn — **the exact race #1034 lost — with zero sessions at stake.**

Deliverables:

- `packages/server/src/ptyHost/` — `localDriver.ts` (spawn via `systemd-run --user` gated on `INVOCATION_ID`, **per-spawn unique unit name**, absolute daemon path, explicit `--setenv` forwarding of the daemon-env keys, dev-flag exec-arg filter; macOS detached+unref), `waitForPidGone.ts` (poll `kill(pid,0)` → ESRCH; load-aware ceiling, ~120s default), pid-gate read, `endpoint.ts` (state machine `connecting | connected | degraded | dead`; emits `{state, identity, startedAt}` on every transition; **map keyed by hostId**), `restart.ts` (the composed sequence — in this PR invoked only as the boot recycle, with capture/drain/reattach in their degenerate forms; the type already requires all steps).
- **Composition root:** `index.ts` boots explicitly — parse argv → `ensureLocalEndpoint()` → construct `LocalTerminalBackend` with the **injected** client → listen. `server/src/ptyHost.ts`'s import-time construction is deleted; no module-global client, no top-level await.
- **The flip:** `LocalTerminalBackend`'s client is the endpoint's socket-backed client; the server **stops serving** the pty-host socket (today's `index.ts:397-407`) — the daemon serves it; kolu-tui is unchanged.
- Contract handshake on every connect (`isContractVersionCompatible` against `system.version`); skew at boot → recycle (the boot policy anyway). Never an import-time version throw.
- Honest minimal state: a per-host daemon status on the surface; the rail's DAEMON column lights up (uptime from the daemon's `startedAt` beside SRV's, green/red dot); a mid-session daemon death presents as an explicit degraded state — visibly distinct from “you have no terminals”.
- e2e: per-worker `XDG_RUNTIME_DIR` socket isolation; hooks teardown kills spawned daemons.

Traps: **no mode flag** — the socket is *the* path (a `KOLU_DAEMON_MODE` toggle is a feature flag in a trench coat); do not implement adoption (assert `terminal.list()` is empty after the recycle); do not widen any shared schema; restore-card-on-deploy behavior must be exactly today's.

Verify: e2e green on both platforms (the darwin lane's *presence* is itself a check — its absence was once silent); kolu-tui works against the daemon; `pkill`-ing the daemon mid-session shows the honest dead state; a staged deploy shows the recycle in the journal.

### B3 — survival <Pill variant="todo">the promise flips on</Pill>

**Goal:** replace the boot recycle with reconciliation; ship the supervised restart and the currency UX. This is the domain-dense PR — ~the smallest of the three in mechanism, the densest in review — and everything it composes is already merged and prod-soaked.

Deliverables:

- `reconcile.ts` — pure function `(daemonList, savedSession) → {adopt, restoreCard, orphanExtras}`. Boot uses it when the handshake passes; daemon-side PTYs not in the saved session are killed and logged; saved terminals not surviving go to the **restore card** — never silently dropped. One structured diagnostic log from day one: `reattach: {daemonPtys, adopting, fromSaved, orphanedSaved, extras}`.
- Adoption: `wireTerminal` as the shared core, `adopt` and `spawn` as siblings over it. Adopt consumes the **whole `SavedTerminal`** (parentId so splits stay nested, `lastAgentCommand` so a later cold restore still offers `claude -c`, and every other server-persisted field), marks ready from the list entry's pid (**no spawn RPC**), re-runs the provider DAG fresh against the surviving taps. The round-trip test iterates the persisted schema's keys — every field survives adopt→snapshot.
- The restart sequence gains its real parameters: capture (`snapshotSession` → `setSavedSession`, which wins the autosave-cancel race by construction) → drain (`killAndUnwatch` per terminal — abort the exit tap *then* kill, the single kill path) → `waitForPidGone` → respawn → reconnect → reattach. Serialized: concurrent triggers observe `restarting`; a failed respawn leaves `dead` + the preserved session + a retry affordance — never an empty canvas. Exposed as the `daemon.restart` RPC.
- Forced path: contract-skew on adopt → the same composed restart with an empty capture.
- Currency UX: `staleKey !== currentBuildId()` derived at read sites; the rail's `⬆ update pending` (amber) is a **one-click confirmed restart** whose dialog explains *why* (the daemon is a build behind), *what happens* (a brief terminal recycle with the session restored), and that it's **optional**; a palette command **Restart kolu daemon**; full DegradedCanvas treatment; a tip in the tips registry; changelog.
- e2e: `daemon-reattach.feature` + `daemon-update.feature`, written red-first where the harness allows.

Traps: never key currency on the commit hash (the #1034 over-prompting bug, re-introduced once already); never store currency as a boolean; never rebuild adopted records field-by-field through the spawn path's metadata seeding — the record flows whole; never let the nudge fire on a server-only deploy (that's the falsifiable A2 test, now end-to-end).

Verify: the unit ordering invariant (`capture < save < drain < waitForGone < respawn < reattach`, and `restart.ts` does not compile with a step omitted); the schema round-trip test; both e2e features; then the gate below.

### The gate — the second deploy (not a PR)

CI structurally cannot exercise “redeploy over a live daemon”; the acceptance signal is a **staged prod checklist**, a planned step: deploy B3 → open terminals + a running agent + a split → **server-only redeploy** (no nudge; survivors adopted; the `reattach:` counts reconcile; the split still nested; the agent still running) → **pty-host-touching redeploy** (amber nudge → confirmed restart → recovery honest; a deliberately failed respawn leaves the restorable session).

<Callout kind="warning" title="Two honest costs, accepted up front">
<strong>1 — B2's recycle can block boot.</strong> On a loaded box, the old daemon's exit can take minutes (#1034's 25G/2min case); B2's boot waits on <code>waitForPidGone</code> up to the load-aware ceiling before spawning. That is the same blast radius as today's in-process teardown — and it is exactly the soak we want on the race before B3 promises anything. <strong>2 — rollback orphans a daemon.</strong> Rolling back B2 or B3 leaves a surviving daemon holding the socket; the in-process server's own socket serve will refuse politely (<code>already-served</code>) and kolu-tui will keep talking to the orphan. The reap is manual and documented: <code>systemctl --user stop</code> the unit (Linux) / kill the pid in the gate (macOS) — the mirror of the A1 cutover reap.
</Callout>

<Callout kind="note" title="Open follow-up — surface ↔ pty-host upstreaming">
The first instalment shipped with kolu-tui Phase 1 (<PrLink pr={1084} />): the unix-socket transport pair, `getRuntimeSocketPath`, `serveOverStdio`'s settled end result, and the generic `isContractVersionCompatible` all moved into <code>@kolu/surface</code>. What remains for a post-B pass: a standard <code>system.version</code>/build-id handshake fragment, plus whatever B2's endpoint glue turns out to want.
</Callout>

## The #1034 postmortem — production failure and hard constraints

The first R4c-UI build (<PrLink pr={1034} />) shipped the build-mismatch "update pending" nudge + a *Restart local PTY daemon* command. On production (Linux/systemd, 2026-05-30) the nudge fired correctly on a deploy; clicking restart then **destroyed a live 20-terminal session and could not bring the daemon back**. From the journal: `killAll` drained all 20 terminals first; the old daemon (13.5h old, 25G RAM on a thrashing box) took ~2min to exit; the respawn timed out at 30s; the user was left with zero terminals, a dead daemon handle, and an **empty canvas indistinguishable from "you have no terminals."** No second daemon ever ran — the failure was the respawn losing the race against the slow old-daemon exit.

### Hard constraints (binding on the redo)

1. **Restart must be recoverable, never kill-then-pray.** Snapshot the session first; after respawn, auto-reattach/offer restore; if respawn fails, a loud recoverable degraded state with the session preserved — never an empty canvas.
2. **Wait for the old daemon to fully exit** — the single-instance lock fights the restart. Wait on actual process exit (`kill(pid,0)` → ESRCH) before spawning, with generous load-aware ceilings.
3. **Timeouts must fit a loaded production box**, not an idle dev one (20 heavy PTYs + a tsx cold-start under swap ≫ 30s).
4. **Never lie about state** — an explicit connecting/degraded UI, never silent emptiness.
5. **A persistent PTY status indicator, always visible** — connected · connecting · degraded/dead · update-pending (became A2's rail).
6. **Key staleness on pty-host, not the whole binary** — hash the `@kolu/pty-host` source closure so the nudge fires only when a restart actually gains something (became A2's staleKey; B1 re-roots the closure at the daemon entry).
7. **Back up the session before a destructive restart** (Export/Import session shipped via <PrLink pr={1046} />).
8. **Fix the preferences storm** (<Issue n={1041} />, fixed in <PrLink pr={1050} /> — coalesced writes).

### Execution lessons (process, not product)

From building + salvaging #1034, re-confirmed during #1275: typecheck every package before commit (a clean `nix build` is **not** proof CI passes, <Issue n={1049} />); a new workspace package has a checklist (`default.nix` explicit fileset, staleKey-closure decision, lockfile, vitest wiring) — the fileset miss broke CI once already; never `git stash` in the worktree; grep for unresolved conflict markers before committing a merge; never defer with a someday-issue — gaps go into the plan and get done in the PR; when the terminal contradicts itself, confirm via an independent path before "fixing" a phantom. Wire-shape rules: additive wire fields are **optional + no contract bump** (a required field force-restarts a surviving older daemon just to add a diagnostic); the identity surfaced to users is the *navigable* one (commit SHA), with the source hash kept as the staleness key only. And the #1275-specific one: **CI-green is necessary, not sufficient — the second deploy is the acceptance signal**; budget the staged-prod loop as a planned step with the diagnostic logging built in advance.

## Design notes that carry forward

**The survivor is `@kolu/pty-host` only** — node-pty fds + the `@xterm/headless` mirror + the raw VT taps + a unix socket + its own process entry. A kolu-server restart re-runs the providers against the surviving PTYs, so detection is never stale while the PTYs persist. Honest cost (accepted): metadata is no longer "warm" across a restart — a brief re-detection pass, trading warm-on-reconnect for freshness-on-deploy. Only a pty-host contract change (rare) forces terminal loss; a provider change (frequent) restarts the cheap layer with PTYs untouched. This corrects #1031, which daemonized a survivor that *held* the providers.

**The cgroup mechanism (spike-verified).** On Linux/systemd the daemon spawns via `systemd-run --user` (gated on `INVOCATION_ID`), landing in its own transient cgroup — a plain detached/`setsid` child does **not** survive on cgroup-v2 (`KillMode=control-group` walks cgroup membership, not the session — the #1031 Linux failure). macOS's detached spawn already survives launchd. Caveats folded into B2: linger must be on; absolute daemon path (minimal unit PATH); **per-spawn unique unit names** (a dead unit can linger loaded). Single-instance two ways: the unit name plus the atomic pid-gate.

**tmux/dtach considered and rejected:** they only keep a PTY alive — no OSC-parsed taps, no headless snapshot for lazy-attach, no home for the provider DAG. You'd still build pty-host's streaming layer next to tmux and inherit its session model on top.

## History

- **R4a** <PrLink pr={1023} /> + **R4b** <PrLink pr={1028} /> shipped clean as pure in-process refactors.
- Two prototypes seeded the design: <PrLink pr={994} /> (remote) proved "providers in the agent", re-provisioned fresh per version; <PrLink pr={1010} /> was a PTY-only local daemon — the thin-survivor model. The redo is their correct combination.
- **R4c first attempt** <PrLink pr={1031} /> dropped after a macOS prod failure: it daemonized pty-host *plus* the provider DAG, so the survivor served stale detection on every deploy — and staleness keyed on the inert `pkgVersion` constant, so `outdated` was always false and a 20-hour-old daemon was reused forever.
- **R4c redo** <PrLink pr={1034} /> — the postmortem above; shipped + salvaged, then discarded by the fresh approach.
- **The fresh-approach redo** (2026-05-30/31): <PrLink pr={1055} /> (A1) landed the in-process foundation and removed every #1034 daemon artifact; <PrLink pr={1059} /> unified the link family; <PrLink pr={1063} /> (A2) landed identity + the closure-hash guard + the rail. kolu-tui Phases 0–2 (<PrLink pr={1073} /> / <PrLink pr={1084} /> / <PrLink pr={1255} />) then battle-tested the socket transport in production.
- **Phase B first build** <PrLink pr={1275} /> (2026-06-11) — the whole feature as one 40-commit PR: functional, verified live across staged prod deploys, and discarded. What it proved: the user-facing spec works end-to-end. What it taught (each now a row in the impossible-by-construction table): the `@kolu/pty-host-daemon` package was extracted mid-PR after the review gauntlet; reattach correctness arrived as three sequential prod fixes (parentId, contract-on-adopt, lastAgentCommand); identity freshness was patched three times; the staleKey hash needed three lockstep file-level exception sites. The redo above designs each of those in.
- **This plan** (2026-06-11): the three-PR redo — B1 (daemon binary, inert) → B2 (the door, survival off) → B3 (survival) → the staged-prod gate.
