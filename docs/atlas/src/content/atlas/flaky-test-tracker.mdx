---
title: Flaky Test Tracker
description: A backlog of flaky tests — e2e (Cucumber + Playwright) and unit (vitest). Drop a row when you hit one; an agent clears the queue from time to time.
parents: [reference]
maturity: seedling
updated: 2026-07-09
---

import Pill from "../../components/Pill.astro";
import PrLink from "../../components/PrLink.astro";

A fix-queue for tests that go red on one CI run and green on the next with no
code change. See a flake → **add a row**. An agent works the backlog over time.

## Flake vs. break

A **flake** fails nondeterministically — timing, ordering, or environment — and
would pass on a same-SHA rerun; a **break** fails the same way every run and is a
real defect, so file a [bug](./bug.html) instead. A one-off rerun via the **odu
MCP** is fine to *triage* which it is — but a rerun is never how a flake gets
*fixed* (see the routine below).

## Backlog

Status: <Pill variant="new">open</Pill> → <Pill variant="run">fixing</Pill> → <Pill variant="ok">fixed</Pill> (then strike the row).

_Queue clear — the eight below were fixed in <PrLink pr={1440} /> (struck = done; the reusable patterns live in [Common flake classes](#common-flake-classes))._

| Test | Lane | Symptom | Repro'd in | Status | Fix |
| --- | --- | --- | --- | --- | --- |
| ~~Code tab history survives switching between terminals in different repos (`code-tab.feature:714`)~~ | `e2e@aarch64-darwin` | `back` button never enabled — `waitFor` 20s timeout | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| ~~Clicking a folder ref reveals and expands the directory (`file-ref-link.feature:69`)~~ | `e2e@aarch64-darwin` | `app/core` never expands — a fresh-open (cold panel) reveal races `fsListAll`'s first snapshot via a one-shot resolve with no re-yield, so a commit-marker barrier can't fix it. Mount the tree first; the fresh-open resolve is covered by `lineRef.test.ts` | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| ~~Selected file survives switching to another terminal and back [branch] (`code-tab.feature:148`)~~ | `e2e@aarch64-darwin` | tree never hydrated — branch-mode gitStatus stuck on `BASE_BRANCH_NOT_FOUND` | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| ~~Regaining window focus repaints a render-stalled terminal (`render_recovery.feature:16`)~~ | `e2e@aarch64-darwin` | `AssertionError` — screen not repainted on focus regain | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| ~~Close sub-terminal via tab close button (`sub-terminal.feature:107`)~~ | `e2e@x86_64-linux` | `sub-terminal should have keyboard focus` — `waitForFunction` timeout (focus not restored after close) | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| ~~Scroll on terminal does not pan the canvas (`canvas.feature:161`)~~ | `e2e@aarch64-darwin` | tile-centering pan raced the recorded baseline → `canvas transform changed unexpectedly` | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| ~~Tile chrome shows task progress when Claude has tasks (`claude-code.feature:77`)~~ | `e2e@aarch64-darwin` | appended-transcript fs event dropped → task progress `3/5` never showed | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| ~~pulam daemon — dials a kaval, serves awareness (`daemon.test.ts`)~~ | `unit@x86_64-linux` | `waitFor` didn't catch a transient oRPC stream error from the live awareness collection → test threw (vitest, no retry) | <PrLink pr={1440} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1440} /> |
| createPtyHost — routes write() to the child and lists live PTYs (`kaval/src/ptyHost.test.ts:373`) | `unit@aarch64-darwin` | `Test timed out in 5000ms` — a real-PTY spawn-then-write test stalled past the 5s default on the darwin box; the linux lane passed the same SHA, and the PR touches no `packages/kaval` file (single-node rerun green) | <PrLink pr={1497} /> | <Pill variant="new">open</Pill> | — |
| Sub-terminal keeps keyboard focus after close (`sub-terminal.feature`, `sub_terminal_steps.ts:132`) | `e2e@aarch64-darwin` | `the sub-terminal should have keyboard focus` — `waitForFunction` timeout (focus not restored after close); the same flake fixed for the linux lane in <PrLink pr={1440} /> recurring on the loaded darwin box (472/473 scenarios passed), unrelated to the PR's surface/pulam changes | <PrLink pr={1497} /> | <Pill variant="new">open</Pill> | — |
| Clicking a folder ref while already browsing expands it in the live tree (`file-ref-link.feature:112`) | `e2e@aarch64-darwin` | `lib/ui` never reaches `aria-expanded=true` — `locator.waitFor` 60s timeout across all 3 retries on both runs. A live change into an **already-mounted** Pierre tree updates the model but never repaints — the unfixed [#1534](https://github.com/juspay/kolu/issues/1534) swallow-emit class (sibling of the fresh-open `:69` case fixed in <PrLink pr={1440} />, whose "mount first" fix doesn't cover the mounted-tree live update). The linux lane passed the same SHA (482/483), and the PR touches **no** Code-tab/Pierre/file-ref code (all changes are pulam-web + surface), so unrelated to it. Carried by R-pulamweb-4's vendored `@pierre/trees` patch. | <PrLink pr={1568} /> | <Pill variant="new">open</Pill> | — |
| worktreeCreate — `WORKTREE_NAME_COLLISION` (`packages/integrations/git`, varying subtest) | `unit@aarch64-darwin` | `Test timed out in 15000ms` — flaked TWICE in one night (different subtest each time) on PR #1714's CI, whose diff touches no `packages/integrations/git` file; the file takes ~50s wall on a loaded darwin box, so the 15s per-test budget is too tight under load. Green on both same-SHA reruns; linux lane green throughout. | <PrLink pr={1714} /> | <Pill variant="new">open</Pill> | — |
| Touch-swiping down inside the terminal scrolls the scrollback up (`mobile-terminal-scroll.feature:14`) | `e2e@aarch64-darwin` | `viewportY` never decreased — stayed at 122 after a 20s poll. Flipped **green** on a same-SHA rerun (a different scenario flaked instead), so a mobile touch-scroll timing race, unrelated to the PR's padi-process changes. | <PrLink pr={1664} /> | <Pill variant="new">open</Pill> | — |
| ~~File-drop / clipboard path reaches the PTY (`file-drop.feature:10`+`:14`, `clipboard.feature:9`)~~ | `e2e` (both) | `the screen state should contain "<file>"` → 20s `waitForFunction` timeout. Root cause is the **screen-state reader**, not delivery: the DIAG dump shows both `scratch/write` + `sendInput` sent, no toast, and the filename fully on screen — but the long W2.2 scratch path (`$XDG_RUNTIME_DIR/kolu-<pid>/scratch/<uuid>/<name>`, ~81 chars) hard-wraps at the 80-col grid before xterm's `fit()` widens it under load, so the filename straddles the wrap (`…no`+`tes.md`). `__readXtermBuffer` joined rows with `\n`, splitting the match. The W2.2 padi-state-root cutover lengthened the path past the old short-`runtimeDir` mitigation; the bash "garbles the cells" note in `hooks.ts` was a mis-diagnosis (cells are clean). Fix: `__readXtermBuffer` rejoins `isWrapped` continuation rows into one logical line, so a straddling filename matches regardless of width/path-length. Verified 10/10 under CPU oversubscription that gave ~100% failure pre-fix (leased `kolu-ci-1`). | <PrLink pr={1664} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1664} /> |
| ~~Shell hook tests depend on host shell/hostname state (`packages/integrations/pty/src/shell.test.ts`)~~ | `unit@aarch64-darwin` | Same-SHA reruns timed out in different OSC hook tests (`OSC7_FN`, then `OSC2_PREEXEC_BASH_GUARD`) and finally the zsh wrapper regression. The helpers said "clean" but inherited the CI process env, spawned the host `hostname` command, and paid one bash/zsh startup per assertion under the parallel unit lane; tests now run with a tiny explicit `HOME` / `PATH` / `TERM` / `TMPDIR` env, stub `hostname` in the OSC7 scripts, and execute shell snippets through long-lived clean shells with per-snippet subshell isolation. | <PrLink pr={1736} /> | <Pill variant="ok">fixed</Pill> | <PrLink pr={1736} /> |
| omits PADI_SOCKET when padi's serving socket is unknown (autodiscovery covers it) (`padi/src/ptyHost/spawnInput.test.ts`) | `unit@x86_64-linux` | 1/9 tests failed on a shared warm pool box; a single-node rerun came back green same-SHA. The PR is client-only (the canvas restore-card gate) and touches no `packages/padi` code, so unrelated to it. | <PrLink pr={1712} /> | <Pill variant="new">open</Pill> | — |
| discoverKavalCandidates — finds a /tmp daemon even with `$XDG_RUNTIME_DIR` set (`kaval/src/socketPath.test.ts:287`) | `unit@aarch64-darwin` | `Test timed out in 5000ms` — the socket-discovery test (unions the XDG root + the /tmp fallback) stalled past the 5s default on the loaded darwin box; the linux lane passed the same SHA and the PR touches no `packages/kaval` file. Green on rerun (a *different* darwin test flaked next — the surface one below — then both cleared). | <PrLink pr={1726} /> | <Pill variant="new">open</Pill> | — |
| mirrorRemoteSurface — fires onRemove when a key leaves the collection's keys snapshot (`surface/src/mirrorRemoteSurface.test.ts`) | `unit@aarch64-darwin` | `AssertionError: expected [] to deeply equal [ 'b' ]` — a reactive collection-key-removal assertion raced on the loaded darwin box; the linux lane passed the same SHA and the PR touches no `packages/surface` *code* (only its README). Green on rerun. | <PrLink pr={1726} /> | <Pill variant="new">open</Pill> | — |
| reconnects when padi dies, and the re-served surface round-trips again (`server/src/padiBinding.test.ts`) | `unit@x86_64-linux` | `AbortError: [AsyncIdQueue] Queue was closed or aborted while waiting for pulling` — the stdio link's `handleTransportClosed` → `peer.close()` raced the async-iterator queue teardown during the reconnect-when-padi-dies test. Single-node rerun green same-SHA; the PR is client-only and touches no `packages/server` code, so unrelated to it. | <PrLink pr={1712} /> | <Pill variant="new">open</Pill> | — |
| discoverPtyHostSockets — finds per-port server namespaces and a bare standalone one / ignores a namespace dir with no socket yet (`kaval/src/socketPath.test.ts:158,177`) | `unit@aarch64-darwin` | `Test timed out in 5000ms` (two sibling tests) on the loaded darwin box during a big two-platform run; a sibling in the same file passed at 3144ms, right at the 5s threshold. Linux lane green same-SHA; the PR touches no `packages/kaval` file. Green on a same-SHA node rerun (same class as the `socketPath.test.ts:287` row above). | <PrLink pr={1730} /> | <Pill variant="new">open</Pill> | — |
| smoke — the `.#default` KOLU_STATE_DIR sub-test's teardown (`ci/smoke.sh`) | `smoke@x86_64-linux` | `rm: cannot remove '…/.local/state/padi': Directory not empty` — the local padi (a grandchild of the sub-test's kolu) briefly outlived its parent after SIGTERM and re-wrote state as `rm -rf` walked the temp `$HOME`, so the recursive delete hit ENOTEMPTY. A teardown race in `ci/smoke.sh`, unrelated to the PR's surface-remote/client diff. Green on a same-SHA node rerun. | <PrLink pr={1730} /> | <Pill variant="new">open</Pill> | — |
| padi dialed over a stdio byte relay — the subprocess-spawning variants (`padi/src/dial.test.ts`, varying subtest) | `unit@aarch64-darwin` | `padi socket never came up` / `Test timed out in 60000ms` — the byte-relay tests spawn a real padi child and wait for its unix socket; on a contended darwin box (import alone 35–51s) the spawn+bind overran the wait window. Flaked TWICE across consecutive runs with a **different** subtest each time (`round-trips … over the byte relay`, then `the durable daemon SURVIVES the front dropping`), while the socket-based (non-subprocess) variants of the same tests passed at ~3.5s. Linux lane green same-SHA; the PR touches **no** `packages/padi` file (all changes are surface-remote/server/client). Green on a same-SHA node rerun. | <PrLink pr={1730} /> | <Pill variant="new">open</Pill> | — |
| kaval socket-path discovery (`kaval/src/socketPath.test.ts`, rotating subtest) | `unit@aarch64-darwin` | `Test timed out in 5000ms` — the socket-discovery tests stalled past the 5s default on a heavily-loaded rasam (this PR ran ~20 CI passes in one session); a **different** darwin socket/state test flaked each rerun. Linux lane green same-SHA (495/495 e2e, unit green); the PR touches no `packages/kaval` file. Same darwin-under-load class as the `socketPath.test.ts:287` (#1726) and `:158,177` (#1730) rows above. | <PrLink pr={1751} /> | <Pill variant="new">open</Pill> | — |
| padi state-root derivation (`padi/src/stateRoot.test.ts:245`) | `unit@aarch64-darwin` | `Test timed out` on the loaded rasam box during a two-platform run — surfaced on the rerun immediately after a sibling `kaval/socketPath` flake, part of the same darwin socket/state-root-under-load class (rotating failures). Linux lane green same-SHA; the PR touches no `packages/padi` file (all changes are the real-agent e2e lane + agent-provider comments). | <PrLink pr={1751} /> | <Pill variant="new">open</Pill> | — |
| A real grok turn flips the sensor working then done (`grok-real.feature:27`) | `e2e@x86_64-linux` | Got `state="thinking"` after 60s — grok reached `thinking` but the `turn_ended` phase in `~/.grok/sessions/<cwd>/<uuid>/events.jsonl` was missed, so the sensor never advanced to `waiting`. A grok-provider `fs.watch`-append robustness gap — **same class as the claude fast-turn one, [#1754](https://github.com/juspay/kolu/issues/1754)** (different provider, same missed-append race); the provider fix should cover both. Green on a same-SHA rerun; grok-real is otherwise reliably green both platforms. | <PrLink pr={1751} /> | <Pill variant="new">open</Pill> | — |
| e2e `Before`-hook `padi/lifecycle/killAll` → HTTP 500 cascade (`support/hooks.ts:1199`) | `e2e@aarch64-darwin` | A worker's kolu/padi server wedged mid-run and returned 500 on `killAll`, failing every subsequent scenario's `Before` hook (128 then 185 scenarios, mostly `code-tab.feature`). Hit 2 consecutive darwin runs under back-to-back load, then did NOT recur (0 occurrences) once rasam load eased — a server-lifecycle crash under memory pressure, not test logic: the crash landed during `code-tab.feature` long after the real-agent scenarios passed, and the same commit was fully green on a rested run (both platforms). | <PrLink pr={1751} /> | <Pill variant="new">open</Pill> | — |

## Logging a flake

When a lane goes red and a single-node rerun comes back green, **add a row**: test
name, `recipe@platform` lane, the assertion/timeout, the PR it reproduced in
(`<PrLink pr={…} />`), <Pill variant="new">open</Pill>. No investigation needed to log it.

**Keep the tracker in lock-step with your PR.** Log a flake your CI surfaced in the
*same* PR that hit it — don't defer to a later cleanup; and a PR that *fixes* a
flake flips its row to <Pill variant="ok">fixed</Pill> (and strikes it) in that
same PR, regenerating `docs/atlas/dist/`. The queue only stays trustworthy if every
PR that touches a flake updates this note alongside its own diff.

## Fixing routine

An agent clears the backlog by driving CI to **N consecutive green runs** (N = 5
by default, or as given) through the **odu MCP** (`run` the test lanes →
`wait_for_settle`, repeated). The green streak **verifies** that a fix is real —
it is never a way to wash a flake out.

Non-negotiable rules:

- **A single failure is a fix, not a re-run.** One red lane → stop and fix the
  root cause. Re-running to hope for green is forbidden, and any failure resets
  the streak to 0.
- **Fix the test, not the app.** A flake is a defect in the *test*; change only
  `packages/tests`, never application code — unless a fix is provably impossible
  without it.
- **Kill the timing dependence, don't pad it.** A "timing issue" is fixed by
  waiting on a deterministic signal (a real DOM/aria state, an app event, an
  awaited promise, correct setup ordering) — *never* by bumping a timeout or
  adding a sleep.
- **Reuse past de-flake PRs.** Read how this suite was de-flaked before — start
  from [Common flake classes](#common-flake-classes) below — and apply the
  established pattern instead of inventing one.
- **Code changes pass `/codex-debate` before the streak counts.** Batch the fixes,
  then drive them through `/codex-debate` to consensus — a CI pass is only trusted
  on codex-debate-passed commits (docs are exempt, and the debate is expensive, so
  debate the batch, not each edit). The debate is read-only code review, so its
  consensus still needs **CI verification**: defend an empirically-grounded fix
  rather than concede a plausible-but-unverified simplification — a consensus that
  contradicts an observed CI failure is wrong until CI proves otherwise.

The loop: **fail** → streak resets to 0, root-cause and fix the test
(<Pill variant="run">fixing</Pill> → <Pill variant="ok">fixed</Pill>, link the PR
with `<PrLink pr={…} />`); while there, drop any <Pill variant="new">open</Pill>
row that no longer reproduces. **Pass** → streak +1. **Done = N green runs
back-to-back with the backlog cleared.**

## Common flake classes

The shapes this suite keeps throwing, and the fix that held — reach for these
before inventing one (full detail in each linked PR / the fix's code comment).

- **darwin drops a *second* fs event.** An *append* or a 2nd create (a transcript
  append, a `<pid>.json` after its dir) is the event FSEvents/inotify drops under
  parallel-worker load, so the watcher never re-fires. Fix: write
  **data-then-trigger** (the reliable first event carries the payload), or
  **`nudgeFiles` the path every poll tick** (`tests/support/nudge.ts`) to re-fire
  the watch. (`claude-code.feature:77`)
- **A one-shot resolve against a stream's first snapshot can't recover.** If a
  consumer resolves once on the first `!pending()` frame and the stream won't
  re-yield (state already settled), a stale first snapshot is permanent — no marker
  barrier saves it. Fix: **warm / mount the source first** so it has enumerated
  before the action. (`file-ref-link.feature:69`)
- **A baseline recorded mid-settle drifts.** Recording a transform/value while an
  animation or a sensor re-resolve is in flight captures a moving target. Fix:
  **settle-before-baseline** — wait on the steady-state signal (tile centered, git
  settled to the repo, tree row enumerated) before recording or asserting.
  (`canvas.feature:161`, `code-tab.feature:714`)
- **A passive reader observes a half-built git state.** A gitStatus first read can
  tear the stream down for good on `BASE_BRANCH_NOT_FOUND` if it lands between
  repo-init and base-ref creation. Fix: **make the base ref exist atomically**
  (seed a bare origin, then `git clone`) and **do setup in a subshell** so the
  terminal's cwd never enters the in-between repo. (`code-tab.feature:148`)
- **Faking occlusion by swallowing render output leaks.** Suppressing `refreshRows`
  still lets an incidental sync paint through. Fix: **model the real freeze — park
  `requestAnimationFrame`** so only a forced sync repaint counts.
  (`render_recovery.feature:16`)
- **An edge-triggered effect can't re-assert after a non-change** *(app fix)*. Focus
  stolen by a transient element when the reactive `focused` state didn't change
  leaves the edge effect unfired. Fix: **bump a level nonce** the handler increments
  so the effect re-asserts. (`sub-terminal.feature:107`)
- **Polling a live, reconciling collection throws transients** *(esp. vitest — no
  retry)*. A poll hits transient stream errors as the collection settles or a key
  reconciles out mid-read. Fix: **catch-and-retry in the poll**, and **skip a key
  that vanishes** between `keys()` and `get()` — the snapshot helper owns the narrow
  suppression, the poller stays a pure condition-checker. (`daemon.test.ts`)
- **A screen-state match string that hard-wraps at the grid edge is split by the
  reader.** xterm wraps a logical line longer than `cols` across rows; a reader that
  joins rows with `\n` inserts a newline INSIDE the wrapped token, so a substring
  check (`includes("notes.md")`) misses it even though the text is fully on screen.
  Flakes because the wrap column depends on content length and on `fit()` having
  widened the grid past the 80-col default — which lags under load. Fix: **rejoin
  `isWrapped` continuation rows** into one logical line in the buffer reader
  (`__readXtermBuffer`), trimming right only at the logical-line end. Width- and
  path-length-independent — never a timeout bump. (`file-drop.feature`, `clipboard.feature`)
