---
title: padi — the per-host terminal-workspace daemon
description: The architecture that replaces the R9/R10 finale plan for remote terminals (#951). One workspace daemon per host — padi, atop kaval — knows everything about that host's terminals and serves it as one surface (padiSurface); kolu-server thins to a web shell that connects each browser view to one padi. The canvas shows one host at a time (instant switch; other hosts reach you via badges and OS notifications). The host owns its state — layout, memory, clock. pulam and pulam-web dissolve. Phases W0–W5 (W4 = consolidation, W5 = demand-driven); W0–W2.2 shipped.
parents: [remote-terminals]
status: proposed
maturity: seedling
updated: 2026-07-03
---

import Callout from "../../components/Callout.astro";
import PrLink from "../../components/PrLink.astro";
import Issue from "../../components/Issue.astro";
import Svg from "../../components/Svg.astro";
import Phase from "../../components/Phase.astro";
import PhaseTree from "../../components/PhaseTree.astro";
import arch from "../../diagrams/padi-architecture.svg?raw";

**The ground-up re-architecture for <Issue n={951} /> — remote terminals — and the extraction that untangles kolu-server and the client along the way.** The old plan ([the finale](remote-terminals-finale.html)) tagged every terminal with a `HostLocation` and checked that tag all over kolu-server, because it assumed one canvas could mix tiles from several hosts. Drop that assumption and the problem gets much simpler: **"which host" is a property of the *connection*, not of each terminal.** One daemon per host — **padi** (படி, the stepped stand a koḷu is arranged on) — knows everything about that host's terminals and serves it as one complete surface. kolu-server becomes a thin web shell that *connects* each browser view to one padi. The four prep PRs (<PrLink pr={1637} /> <PrLink pr={1638} /> <PrLink pr={1639} /> <PrLink pr={1640} />) were closed; pulam and pulam-web dissolve into padi. Before committing, the design was stress-tested hard — seven independent reviewers each trying to break it a different way, every finding re-checked by a skeptic, plus a UX study of two real usage patterns; everything they caught is baked in below.

export const ROADMAP = [
  { d: 0, g: "✓", c: "ship", l: "W0 · decks cleared", m: "all landed", h: "#w0" },
  { d: 1, g: "✓", c: "ship", l: "overflow frame — typed overflow → re-attach on kaval's attach contract", m: "#1591 · kaval 5.0", h: "#w0" },
  { d: 1, g: "✓", c: "ship", l: "#1637–#1640 closed · finale superseded", m: "2026-07-01", h: "#w0" },
  { d: 1, g: "✓", c: "ship", l: "freeze terminalWorkspaceSurface at 3.0 · retire pulam-web", m: "#1650", h: "#w0" },
  { d: 0, g: "✓", c: "ship", l: "W1 · the padi seam, in place — ONE PR; commit stages contract → motion → rewiring", m: "#1652 · 2026-07-02", h: "#w1" },
  { d: 1, g: "✓", c: "ship", l: "W1.C · the contract — packages/padi born: padiSurface 1.0 + control core + tests", m: "in #1652", h: "#w1c" },
  { d: 1, g: "✓", c: "ship", l: "W1.M · the motion — the terminal domain relocates into @kolu/padi (severing commits named)", m: "in #1652", h: "#w1m" },
  { d: 1, g: "✓", c: "ship", l: "W1.R · the rewiring — serve complete · client onto the surface · root RPC dies · seal", m: "in #1652", h: "#w1r" },
  { d: 0, g: "○", c: "prog", l: "W2 · padi the process — 3 PRs ↓ · byte-identical · warm restarts", m: "needs W1 ✓", h: "#w2" },
  { d: 1, g: "✓", c: "ship", l: "W2.1 · the re-serve machinery in @kolu/surface* — forwarding policy · per-binding scope", m: "#1661 · drishti #84 adopts", h: "#w21" },
  { d: 1, g: "✓", c: "ship", l: "W2.2 · padi the process — ONE PR: binary + identity + owns kaval + the cutover (bind · re-serve · store migration)", m: "#1664 · 2026-07-03", h: "#w22" },
  { d: 1, g: "▶", c: "prog", l: "W2.3 · dial carve + padi-tui born + the burial — wait/status/create/worktree · pulam* retired · terminalWorkspaceSurface DELETED", m: "READY — W2.2 ✓", h: "#w23" },
  { d: 0, g: "○", c: "prog", l: "W3 · remote + the switch — 3 PRs ↓ · the user-visible feature", m: "needs W2", h: "#w3" },
  { d: 1, g: "○", c: "prog", l: "W3.1 · the remote binding — provision + front a remote padi (complete, invisible)", m: "parity e2e on a pu box", h: "#w31" },
  { d: 1, g: "○", c: "prog", l: "W3.2 · the switch — binding pool · per-view scope · host picker · clock handshake", m: "needs W3.1", h: "#w32" },
  { d: 1, g: "○", c: "prog", l: "W3.3 · cross-host attention — urgency fan-in · OS badge · notification deep-link", m: "needs W3.2", h: "#w33" },
  { d: 0, g: "○", c: "prog", l: "W4 · consolidation — refactors · decomplecting · electricity upstreaming · deletions", m: "→ the ledger note", h: "padi-cleanup.html" },
  { d: 0, g: "○", c: "last", l: "W5 · demand-driven — cross-host dock · kolu-tui · hybrid only if earned", m: "", h: "#w5" },
];

<PhaseTree title="ROADMAP — padi (replaces R9/R10) · kept up to date" phases={ROADMAP} />

export const PAL = { txt: "#c8d0de", sub: "#8b94a6", dim: "#5b6678", green: "#7ec699", amber: "#e6a23c", cyan: "#56b6c2", violet: "#a78bfa" };

export const Meter = ({ n, s = 9 }) => (
  <span style={`display:inline-flex;gap:2px;vertical-align:middle`} title={`${n}/5`}>
    {[1,2,3,4,5].map(i => (
      <span style={`width:${s}px;height:${s+2}px;border-radius:2px;${i <= n ? `background:${PAL.amber}` : "background:transparent;border:1px solid #2a3145"}`}></span>
    ))}
  </span>
);

export const SubDots = ({ items }) => (
  <span style="display:flex;flex-wrap:wrap;gap:.35rem 1.8ch;align-items:center">
    {items.map(([id, n, delta, done]) => (
      <span style={`white-space:nowrap;color:${done ? PAL.green : PAL.sub};font-size:.92em`}>
        {done ? "✓" : ""}{delta ? <span style={`color:${PAL.cyan};font-weight:700`}>Δ</span> : ""}{id}&nbsp;<Meter n={n} s={7} />
      </span>
    ))}
  </span>
);

export const LENS = [
  { p: "W0", h: "#w0", done: true, user: "nothing new", vis: false, work: 1, why: "deletions + one shipped kaval rev",
    beh: "a shed slow tab now recovers instead of freezing (shows only when it fires); kaval 4.x survivors recycled once at the 5.0 upgrade", sub: null },
  { p: "W1", h: "#w1", done: true, user: "nothing — pixels byte-identical, pinned by tests", vis: false, work: 4, why: "the client re-wired member-by-member; the root RPC namespace dies; one new contract",
    beh: "same process, but the rewiring stages (ΔR) changed how data flows — timestamps now stamped by the server · the Code tab re-fetches on change instead of receiving pushed values · preview bytes come through a procedure (with ranges), not a raw HTTP route · restore runs server-side (the old respawn race is gone); C and M changed nothing by construction",
    sub: [["C",2,false,true],["M",2,false,true],["R",4,true,true]] },
  { p: "W2", h: "#w2", user: "nothing — except kolu-server restarts stop losing terminal metadata", vis: false, work: 5, why: "process split · new daemon identity · re-serve machinery (drishti-gated) · store migration",
    beh: "the big shift, behind identical pixels — Δ2.2 a second daemon appears; sockets get new names (kaval-<port> retires); keystrokes take one extra hop (budget < 5ms); boot order changes; the session file moves (one-time import) · Δ2.3 `pulam-tui wait` becomes padi's wait",
    sub: [["2.1",4,false,true],["2.2",5,true,true],["2.3",2,true]] },
  { p: "W3", h: "#w3", user: "everything — host switcher · remote canvas as a full peer · cross-host badges", vis: true, work: 2, why: "thin by design: lights seams the prep built (dial stack reused as-is)",
    beh: "Δ3.2 each connection now belongs to one host — the client rebuilds its plumbing when you switch · Δ3.3 notifications now fire for hosts you are not looking at (3.1 ships switched off by default)",
    sub: [["3.1",2],["3.2",3,true],["3.3",1,true]] },
  { p: "W4", h: "#w4", user: "nothing — that is the point", vis: false, work: 3, why: "paying down what W2–W3's pace deferred: refactors, upstreaming, deletions off the ledger",
    beh: null, sub: null },
  { p: "W5", h: "#w5", user: "cross-host dock rows · kolu-tui — only if demanded", vis: true, faint: true, work: 1, why: "additive layers over unchanged daemons",
    beh: null, sub: null },
];

export const PhaseLens = () => (
  <div style="font:14px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace;background:#0f1117;border:1px solid #05070b;border-radius:11px;padding:1rem 1.3rem;margin:1.4rem 0;width:min(60rem,94vw);position:relative;left:50%;transform:translateX(-50%);box-sizing:border-box">
    <div style={`display:flex;justify-content:space-between;color:${PAL.dim};font-size:.8em;padding-bottom:.6rem;border-bottom:1px solid #1c2231`}>
      <span>phase · <span style={`color:${PAL.green}`}>◉ user sees</span> · <span style={`color:${PAL.cyan}`}>Δ behaviour</span></span>
      <span>architectural rework: <Meter n={3} s={7} /> = 3/5</span>
    </div>
    {LENS.map(r => (
      <div style="display:grid;grid-template-columns:5ch 1fr;column-gap:1ch;padding:.75rem 0;border-bottom:1px solid #141926">
        <a href={r.h} style={`color:${r.done ? PAL.green : PAL.cyan};text-decoration:none;font-weight:700;font-size:1.08em;white-space:nowrap`}>{r.done ? "✓ " : ""}{r.p}</a>
        <div style="display:flex;flex-direction:column;gap:.4rem;min-width:0">
          <span style={`color:${r.vis ? (r.faint ? PAL.sub : PAL.green) : PAL.dim}`}>{r.vis ? "◉ " : "○ "}{r.user}</span>
          <span style={`color:${r.beh ? PAL.txt : PAL.dim};font-size:.95em`}><span style={`color:${PAL.cyan};font-weight:700`}>Δ </span>{r.beh ?? "no runtime-behaviour change until built"}</span>
          <span style="display:flex;align-items:center;gap:1.2ch;flex-wrap:wrap">
            <Meter n={r.work} s={11} />
            <span style={`color:${PAL.sub};font-size:.95em`}>{r.why}</span>
          </span>
          {r.sub && <SubDots items={r.sub} />}
        </div>
      </div>
    ))}
    <div style={`color:${PAL.dim};font-size:.8em;padding-top:.6rem`}><span style={`color:${PAL.green}`}>✓ shipped</span> · ◉ user-visible · ○ invisible · <span style={`color:${PAL.cyan}`}>Δ</span> runtime-behaviour change — pixels can be byte-identical while the system runs differently (a Δ-marked sub-PR is where that happens). The shape to notice: rework <em>and</em> the behaviour cliff (W1–W2) land where the user sees nothing; the user-loud phase (W3) is deliberately thin.</div>
  </div>
);

<PhaseLens />

export const HostChip = ({ name, active, badge, remote }) => (
  <span style={`display:inline-flex;align-items:center;gap:.5ch;padding:.18rem .65rem;border-radius:6px;font-size:.85em;${active ? `background:rgba(126,198,153,.13);color:${PAL.txt};border:1px solid rgba(126,198,153,.4)` : `color:${PAL.dim};border:1px solid #222838`}`}>
    {remote && <span style={`color:${PAL.violet};font-size:.85em`}>ssh</span>}
    {name}
    {badge && <span style={`background:${PAL.amber};color:#0f1117;border-radius:8px;padding:0 .55ch;font-weight:700;font-size:.8em`}>{badge}</span>}
  </span>
);

export const Switcher = () => (
  <div style="font:13px/1.6 ui-monospace,SFMono-Regular,Menlo,monospace;background:#0f1117;border:1px solid #05070b;border-radius:11px;overflow:hidden;margin:1.2rem 0;max-width:44rem;box-shadow:0 10px 34px rgba(0,0,0,.4)">
    <div style="display:flex;align-items:center;gap:.45rem;padding:.5rem .8rem;background:#0d0f15;border-bottom:1px solid #1c2231">
      <span style="color:#5b6678;font-size:.8em">host</span>
      <HostChip name="local" />
      <HostChip name="nix@prod" active remote />
      <HostChip name="pu-kolu-3" badge="2" remote />
      <span style="margin-left:.2rem;color:#3f4858">+ host</span>
      <span style="margin-left:auto;color:#5b6678;font-size:.8em">⌥H switch</span>
    </div>
    <div style="padding:.7rem .9rem;color:#8b94a6">
      The whole canvas is <span style={`color:${PAL.violet}`}>nix@prod</span>'s — its own layout, its own dock, exactly as any other device sees it.
      <span style={`color:${PAL.amber}`}> pu-kolu-3</span> has 2 agents awaiting you: the chip badge, the PWA app badge, and an OS notification all say so —
      clicking the notification switches the view and focuses the tile (one action).
    </div>
  </div>
);

## User-facing description

<Switcher />

Nothing changes until W3. Then: the ChromeBar grows a **host switcher** — *kinda like tmux sessions*. A canvas shows **one host's terminals**; switching hosts swaps the whole canvas instantly (bindings stay warm server-side). A host's arrangement — tile layout, names, themes, remembered agent commands — **lives on that host**, so your desktop, laptop, and phone all see the same canvas for `nix@prod`, and a teammate-free second kolu (or a future kolu-tui) joins the same arrangement. A remote canvas is a **full peer** of the local one: Code tab, binary preview, paste/upload, transcript export, sleep/wake, session restore — all against the terminal's own host, nothing silently wrong-host.

While you're parked on one host, the others reach you through the channels an installed PWA already owns: the **app badge** aggregates awaiting-agents across all bound hosts, the **switcher chips** carry per-host counts, and an **OS notification's click is one action** — switch binding + focus the tile. (The per-terminal plumbing — `setAppBadge`, service-worker notifications — already exists; W3 aggregates it across bindings.)

Two personas anchor the design, and both get the same shape: an **ephemeral cloud box** (a pu-style instance per project — teardown removes the host from the picker as one clean unit, never wreckage on a shared canvas) and a **permanent headless server** (days-long agents; hop local⇄server many times a day — instant switch + 1-action notification response is the tmux-grade loop that persona lives in).

<Callout kind="note" title="Why not hybrid tiles (several hosts on one canvas)? Evaluated, deferred — not foreclosed">
We compared three designs against real usage: plain switching (A), switching with warm connections and badges (A+ — this design), and mixed tiles (B). A+ gives nearly everything B would: you find out when an agent elsewhere needs you, you respond in one click, every device shows a host's canvas the same way, and tearing down a cloud box removes it cleanly. B's one real advantage is watching two hosts' terminals side by side in one window. Its real cost: two tiles that look identical — same repo, same branch, one of them is prod — differing only by a tiny badge. That's a wrong-host paste waiting to happen. And when the app is in the background (most of the day), B's on-screen indicators are invisible anyway; both designs end up relying on the OS notification. So: ship A+, and keep B possible later — it would be a layer on top of the same daemons, nothing below changes. If switching ever feels insufficient, the first thing to try is a **cross-host dock** (W5): other hosts' agents listed in your dock, click = switch — you want attention across hosts, not tiles across hosts. The dock data reserves a host field from W1 so this can land without breaking the contract.
</Callout>

## Architecture-level changes

<Svg svg={arch} caption="The padi architecture. Every host runs kaval (PTY survivor, unchanged role) + padi (the workspace authority: registry, one fold on the host's clock, lifecycle, fs/git+bytes, persistence, kaval supervision) serving padiSurface — the ONE complete surface. kolu-server is a web shell holding a pool of warm bindings; each client connection is scoped to one binding, so a host switch closes one scope and opens another — no global rebind exists. Local and remote legs are byte-identical; remote adds ssh + frontDaemonOverStdio + Nix provisioning." />

**The gap this fixes.** Today, no single thing can tell you everything about one host's terminals. kaval has the PTYs. pulam has the awareness and fs/git — which it gets by dialing kaval itself. Everything else — layout, memory, restore targets, saved sessions, sleep/wake, boot adoption — lives only inside kolu-server. Because there was no one place to point at, the old plan had to tag every terminal with its host and check the tag at ~11 different places, behind two different lookup helpers. Meanwhile the client never needed any of it: it talks to one server over one socket, and no client code reads a host field. padi fixes this at the right level: **below, one daemon that knows everything about its host's terminals; above, one connection per view that picks the host; nothing per-terminal anywhere.**

### The three layers, and what each owns

| layer | owns | explicitly does NOT own |
| --- | --- | --- |
| **kaval** (per padi) | PTYs, screen mirror, VT taps, inventory — unchanged role, byte-minimal; the overflow frame **shipped** (<PrLink pr={1591} />, contract 5.0) | awareness, git, sleep, persistence (as today) |
| **padi** (per host × state-root) | the registry (**`terminals` = authored ⋈ snapshot, composed server-side**); the **one fold** + `AgentMemory` on **the host's clock**; `restoreTarget`; lifecycle (create · kill · sleep/wake · boot adoption · inventory reconcile); spawn-policy composition against its kaval's `system.info`; fs/git endpoint + watcher pulses; byte procedures (`previewRead` · `scratch.write`); `transcript.exportHtml` (the agent loaders run host-side — codex/opencode transcripts are **SQLite queries**, not file reads); **worktree create/remove**; the **MRU activity feed** (`recentRepos`/`recentAgents` — host-fs facts written inside the fold's emit path); session persistence in its state-root; **kaval supervision**; status cells (kaval `daemonStatus`, `expectedKaval`); the **urgency projection** (awaiting counts + ids — no recency); the frozen **control core** (hello · version · drain · `clock.now`) | serving browsers; user preferences; which client looks at it |
| **kolu-server** | HTTP · static · PWA · websocket; user-scoped preferences; buildInfo (minus `expectedKaval`, which moves to padi); the **binding pool** (dial + supervise-or-front padis; per-connection scope; re-serve); binding/link status on its own shell surface; notification fan-in across bindings | **terminals — it writes nothing about them.** No registry, no fold, no adoption, no terminal-derived cells — with one read-only exception: the **cross-binding urgency sum** for badges, sourced solely from each padi's urgency-projection member |
| **client** | one padiSurface consumer per view + the shell surface; ages recency against the **bind-time clock offset** (never the browser clock vs a foreign clock) | per-terminal host routing — there is nothing left to route with; `HostLocation` is deleted from records and create input |

**padi's identity is its state-root — the folder on the host that holds its data — and clients never kill a running padi.** (kaval today is namespaced per kolu-server port precisely because its supervisor kills-and-respawns on version mismatch, and a shared daemon once let a dev server kill prod's — the #1313 incident.) padi flips the relationship: **one padi per (host, state-root)**. The deployed kolu binds the host's default state-root; a dev kolu and each e2e worker pass their own private one, which also gives them a private kaval. A dev instance therefore *can't* touch prod's daemon, and a second kolu binding the same state-root *does* see the same canvas — sharing vs isolation is now an explicit choice, not a port accident. The mechanics (each exists because a reviewer constructed the failure without it):

- **The data folder is permanent; the socket is not.** The state-root must survive reboots — restore depends on it. But the socket and the lock file live in the runtime dir, named by a hash of the state-root path (`$XDG_RUNTIME_DIR/padi-<digest>/`; same scheme for padi's kaval). The runtime dir is wiped at every boot, so a stale lock can never make a dead padi look alive, and socket paths stay short no matter how deep the state-root sits. A small file in each runtime dir maps hash → state-root, so `kaval-tui`'s no-flags discovery keeps working and can label what it finds — that migration ships in the same PR, not a release note.
- **The padi binary decides the default state-root, on the host.** One fixed rule in one place — never computed by a client, never sent over the wire. Otherwise two ways of reaching the host (login shell vs ssh, with different env) could compute two different "defaults" and silently split the host's terminals across two padis. Clients pass nothing (default) or an explicit path (dev/e2e).
- **Clients may start or join a padi — never restart one.** This is a new, third supervisor policy, named so nobody reaches for the existing one (which kills-and-respawns on version mismatch — exactly what must not happen here). Two clients racing to start the same padi is safe: taking the lock is atomic, and the loser just connects to the winner.
- **Upgrades converge on the newest version, through a small frozen "control core".** padiSurface will grow, so version mismatches will happen — and if "restart" only existed inside the versioned contract, the one moment you need it is the moment you can't call it. So padi also serves a tiny never-changing side channel: hello · version · **drain** · `clock.now`. The rule: a client *older* than the running padi is refused, loudly (upgrade your kolu). A client *newer* may ask padi to **drain** — save everything and exit; the PTYs stay alive in kaval — and then start the newer version. Newest always wins, so two clients at different versions converge instead of fighting, and nothing ever force-kills a padi.

### The state model — what survives from the awareness design, and what moves

The [observe-vs-remember split](awareness-derive-store.html) survives unchanged *inside* padi: the sensors still cannot write memory (the types forbid it), the fold stays the one and only writer, and the shipped types are reused as-is. What moves is *where remembering lives*: sensors and fold now sit in the same process, so nothing folded ever has to be rebuilt across a wire — the whole planned event-stream apparatus (#1638's framer, sequence numbers, gap frames) is simply never built, and the old fold-overwrite bug class (#1614) can't happen because there's no second writer left. Two consequences, decided out loud rather than slipped in:

- **The host's clock stamps memory now.** (The old rule said the consumer's clock — but that rule existed for a mixed-host canvas, which no longer exists. Within one host, its own clock is the only one; two hosts' times are never compared, because no view ever shows two hosts.) For display ("2m ago"), the client measures the clock difference once per connection — one `clock.now()` round-trip, halved — and ages everything locally against that offset, so even a badly-skewed host shows sane ages. We deliberately do NOT serve a ticking "now" value: it would either be stale at connect or push a useless frame to every connection forever. Anything that does cross hosts (badges, the future cross-host dock) carries **counts only, never times**.
- **The client-side join retires.** R8 split a terminal's data into two collections because two processes wrote the two halves. In padi one process writes both, so it serves one composed **`terminals`** collection (the same compose function, now run server-side; disk saving keeps using it too). The two-halves split lives on as internal types — the fence that keeps sensors from writing memory — it just stops being a wire format.

### Session state and restore — the host restores itself

Today session state lives in kolu-server's config store, and **restore is client-driven**: after a restart the browser re-creates saved terminals one `create` call at a time, with a known race against PTYs that survived. Under padi, session state lives with the process that can actually guarantee it, and the client stops participating:

| state | lives | survives |
| --- | --- | --- |
| live PTYs + scrollback | **kaval** (RAM) | padi restarts, kolu-server restarts, ssh blips |
| terminal session — authored records (layout · chrome · active tile · panel sizes · memory · `restoreTarget`) + the restore-relevant awareness projection | **padi's state-root, on the host** (persistent storage — pinned) | **host reboots included** — at a reboot only the PTYs die (kaval's row); the records park for restore |
| user preferences · binding pool (which hosts, last binding per view) | **kolu-server** conf-store | kolu-server has no terminal state to lose |
| per-tab view posture (canvas-maximized, minimap-expanded, …) | **browser localStorage, namespaced per binding** | host switches don't collide two hosts' view-state. (Active tile + panel sizes are NOT here — they ride padi's authored session via the chrome procedures, as today's `setActive`/`setSubPanel` persistence does) |

The four restore flows, each owned by the layer that knows:

1. **padi restarts** (deploy, crash): on boot it adopts surviving kaval PTYs by id, reconciles them against its persisted session, re-seeds fold memory from its store, and re-runs sensors fresh (the #1031 posture). By the time any client binds, the canvas is already whole — no client participates.
2. **Host reboots** (the PTYs are gone): padi's boot finds saved records with no live PTYs and marks them **`parked`** — a distinct state. Deliberately-slept terminals stay `sleeping`, untouched: waking a slept terminal resumes its agent, so parking must not blur the two (they restore differently, exactly as today). padi **never auto-respawns agents with nobody watching** — and a warm server-side connection is not a watcher — so restore is never a side effect of connecting. The browser shows today's restore card over the parked records, and your click calls `session.restore({resumeIds})` (per-terminal opt-out preserved). The respawn then runs inside padi, the single writer, and calling it twice is harmless (each record flips parked→active exactly once). The old client-loop race is gone by construction. "Import session" becomes a padi procedure too.
3. **kolu-server restarts**: nothing happens to terminals at all — padi's registry stays warm (strictly better than today, where every restart re-derives all metadata). The browser reconnects and rebinds; the canvas is byte-identical.
4. **Browser reload / device switch**: pure re-subscribe. The client no longer contains respawn logic of any kind; a reload against a rebooted *remote* host lands in flow 2 on that host's padi, same as local.

Two safety rules came out of W1's shipping rounds, each pinned by a test that fails if its guard is removed (`reattach.test.ts`, `restartLocal.test.ts`): **nothing but the user may ever empty or shrink a non-empty saved session** — parked records freeze the autosave, and the restart's capture→drain→park window freezes it entirely (that transiently-empty moment is exactly where a session got eaten on a real host); and **a daemon we adopt that isn't the one we recorded pairing with** (compared by `startedAt`) **is treated as a fresh boot** — preserve and park, never overwrite the saved session with the new daemon's emptiness.

<a id="padisurface"></a>

### padiSurface — one contract, with per-member forwarding semantics

padi serves a **new** surface, `padiSurface` 1.0. (The old `terminalWorkspaceSurface` is frozen at 3.0 and dies with the pulam daemons at W2.3 — growing it instead would have forced every new member through three packages we're deleting, because a served surface must implement every member or refuse to start.) Members: `version` + status cells; the `activityFeed` (MRU) + `session` cells (padi-owned on the wire from W1; conf-store storage moves to the state-root at W2.2); the composed `terminals` collection (record states `active | sleeping | parked`; the dock projection carries the optional host axis, reserved); the **urgency projection** (awaiting counts + terminal ids, recency-free — the sole thing kolu-server reads from *every* warm binding, for badge fan-in); `activity` stream; repo/file `{seq}` pulses; fs/git procedures (incl. worktree ops); byte procedures; `transcript.exportHtml`; lifecycle + chrome procedures; `session.restore` / `session.import`; `terminalExit` event; **`terminalAttach`**. Beside the surface, the frozen **control core** (hello · version · drain · `clock.now`) that never versions.

**Per-connection scope is new machinery, and we say so plainly.** Today the framework wires every member to one store at startup, and every websocket shares one router — nothing is per-connection. W2/W3 add it: one mirror store per bound host (shared by all connections watching that host), a router chosen by the connection's declared host, and the client's global wire objects rebuilt per binding, so an in-app switch is a clean rebuild (v1 of the switch may simply be a page reload). Whatever part of this lands in `@kolu/surface*` goes through the usual drishti check.

The contract's one novelty, forced by review: **every member declares how it may be relayed**. Value members (cells, collections, pulses) can be held open across a hiccup and replayed — replaying a value is harmless. **Byte streams (`terminalAttach`, `activity`) must fail through instead**: if the padi↔kolu-server leg drops, the browser's stream must end too, so the existing retry reconnects end-to-end and a scrollback snapshot only ever arrives as the *first* frame of a *fresh* stream. (Holding a byte stream open and splicing a replayed snapshot into a live terminal would corrupt the screen — the relay helper makes that a type error, not a convention to remember.) Attach keeps its special path — one dedicated stream per subscriber, straight through every hop — and the shipped overflow signal (<PrLink pr={1591} />) rides it unchanged. The relay helpers grow out of pulam-web's `reserve` into the shared surface stack, behind the usual drishti check.

### Rejected shapes (each killed by a specific review finding)

- **Per-terminal `HostLocation` tagging** (the finale plan): checked the host at a dozen places when one place (the connection) suffices — and the client never needed it at all. Closed with #1637–#1640.
- **Merging kaval into padi**: padi bundles the system's highest-churn code (agent sensors, fs/git serving); kaval earns "restarts only on contract change" by containing nothing that churns. Fusing them means sensor churn kills PTYs. padi restarts are instead *cheap by design*: re-seed from the state-root + fresh sensors (the #1031 posture — warm across web-shell churn, honestly re-derived across padi churn).
- **Server-global binding with a "rebind + client reset" switch** (the brief's own first draft): behind a constant origin, in-flight id/repoPath-keyed calls and every other device silently straddle a rebind — a two-actor temporal convention with a race window. Replaced by **per-connection binding scope**: a call minted under one binding cannot reach another, and other devices are untouched by your switch.
- **Local padi as in-process library, process only for remote**: keeps two assemblies (the drift the one-library-two-homes era already suffered), keeps kolu-server fat, and forfeits warm-across-restart metadata and multi-client (kolu-tui) for the local host.

## Implementation details

<Phase id="W0" name="decks cleared — freeze the old contract, retire pulam-web, land the overflow frame, close the PRs" status="done" />
<Phase id="W1" name="the padi seam, in place — padiSurface served in-process, client migrated, root oRPC dispositioned" status="shipped" needs={["W0"]} links={[{ label: "#1652", href: "https://github.com/juspay/kolu/pull/1652" }]} />
<Phase id="W2" name="padi the process — state-root identity, owns kaval, on-host persistence; kolu-server binds + re-serves" status="todo" needs={["W1"]} />
<Phase id="W3" name="remote + the switch — ssh/Nix leg, host picker, per-view bindings, cross-host badges" status="todo" needs={["W2"]} />
<Phase id="W4" name="consolidation — the cleanup ledger: refactors, decomplecting, electricity upstreaming, deletions" status="todo" needs={["W3"]} />
<Phase id="W5" name="demand-driven — cross-host dock (B-lite) · kolu-tui · hybrid canvas only if earned" status="todo" needs={["W4"]} />

**When packages are born** — graduation is a scheduled event, not a side effect:

| package | born | why then, not earlier/later |
| --- | --- | --- |
| `@kolu/padi` | **W1.C** | location is structure: code destined for the daemon must not camp in `packages/server`. Package at W1, **process at W2.2**. **The dependency arrow: padi imports no kolu app package** (libraries only) — the app imports padi, never the reverse. Otherwise app churn would pollute padi's restart hash, and kolu-tui (which exists to prove padi needs no kolu) would drag kolu back in |
| re-serve machinery | **W2.1** ✓ <PrLink pr={1661} />, into existing `@kolu/surface` / `surface-nix-host` | it generalizes proven pulam-web code for a second consumer (kolu-server) — that *is* the graduation moment; no new package, it extends the receptacle that already owns mirroring (drishti-gated) |
| the control core | lives in `@kolu/padi` | extraction is gated on electricity test ② (a real axis of variation), and with one daemon the axis doesn't vary yet — nothing to encapsulate. If a second daemon adopts it, that's test ③'s after-the-fact proof and it graduates to `@kolu/surface-daemon`. (Corrected 2026-07-03: an earlier wording here inverted the doctrine into 'proof before extraction' — the second consumer is the proof extraction was right, never the gate for doing it) |
| padi's `wait` (the agent done-signal) | **W2.3**, a subcommand of the padi binary | no new package: the kaval-tui precedent (separate TUI package) is for a full client; a done-signal probe rides the daemon's own CLI |
| `kolu-tui` | **W5**, new package | a new consumer app, not an extraction — it exists to prove padiSurface serves a second frontend (test ③'s proof). W5 because it's demand-driven, not because anything forbids it earlier |

<a id="w0"></a>

### W0 — decks cleared *(all three landed)*

1. **Freeze `terminalWorkspaceSurface` at 3.0 + retire pulam-web** — ✓ **shipped** (<PrLink pr={1650} />). No member is ever added to the frozen surface (this alone breaks the lockstep: padiSurface is new, so nothing forces members through the dying consumers); pulam-web goes with its flake/nix wiring in the same change. **pulam-tui and the pulam daemon survive until W2**: `pulam-tui wait` is the agent-drives-agent done-signal (the `/kolu` and `/debate` skills, the dock-fleet-mirror rule, and the home-manager module all depend on it), so they retire only when W2 re-homes that signal.
2. **The overflow frame** — ✓ **shipped** (<PrLink pr={1591} />, kaval contract **5.0**): kaval's attach output carries the typed *overflow → re-attach* signal, and the recovery loop (`reattachingDeltas.ts`) re-attaches with a fresh snapshot, distinguishable from PTY exit. The loop landed in kolu-server's endpoint — it **migrates into padi with the domain at W2**, and W1.4 re-runs its e2e through `padiSurface.terminalAttach`.
3. **Close <PrLink pr={1637} /> <PrLink pr={1638} /> <PrLink pr={1639} /> <PrLink pr={1640} />** — ✓ done (2026-07-01, each with a pointer here); the [finale](remote-terminals-finale.html) is marked superseded. #1577 (kaval transcript memory) is orthogonal and unaffected.

<a id="w1"></a>

### W1 — the padi seam, in place — *shipped* <PrLink pr={1652} /> *(ONE PR; commit stages contract → motion → rewiring)*

**Shipped 2026-07-02** as planned: one PR, the contract → motion → rewiring commit structure, no adapter ever existed, all five seal checks green, pixels identical, CI on both platforms. The review-and-deploy rounds added three **session-loss fixes**, each first reproduced as a failing test (and each test fails again if you remove its guard): (1) booting against a *replaced*, empty kaval — not the daemon we recorded pairing with, compared by `startedAt` — now preserves and parks the saved session instead of overwriting it with emptiness; (2) creating a terminal while parked records await restore no longer silently forfeits them; (3) the in-app "Restart kaval" had a moment where the autosave could fire while the registry was briefly empty and wipe the just-saved session (a real incident on a deployed host, diagnosed live from log-only tracing) — the autosave is now frozen across the whole restart window. Session writes carry **permanent trace logging**: every destructive write logs a stack, so it can never happen silently. The owed **typing-speed baseline** is now delivered too (#1660): p50 2.14 ms · p99 **4.36 ms** on a quiet box — so W2.2's ceiling is ≈ 9.3 ms p99, same box, same method (see [the baseline note](padi-latency-baseline.html)). What's left of koluSurface: exactly `preferences` + `processMemory`.

Define **`padiSurface` 1.0**, relocate the terminal domain into **`packages/padi`** (born at W1.C — location is structure: code destined for padi camping in `packages/server` would make the seal a bespoke test fighting gravity, and W2.2 a double move), then rewire the client onto the surface. The phase proves one path — *the client runs entirely on padiSurface + the shell surface, with the in-process server behind the same contract a remote padi will serve* — sealed at the end of W1.R.

**W1 was ONE PR** (settled after reviewing the first attempt at slicing it, <PrLink pr={1651} /> — closed; its contract work carried over). Splitting W1 across PRs kept failing the same two ways: every intermediate PR had to leave the app pixel-identical, which forces throwaway scaffolding (#1651 needed a ~160-line adapter whose only job was making its PR boundary green), and a pixel-identical partial PR tells a reviewer almost nothing — the design is only visible in the full before/after diff. One PR avoids both: **only the final state must be green and pixel-identical**, and you review the whole design once. The ordering survived as the **commit structure** — **C** (the contract: small, read every line), **M** (the motion: big, skimmable renames), **R** (the rewiring: the dangerous part, one member per commit) — so history stays bisectable without the scaffolding. Intermediate commits don't each need green CI; the safety net (e2e, differential tests, the seal) runs against the final state. The old root RPC namespace died entirely, per member:

| root oRPC member | disposition | stage |
| --- | --- | --- |
| `terminal.create/kill/killAll`<br/>`terminal.sleep/wake`<br/>`terminal.discardSleeping/restoreSleeping`<br/>`terminal.setTheme/setIntent/setParent/setActive`<br/>`terminal.setCanvasLayout/setSubPanel/setRightPanel`<br/>`terminal.resize/sendInput` | padiSurface lifecycle + chrome procedures (`create` **loses the client-supplied `lastActivityAt`** — padi stamps) | W1.R |
| `terminal.attach` | `padiSurface.terminalAttach` — the fail-through per-subscriber stream (the carve-out, kept) | W1.R |
| `terminal.screenState/screenText` | padiSurface procedures | W1.R |
| `terminal.pasteImage/uploadFile` | `scratch.write` + a paste procedure (bytes land on the terminal's host) | W1.R |
| `terminal.exportTranscriptHtml` | `transcript.exportHtml({terminalId, mode}) → {html, filename}` — loaders (claude JSONL, codex/opencode SQLite) run host-side | W1.R |
| `git.worktreeCreate/worktreeRemove` | padiSurface git procedures — worktrees are the host's filesystem, so creating one on the wrong machine becomes impossible to even write | W1.R |
| the client-driven restore loop (`useSessionRestore` respawns) | deleted — padi's boot reconcile + `session.restore`/`session.import` (see the restore section) | W1.R |
| `daemon.restart` | becomes a binding procedure — *restart the bound padi* (drain-and-exit; PTYs survive) — when the binding exists | W2 |
| `server.info` | stays — web shell | — |

<a id="w1c"></a>

#### W1.C — the contract *(commit stage 1: small and dense — read every line)*

**Creates `packages/padi`** — the daemon package, from day one, though no process exists until W2.2 (the export-map split `terminal-workspace` already uses: `./surface` browser-safe, the node-only side beside it). In it, the **contract only** — nothing served, zero runtime change: every member from the [surface section](#padisurface) — the composed `terminals` collection (with `parked` in the record-state union and the host axis reserved on the dock projection), the urgency projection (counts + ids, recency-free), `activity`, the repo/file pulses, fs/git + worktree + byte procedures (**`preview.read` is range-capable from day one** — serve-dir-shaped `{status, headers, bodyBase64}` with an optional `range`; a whole-file-blob-only shape would bake a large-file regression into the contract), `transcript.exportHtml`, lifecycle + chrome procedures, `session.restore`/`session.import`, `terminalExit`, `terminalAttach` — each annotated with its **forwarding policy** (value vs fail-through), plus the frozen control-core types (hello · version · drain · `clock.now` — they live in `@kolu/padi`, and graduate to a shared package whenever a second daemon wants them). This is #1651's good half, carried nearly verbatim.
- **Stage acceptance:** the contract test pins the member list, **set-equality** between members and policy annotations (no unannotated member, no orphan), delta = exactly {`activity`, `terminalAttach`}, and version `1.0`; nothing else in the repo changes.

<a id="w1m"></a>

#### W1.M — the motion *(commit stages after C: huge but skimmable — nothing changes)*

Pure relocation of the terminal domain into `@kolu/padi`, verbatim: the registry, the lifecycle functions (`terminals.ts` and the endpoint's spawn/kill/sleep/wake/adopt paths), the fold + metadata commit path, the terminal-workspace endpoint bindings, the scratch/transcript/worktree helpers (the transcript loaders behind one extracted `transcriptExport` entry), session persistence + reconcile, the MRU trackers — **and the vocabulary**: the terminal-domain schemas out of `kolu-common` (`HostLocation` · `TerminalEndpoint`/`TerminalHandle` · `TerminalId` · the `SavedSession`/`SavedTerminal` family · upload/transcript domain bits) into padi's browser-safe `./surface`, with kolu-common/client/server importing from `@kolu/padi/surface` directly (no re-export shim — kolu-common ends up app-shared and terminal-free). kolu-server's imports repoint; **no logic changes, no wire changes, no new callers**. Split into commits by area (domain core · fs/bytes · session); each commit stays pure-motion. Where a tendril into server internals (surfaceCtx cells, conf-store, config) must be severed to make the move possible, the severing is its own named commit BEFORE the move — never smuggled inside a rename diff.
- **Stage acceptance:** git detects the moves as renames; every existing test passes unchanged; zero wire/UX delta; `packages/server` retains only web-shell + serving code that *calls into* `@kolu/padi`.

<a id="w1r"></a>

#### W1.R — the rewiring *(the dangerous stages, after M; one member per commit)*

The package's assembly serves **padiSurface complete, natively** (`implementSurface` is fail-fast — no member may stub; every backing now lives in-package), and the client migrates onto it, one member per commit, in this order:

1. **the `terminals` collection** — `useTerminalMetadata` reads the one composed collection; `composeTerminalMetadata` moves server-side (still the single join shared with disk-save); the client join and its `authored`/`snapshots` subscriptions are deleted. *A differential test pins the client store byte-identical pre/post; Dock/StatePip/recency render unchanged.*
2. **lifecycle + chrome procedures** — the sixteen `terminal.*` RPCs per the table; `create` stops accepting `lastActivityAt` (padi stamps); root members deleted as call sites migrate.
3. **`terminalAttach` + screen** — the tile's attach swaps from the root `unenrolledStreamCall` onto the surface member, same per-subscriber fail-through semantics (snapshot only ever the first frame of a fresh stream); `screenState`/`screenText` become procedures. *The R5 slow-subscriber e2e (<PrLink pr={1591} />) re-runs green through the member; typing-echo latency captured as the W2 baseline.*
4. **Code tab on procedure + pulse** — re-derive #1639's client half (`createPolledQuery`, one `repoPulse`, the #818 selection-stability guard, binary-preview orchestration client-side); `koluSurface`'s four fs/git value streams are deleted.
5. **bytes** — the iframe preview route reads via range-capable `preview.read` (dropping the raw `@kolu/serve-dir` bypass); paste/upload via `scratch.write`; transcript export via `transcript.exportHtml`; worktrees via the git procedures. *The `..`/`%2f`/symlink 403s re-enforced at the endpoint and re-tested.*
6. **session** — the `parked` record state; boot reconcile parks reboot-killed actives (sleeping records untouched); the restore card calls `session.restore({resumeIds})` (consent + per-terminal opt-out preserved); `session.import`; `useSessionRestore`'s respawn loop deleted. *A fixture unit test pins the server-side restore against the old client loop (top-level active + sleeping + **sub-terminals incl. slept ones** + resume opt-out — the #1651 review caught the two implementations disagreeing on slept sub-terminals; the fixture decides the truth and pins it). A test asserts the client issues zero `create` calls during restore.*
7. **The last cells, then the seal.** The recent-repos/agents feed and the `session` cell become padiSurface members (padi owns them on the wire from W1; their on-disk storage stays in kolu-server's config until W2.2 gives padi its own folder). `terminalExit` moves to padi; the redundant `terminalList` cell is deleted (the ids are just the `terminals` keys); `expectedKaval` moves to padi's status; kolu-server's dormant copy of the old surface is deleted. Then the **seal** — tests that pin the boundary in both directions: `packages/server` contains no terminal code and imports only padi's public entry points; and padi imports *nothing* from kolu's packages — not types, not via namespace imports, and nothing anywhere in its dependency tree. (Both reverse rules exist because the first attempt broke them for real: one caused a boot crash, the other a backwards dependency a human reviewer caught.)

- **Done (= W1's done):** the seal test is green and fails on any reintroduced bypass or deep import; **no backings adapter exists** (one appearing mid-commit must be gone by the head — the stalled-strangler tell); UX byte-identical at the head; all e2e + the differential tests green; the R5 re-run green; the typing-echo latency baseline delivered (#1660, [method + numbers](padi-latency-baseline.html): p99 4.36 ms; W2.2's done-criterion (e) compares against it). W1 exits with `@kolu/padi` complete **as a library** — W2.2 gives it a process without touching the contract.

<a id="w2"></a>

### W2 — padi the process *(local; three PRs, phased below; UX byte-identical; restarts get warm)*

W1.R's seal guarantees the domain lives behind the contract, complete as a library; W2 gives it a process. **Every PR here is self-sufficient — it ships nothing that isn't consumed within it** (the W1 lesson, generalized: #1651's complete-but-unconsumed artifact was the tell that a PR boundary sat in the wrong place). So: **W2.1** is framework work, separate *because* it is self-contained — its capability is exercised by its own tests **and its paired drishti run** (the R7 precedent: drishti is the consumer-proof), and landing it first freezes the surface API before the big PR churns through review. **W2.2** is ONE PR in which the padi binary is born **and** kolu-server binds it — a daemon with zero consumers is a *commit stage*, not a PR. **W2.3** buries the pulam era (separable by nature: pure deletion + a new CLI over already-shipped procedures).

<a id="w21"></a>

#### W2.1 — the re-serve machinery in `@kolu/surface*` — *shipped* <PrLink pr={1661} />

**Shipped 2026-07-03** (squash `7e7a5b4f1`), hardened by the full gauntlet plus an external 8-angle / 23-candidate review with a 22-agent verification and a 3-auditor final pass. What the rounds added beyond the plan: the **`initialKeys` reconcile hook** on `@kolu/surface`'s `mirrorCollection` (kills stale rows across a reconnect; adopted by drishti — [srid/drishti#84](https://github.com/srid/drishti/pull/84) fixes its own ghost-PID bug with it, upgrading the gate to a real second-consumer proof); the policy re-scoped to a **streaming-survival axis** (streams + events only — cells always fold, procedures always forward); mirrored cells became a **read mirror** (write verbs throw loudly until W2.2 forwards them); and the audit's residuals were pinned into W2.2's done-criteria (c)/(g)/(h) below.

**W1 already shipped the classification** — every padiSurface member declares whether it's a replayable value or a fail-through byte stream, pinned by a contract test. W2.1 builds the *machinery* that honors it: move pulam-web's relay code into the shared surface stack, generalized — the per-member relay policy (values may be held open and replayed; byte streams must end downstream when upstream drops, and the helper refuses to hold open anything not declared replayable), a relay for input-keyed streams (pulam-web's version holds streams open, which is exactly wrong for attach), and per-host stores + per-connection routers (today everything is one global).
- **Done:** unit-proven against a toy surface (kill-the-middle-hop on a fail-through member terminates downstream; a hold-open member replays values); the **paired drishti PR** passes CI pinned to final kolu HEAD per `.claude/rules/surface.md`.

<a id="w22"></a>

#### W2.2 — padi the process: the binary AND the cutover — *shipped* <PrLink pr={1664} /> *(one PR, two commit stages)*

**Shipped 2026-07-03** (squash `590f49e48`), fully green both platforms, drishti #85 re-pinned at the final head. Beyond the plan, the endgame rounds (a 5-axis pre-gauntlet review, the gauntlet, six adoption VM tests, and three live defects srid caught dogfooding) added: the **newest-wins auto-drain arm** (a newer binder drains a skewed padi via the control core and respawns its own closure — upgrades converge without a human; an older binder is refused, never recycles anything); **Restart-kaval restored to its label** (a padiSurface procedure: freeze autosave → capture+park → recycle kaval → restore, padi staying up — the W1 invariant tests regained their production caller); the **migration adopter** (a pre-W2.2→W2.2 deploy ADOPTS the running port-keyed kaval — live agents survive the upgrade; converges to digest on the next recycle or reboot, the reboot bound pinned by a VM arm; this replaced a reap I had wrongly ruled — **deploy-adoption is doctrine: a deploy must adopt a compatible daemon and its live PTYs; restore is the path for a DEAD daemon only**); the **legacy-conf copy-aside** (`.pre-padi-import.bak`) before the one-shot import; `KAVAL_BUILD_ID` **baked** into padi's wrapper (the kaval-update nudge lives); **padiLink** on the shell surface (a padi drop shows honest connecting/degraded — padi cannot serve its own unreachability); the **split-restore trio** (client reconcile gated on daemon state · `viewSeeded` reset on in-session restore · **parked records immutable** — the record-level no-non-user-writer invariant); the **capture merge** (parked ∪ live, deduped by id, live wins — a restore-pending session can never be shrunk by a restart); the **3-process memory rail** (kolu-server · padi · kaval, event-driven — the one sanctioned pixel delta); `RemoteMirrorSession` published with the **variance fix** (consumer side generic in C; a compile-time pin encodes drishti's shape locally); and the measured hop: **+1.3 ms p99** against the +5 budget. Original plan text below, kept as the record.

**Commit stage 1 — the binary and its identity.** `packages/padi` has existed since W1; this stage gives it a **process entry** and its own Nix build, and kaval's rule starts applying: the package IS the process IS the restart-hash — the boundary was drawn back in W1, so nothing moves, it just gains a `main`. Identity per the architecture section: persistent state-root with the default **spelled by the padi binary on the host**; **digest-keyed runtime rendezvous** (`padi-<digest>/`, gates auto-invalidate at boot); 0700 socket; the frozen **control core** (hello · version · drain · `clock.now`) served for real. **The daemon-pairing work extends what W1 already shipped** — `lastPairedDaemon` (matched by `startedAt`), `isReplacedDaemon`, and the preserve-and-park boot are live on master. This stage moves that pairing record into the state-root and may strengthen the match (pid, and the handshake identity where present). padi **spawns-or-adopts its kaval** under `kaval-<digest>/` (retiring `kaval-<port>`), and `kaval-tui`'s **discovery + labels move to the digest pattern + a manifest (digest → state-root) in the same stage** — a flag-less `kaval-tui` keeps working throughout. *Stage acceptance: a dial test (kaval-tui-style) spawns-or-adopts padi at a private state-root, handshakes the control core, round-trips a terminal through `padiSurface` over the socket; two padis at distinct state-roots never touch each other's kaval (the #1313 property, by construction); `nix build` produces the padi closure with a stable staleKey.*

**Commit stage 2 — the cutover** (the reason this PR exists; nothing from stage 1 ships unused). kolu-server's in-process serving is deleted; it **binds** the local padi (**adopt-or-spawn-or-refuse** — a new supervisor policy; the existing endpoint's skew arm recycles and must not be reused) and **re-serves** through W2.1's machinery with the typed forwarding policy. Session persistence lands in the state-root: the conf-backed storage behind the `session` + `activityFeed` members (wire-owned by padi since W1) moves here, **plus the `lastPairedDaemon` pairing store** (a third conf-backed padi datum W1's bug rounds added), (the `activity` stream stays served-quiet — its live backing is **deferred to W2.3**, where its first real consumer is born; lighting it in W2.2 would ship a producer nobody reads). The one-time import has to decide **whose config wins** when several instances were sharing one file (this happened for real: a dev instance and the deployed one co-writing `~/.config/kolu/config.json` — exactly the collision the state-root ends). Answer: the importing padi takes the file as-is, once, then never falls back (a failed import crashes loudly rather than starting empty) — and the import **copies the old file aside first** (a one-time `.pre-padi-import.bak` beside the original, written by the import path itself), so a wrong pick stays recoverable. (#1658 — general boot-time backups — stays unimplemented and out of scope; srid's call.) Boot adoption, inventory reconcile, the MRU feed, and the R5 recovery loop (`reattachingDeltas`) now run inside padi. The e2e harness gives each cucumber worker a **private state-root** (its sandboxed `$HOME` exists; the agent-dir env vars flow into padi's spawn). The **mirrored-cell write story also lands its final form here** (flagged by W2.1's gauntlet): the re-serve narrows mirrored cell verbs to `["get"]` (as `connection` already is) and real writes **forward to padi** — retiring W2.1's interim shape, where write verbs exist on the wire but throw at invocation.

- **Done, each a test:** (a) UX byte-identical, all e2e green; (b) **kolu-server restart keeps metadata warm** (the registry survives in padi); (c) **kill-the-middle-hop**: killing padi mid-attach terminates the browser stream and the retry re-attaches with a fresh snapshot — never spliced bytes; killing kolu-server and rebinding **adopts, never respawns**. (W2.1's audit showed the re-attach is work, not a freebie: a mid-chain error reaches the browser as a non-retriable `ORPCError`, so the stock stream-retry will NOT fire — the end-to-end re-subscribe is application wiring this PR must build and test); (d) the fold differential pins local awareness byte-identical pre/post extraction; (e) typing-echo p99 within budget of the W1 baseline (&lt; 5ms added over #1660's 4.36 ms — re-run [the same bench](padi-latency-baseline.html) back-to-back on one quiet box); (f) **the W1 session-safety invariants survive the process split** — the restart-freeze (capture→drain→park) and replaced-daemon preserve+park tests (`restartLocal.test.ts`, `reattach.test.ts`) keep passing with the orchestration relocated into the padi process, and the *no non-user writer nulls/shrinks a non-empty blob* property is re-pinned where the restart now crosses the kolu↔padi wire; (g) **the deferred hold-open question from W2.1's review is resolved** (#1661 candidate 3a: a per-input upstream termination while the link stays live parks a held-open stream silently — deferred with a docstring TODO because classifying it races link-death) — before padi's value streams gain their first real consumer, either fix the classification or prove the case unreachable, with a test either way; (h) **the other W2.1-audit residuals are resolved**: `terminalExit` rides hold-open with no self-snapshot, so an exit fired during a link gap is lost to a held subscriber — make exit state re-observable at rebind (the `terminals` collection already carries it) or reclassify the event; the relay's upstream receive buffer (oRPC's queue) is unbounded with an unpaced sender — measure a slow-consumer burst and bound it if needed; and a mirrored cell declaring `equals` would dedup-skip an equal-valued write before the throwing store — pin it or make it moot via the verbs-narrow-to-get move above.

<a id="w23"></a>

#### W2.3 — padi-tui is born, and the burial *(needs W2.2)*

**First, the dial kit moves home**: `connectPadi` (state-root→digest→socket resolve · control-core handshake · typed skew refusal · the padiSurface client) is carved out of `packages/server/src/padiBinding.ts` into a **`@kolu/padi/dial`** entry — the kaval precedent: a daemon's package owns its client-side dial kit. kolu-server and padi-tui both import it (the seal's allowed-entries list grows by `/dial`); supervision/re-serve stay binder-only, the verbs stay tui-only. **Zero `@kolu/surface*` edits in this phase** — if a helper feels missing (e.g. wait-until-predicate over a collection), hand-roll it in padi-tui and note it on the W4 ledger (its upstreaming is gated on kolu-tui — W5 — as the second consumer that forces the honest split).

**`padi-tui` is born** — a small command-line tool on padi's socket, kaval-tui's sibling. Every verb is a thin wrapper over a padiSurface procedure that has existed since W1: `wait`/`status` (the precise "is the agent done / does it need me" signal, read from real agent state — better than kaval-tui's guess-from-silence), `create` (including `--parent` for a split tile), and `create --worktree <branch> -- <agent argv>` — a worktree'd agent in one command. The `activity` stream **gets its live backing here** (padi taps its kaval's bytes), arriving together with its first consumer — `padi-tui watch`/`status` — per the self-sufficiency rule that deferred it out of W2.2. One tool to spawn an agent and wait on its actual state. *(kolu-tui at W5 is a different thing — an interactive canvas; padi-tui is verbs, no canvas.)*

**The legacy conf sheds its fossils**: W2.2's import copies terminal keys (`session` · `activityFeed` · `sleepingTerminals` · `lastPairedDaemon`) into the state-root but deliberately leaves them in `~/.config/kolu/config.json` as rollback insurance; once the cutover has soaked, W2.3 deletes them (the `.pre-padi-import.bak` remains the archaeological copy) — a stale coherent-looking session in the old file is a debugging trap and a rollback-resurrection risk.

Then **pulam-tui and the pulam daemon retire** — padi-tui's `wait` takes over the done-signal first, so the agent-drives-agent workflow never loses it. The home-manager module, flake default, the `/kolu`/`/debate` skills, and the dock-fleet-mirror rule update in the same change. And the frozen **`terminalWorkspaceSurface` is deleted** — its last servers are gone; freezing (#1650) stopped the bleeding, this buries it. The `@kolu/terminal-workspace` library survives (sensors · fold · schema · endpoint · agentProjection) minus its surface. *(Why not earlier: pulam's one load-bearing job is the done-signal, and its replacement must talk to the padi that owns the real terminals — which only exists after W2.2. An interim over kolu-server's HTTP port was considered and rejected as throwaway glue.)*
- **Done:** `padi-tui wait` drives the `/kolu` skill's done-signal in a live loop; `padi-tui create --parent` yields a split tile and `create --worktree -- claude` yields a worktree'd agent, both visible in the browser canvas; no package serves or consumes `terminalWorkspaceSurface` (the export is gone); flake eval + home-manager module green.

<a id="w3"></a>

### W3 — remote + the switch *(three PRs, phased below; the user-visible feature)*

<a id="w31"></a>

#### W3.1 — the remote binding *(needs W2.3; complete but invisible)*

The named path this PR exists to prove: **`padiSurface` consumed over ssh**. Arch-keyed drv maps for padi (kaval rides *inside* padi's closure reference — one `provisionAgent` dial provisions both); `getHostSession` + `frontDaemonOverStdio` front the remote padi exactly as kaval-tui fronts kaval; blip-survival from `HostSession` reconnect + the durable daemon; the remote binding is reachable via a config/env knob only — **no UI yet**.
- **Done:** the remote-canvas **parity checklist e2e** against a real ssh host (a pu box): PTY echo · Code tab git/diff/tree · binary preview · paste/upload · transcript export (all three agent kinds) · sleep/wake · worktree flow · kolu-server restart re-adopt · browser reload re-dial · host-reboot park-and-restore — every axis, plus all local e2e green. *The done-criterion can only be met by driving the ssh leg — no local shortcut satisfies it.*

<a id="w32"></a>

#### W3.2 — the switch *(needs W3.1)*

Binding pool in kolu-server; **per-connection binding declared in the wire scope**; the client's wire singletons rebuilt **per binding** (the in-app instant switch — W2.1 laid the server-side scope); the **clock offset measured at each bind** (control-core `clock.now()`, RTT-halved); per-binding localStorage namespace for view posture; ChromeBar host picker (ssh-config sourced + free-text); persisted last-binding per view; #1034 loud connecting/degraded/skew states; create refused while the binding warms (today's `refuseIfWarming`, per binding).
- **Done:** an e2e switches local → remote → local with tiles live on both ends — no cross-binding call survives a switch (a stale in-flight id-keyed call is rejected, not misrouted); two browser views parked on different hosts stay independent; the switch never disturbs another device.

<a id="w33"></a>

#### W3.3 — cross-host attention *(needs W3.2; the A+ bar from the UX evaluation)*

kolu-server subscribes each warm binding's **urgency-projection member** (the one thing it reads from every pool host) and sums it onto the shell surface: PWA **app badge** aggregated across bindings; per-host **switcher-chip counts**; **OS notification whose click = switch + focus** (1 action). **Urgency-only** on any multi-host wire — no recency, no snapshots.
- **Done:** an agent flips to awaiting on host B while every view is parked on host A → chip count, app badge, and notification all fire; the notification click lands focused on the right tile; killing host B's link degrades its chip, never the app.

<a id="w4"></a>

### W4 — consolidation — the cleanup ledger *(scope lives in [its own note](padi-cleanup.html))*

W2 and W3 ship at pace (a deliberate call); W4 pays the pace's debts — refactors, decomplecting, framework-level electricity upstreaming, deletions. Nothing user-visible ships here; that is the point. **The full ledger — each entry a self-sufficient, lens-framed brief an implementing agent can execute under coordinator guidance — lives in [padi — the consolidation ledger](padi-cleanup.html)**, appraised and appended as each W phase lands (a shipped phase's "deferred / acceptable-for-scope" review items land there explicitly; an entry that never ripens is deleted with a reason). The index, for orientation:

- **Upstreamings (lowy · drishti-gated):** L1 poll-on-change client core → `@kolu/surface` (ready) · L2 padi-tui's wait helper (gated: kolu-tui) · L3 the control core → `surface-daemon` (gated: a second daemon)
- **Boundary re-cuts (lowy):** L6 `padiBinding.ts` post-carve audit (gated: W2.3) · L7 `terminal-workspace` post-burial re-cut (gated: W2.3) · L9 sensors out of the staleKey hash (gated: evidence)
- **Dedupes/sweeps (hickey):** L5 one home for `ProcessRssSchema` (ready) · L11 W3.2's interim wire shapes (gated: W3.2)
- **Deletions & hygiene:** L4 the legacy-kaval migration arm (at a named version) · L8 doc sweep for the buried world (after W2.3) · L10 the flaky-test debt (ready) · L12 fine-grained CI token (srid mints)

<a id="w5"></a>

### W5 — demand-driven

**Cross-host dock** (B-lite: foreign hosts' rows, urgency-ranked, click = switch + focus — the host axis is already in the contract) is the first escalation if switching proves insufficient. **kolu-tui** — the graduation proof: consume padiSurface over the socket/ssh *without kolu-server*; done only when that named path renders a live canvas (the path, not the feature, is the unit of done). **Hybrid canvas** (model B) only if one-window side-by-side proves a recurring demand — it stays an aggregation layer over N bindings, zero daemon changes.

### Risks, named, with mitigations

- **Attach latency gains a local hop** (kaval→padi→kolu-server vs kaval→kolu-server). Measure typing-echo p99 before/after W2 with a budget (&lt; 5ms added); if breached, the carve-out permits a raw-byte relay on the padi→kolu-server leg (frontDaemonOverStdio-style, no decode) without touching the contract.
- **Sensor code changes often, and any padi change restarts padi** (the restart-hash covers the whole package). Accepted: a padi restart re-reads its store and re-runs sensors — seconds, PTYs untouched. If deploys make this hurt in practice, the sensors can later be split out of the hash; we'll decide that on evidence, not up front.
- **Multi-client writes** (two kolu-servers, one padi): padi serializes all chrome/layout procedures — one writer, last-write-wins, concurrent editors see each other's moves live. Accepted semantics (same as any shared session).
- **drishti gate**: the graduated forwarding helpers (per-member policy, input-keyed forward) and any per-connection scope machinery landing in `@kolu/surface*` — paired drishti PR, pinned to final kolu HEAD, per `.claude/rules/surface.md`.
- **N warm bindings have a standing cost** (one ssh session + one urgency subscription per host, even while parked elsewhere). Bounded: the pool holds only user-added hosts, the subscription is the count-sized urgency member (never `terminals`), and an unreachable host degrades its chip, not the app. Costed explicitly rather than discovered in production.
- **Session-file migration**: one-shot import at padi's first boot, then hard cutover — a failed import crashes loudly (fail-fast), never silently starts empty.
- **Testing W2.2 on zest**: that box runs TWO instances sharing `~/.config/kolu/config.json` — the deployed one and a `faint-bottom` dev worktree carrying an older, pre-fix pairing implementation that can null the shared file and poison any repro. Stop or reconcile `faint-bottom` before testing there. (The state-root move being tested is exactly what ends this collision class.)

### Superseded by this note

The [finale](remote-terminals-finale.html) (whole plan: PR-1/2/3, F-REMOTE, R10) — closed PRs <PrLink pr={1637} />, <PrLink pr={1638} />, <PrLink pr={1639} />, <PrLink pr={1640} />. Partially superseded: [awareness-derive-store](awareness-derive-store.html) (the fold's *home* moves to padi; owner-clock replaces consumer-clock; producer, fold, and types survive verbatim) and [the terminal model](terminal-metadata-model.html) (the reader-join collapses into padi's composed `terminals` collection; the authored/snapshot split stays as padi-internal types). Unaffected: kaval's design ([pty-daemon](pty-daemon.html)) apart from the shipped overflow frame (contract 5.0, <PrLink pr={1591} />); #1577.
