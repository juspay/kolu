---
title: PR #1714 — live tracker (ephemeral)
description: Ephemeral task/bug tracker for the surface-map endgame, to be deleted after merge — what's decided, what's in flight, what's blocked on whom, and the task list to done (kolu #1714 · drishti · odu). Updated by the coordinator as state changes; superseded task states are overwritten, not appended.
parents: [padi, remote-surfaces]
status: accepted
maturity: budding
updated: 2026-07-07
---

**What this is.** The working tracker for landing [remote-surfaces](remote-surfaces.html) — kolu [PR #1714](https://github.com/juspay/kolu/pull/1714) plus its paired drishti and odu PRs. The design lives in the remote-surfaces note; **this note is only state**: the task board, the open decisions, and who's blocked on whom. srid deploys the branch to his production and tests live; the agent pushes small green commits continuously. **One W4 agent owns all three repos** (kolu · drishti · odu).

## The task board

Legend: ✅ done · 🔄 in flight · ⏸ blocked · ⬜ not started

### ☀️ MORNING REPORT (2026-07-08, read this first)

**The night sequence completed, all five steps. HEAD `6ccbbf5c4`, CI green both platforms (29 checks), tree clean, production untouched throughout (PID-verified). Deploy and judge.**

1. **Held batch shipped** (your 4 decisions, as recommended): types foundation (`EntryStatus<Cause>` generic + typed skew version pair) → **P0 ownership-gated drain** (local `supervisor.pid` gate → boot fail-fast with remedy; same-lineage restart still drains; remote cross-supervisor via anti-livelock reuse; `KOLU_REMOTE_PADI_STATE_DIR` isolation lever; dev/E2E always isolated) → **Skew-UX** (CanvasFacts sum — kaval-death-over-a-failed-host is now uncompilable; cause-typed host-down card, `[Switch to local]` only; skew badge in the stationary header slot).
2. **Architecture review** (23 agents · 6 lenses · both consumers): **16 findings refuted, 1 confirmed defect + 2 minors — all fixed + pinned**. The framework substantially held your "perfect" bar; the confirmed defect (retriable `disconnected` collapsing into terminal `failed`) was found via the consumer-witness lens.
3. **Step-5 live retest caught TWO real bugs** the whole unit suite couldn't see: the finding-#1 fix masked *standing-refuse* causes as warming (`66d52190a` — semantics now: **warming = in motion · failed = needs intervention**, invariant pinned at the session layer), and the canvas loading-gate stranded failed hosts so the card never rendered (`6ccbbf5c4`). Both pinned.
4. **End-to-end validation against a REAL contested host**: sincereintent had a foreign-supervised padi — **D3 fired for real** (drain once → detect fight → fail-honest refuse), the cross-supervisor card rendered verbatim (naming the isolation remedy), `[Switch to local]` worked, and setting `KOLU_REMOTE_PADI_STATE_DIR` made the same host connect cleanly with its own padi. Screenshots captured. All happy-path legs pass: remote terminal (real ssh shell), split + split-survives-switchback, per-host code inspector, per-host daemon slot, no wrong-host RPCs.
5. **Adjudications + honest flags for your eyes**: the padiBinding AbortError → **#1719** (deterministic mechanism repro built; every consumer audited-guarded; the only quick fix would itself be an escape hatch — tracked with mechanism + hypotheses, rides the #90 gate). `/debate` skill **removed** from apm.yml (no APM ref form works for a package repo without dragging its stale closure; re-add after llm-debate bumps its kolu pin). darwin `worktreeCreate` flake → flaky-test-tracker. **Teardown lapse (agent, self-reported)**: its reap swept all non-production isolated daemon digests instead of only its own captured PIDs — production provably untouched, blast radius recoverable-by-design, fence re-affirmed.

**Remaining to merge**: you deploy + judge → drishti #90 pin-bump at final HEAD (absorbs `EntryStatus<Cause>`, the `kind`-tag, `causeFor`, zod unpin) → odu #40 pin-bump → PR body final pass → undraft → you merge.

### Overnight log (detail, chronological)


- `f8d98fb12` — apm: `srid/llm-debate` REMOVED entirely (fallback ruling). Finding: no APM ref form works for a *package* repo — the skill-path form (fine for apm.yml-less repos like `juspay/skills`) doesn't emit, the whole-repo form drags the closure whose stale kolu self-pin clobbers kolu's own skill. `/debate` (was #1622) returns in a future PR once llm-debate bumps its kolu pin past the padi rename. kolu skill verified correct.
- Held batch **IMPLEMENTED + PUSHED** (`e40d79b19`): types foundation `47591c2b8` (D1 generic + D2 typed pair, full producer chain) → P0 `24e8b6831` (local supervisor.pid gate → boot fail-fast w/ remedy; dead-predecessor still drains; remote anti-livelock surfaces typed cross-supervisor + fail-honest both axes; `KOLU_REMOTE_PADI_STATE_DIR`; dev/E2E always isolated) → Skew-UX `e40d79b19` (CanvasFacts sum w/ type-level pin; `hostDownCopy` total over causes; single [Switch to local], no Retry; skew badge in stationary slot). Local green: workspace tsc, 194+623 tests, nix build, fmt.
- Header-v2 **LANDED** (`76e22ab24`): stationary daemon slot + responsive strip, host-switch never reflows.
- ⚠ MORNING-REVIEW items: (1) `serveHostMap` gained an *optional* `causeFor` hook (additive) — my architecture review judges it against the no-knobs bar; (2) the drishti pairing for the API ripples rides the planned pin-bump of #90 (must adapt `EntryStatus<Cause>` + any serveHostMap ripple) before merge.

**Architecture review verdict (step 4, 23 agents · 6 lenses · adversarially verified)**: **16 findings refuted, ONE confirmed framework defect, 2 minors** — the framework substantially held srid's "perfect" bar. The confirmed defect (P4, found via the consumer-witness lens): `projectStatus` collapses retriable `disconnected` into terminal `failed`, so a healthy host's reconnect window renders as a steady red dead-host chip in both consumers. Fix ordered (recommended + logged): `disconnected → warming`, `failed` strictly terminal — keeps the 3-arm vocabulary, zero API ripple. Minors: tag the `EntrySession|EntryFault` sum; make the adopt-path's silent `markConnected` no-op loud. All never-defer; batching with the AbortError disposal fix → one CI → re-test.

**Step-5 retest caught a real interaction bug (ruled + fixing)**: the finding-#1 fix (disconnected→warming) MASKED standing-refuse conditions — cross-supervisor/skew ride `disconnected` (hold-degraded, no reconnect), so the Skew-UX card never rendered (eternal "Connecting…" again — caught live on sincereintent, where D3 fired FOR REAL against a genuine foreign supervisor and fail-honest refused, exactly as designed). Ruling: cause-discriminated projection (specific cause → `failed`+card; other/absent → `warming`), semantics restated: **warming = in motion · failed = needs intervention**. Invariant pinned at the session layer (refuse ⇒ specific cause). Typed alternative (SessionState splitting standing-refuse from transient) ledgered for padi-cleanup, not this PR. Card re-verified against the live repro before CI.
- **Step-5 bug #2** (`6ccbbf5c4`): the canvas loading-gate included `failed` in its ceiling, so the host-down card was UNREACHABLE for any failed host (canvas stuck "Connecting…"/"kaval didn't start" even with the chip honest). Fixed + pinned (failed entry has nothing to wait for → falls through to its cause-typed card). **END-TO-END LIVE VERIFICATION**: against sincereintent's real contested padi, the chip reads failed(anti-livelock), the canvas renders the cross-supervisor card verbatim (naming the `KOLU_REMOTE_PADI_STATE_DIR` remedy), [Switch to local] works, screenshot captured. Both step-5 bugs were invisible to unit tests — only real-remote driving surfaced them.

**The night sequence**: current slew (apm `/debate` emission, header-v2 stationary slot) → held-batch implementation (P0 ownership-gated drain + Skew-UX CanvasFacts/card/badge) → full CI green (do.md) → coordinator's architecture review (types + framework electricity, both consumers, "is this a perfect framework") → every finding fixed → CI green again → user-flow re-test (local↔sincereintent) → morning report.

### kolu #1714 — remaining work

- ✅ **drishti fleet stuck "connecting" — SOLVED: zod version drift** (not surface-map, not the adapter): drishti's lock floated to zod 4.4.3 where `z.object({input: z.void()})` newly rejects a missing key; kolu's 4.3.6 is lenient. Fixed in PR #90 (`be56501`, zod pinned, verified live — all cells deliver). The framework falsification stands vindicated.
- ✅ **Chip close-✕ visible at rest** (`c0e5d4cf4`, one line) — srid's twice-reported bug; landed standalone, deployable.
- 🔄 **PadiIdentity 4-way name collision** (srid find: wire cell · build identity · hello identity · dialog projection all named `PadiIdentity`) — rename sweep QUEUED behind the header subagent (shared files).
- 🔄 **host/ folder organization** (srid find: new host cluster flat in client/src) — mechanical move, QUEUED same reason.
- ⏭ **server/src folder organization** — deferred out of W4 per srid → already ledgered as **[padi-cleanup L27](padi-cleanup.html)** ("re-cut server/src into padi/ + a thin shell"); #1718 closed as redundant.
- ✅ **remote-host-testing APM skill** (`cae9e3c83`) — methodology distilled (remote-scope, prompts for hosts + fences); full ai::apm regen committed (0.24.0 bump per srid's trust-it ruling).
- ✅ **Header-bar redesign** (`0cd4e692e`) — Padi/Kaval sub-chips inside the active host chip (`host/HostDaemonChips.tsx`), Kolu chip compacted, gate-off consistent, Skew-UX seam left ready. 611 green. **Deploy-ready.** Open srid call: `+add-host` stays GATED (a gate-off user can't bootstrap multi-host from the `+`) — one-liner to open if wanted.
- ⏸ **P0 · ownership-gated drain** — `nix run` can never drain a padi a *different live* kolu owns (the production-incident fix): local boot-time gate + CLI fail-fast; remote via anti-livelock reuse (Decision 3); `KOLU_REMOTE_PADI_STATE_DIR` isolation (P0-b). Pins: two-local-kolu war repro → zero drain, first padi untouched; same-lineage upgrade still drains.
- ⏸ **Skew-UX** — `CanvasFacts` sum (kaval facts only on the connected arm — "kaval didn't start" over a skewed remote becomes *uncompilable*); host-down card switching on the typed `cause` (no string parsing, no inert Retry); header contract badge host-scoped (running + expected pair, Decision 2).
- ✅ **Split-pane restore** — **shipped `c6ed48c56`** (root cause: on switch-back, tiles re-key faster than their layout metadata; the default-placement effect treated them as new and PERSISTED cascade layouts over the saved split. Fix: placement classifies existing|new|DEFER — no-layout+no-record tiles wait; only record-present no-layout tiles are genuinely new; effect re-arms on records-settled). srid's A→B→A pinned red-first. Suite 609 green. **Deploy-ready.**
- ✅ **Per-host canvas** (Decision 4 = B) — **shipped `621ea2c48`**: per-host `{activeId, mruOrder, attention}` swapped on switch (in-memory survives), per-host restore latches (first visit seeds from savedSession, adopts the active tile immediately), reconcile carries the host key (no cross-host storm), pure switch fires zero RPCs. All 3 red tests flipped green (suite 605, no expected-fails). **Deploy-ready.**
- ✅ **Canvas red repro tests** — pushed (`89e601b4f`): 3 red-proofs encoding srid's exact symptoms (confirmed RED first, then `it.fails` so the suite stays green; they flip to `it()` when the Decision-4 fix lands) + the 17 by-design comments.
- ✅ **Security code-scanning alert #30** — resolved (false-positive dismissal, no code change).
- ⬜ **Final sweep once the above land**: my third delta re-review over the accumulated waves → fix loop until clean.
- ✅ **Two-platform CI — GREEN** on `cb1d43f55` (all nodes, linux + darwin). Three pre-existing reds peeled: adoption-VM seed race (bounded retry, `9a40481`), stale smoke title (`92e364b`), non-idempotent stdio teardown (`cb1d43f`).
- ✅ **zod-drift hardening** (`c751948a9`) — void-input fold carries no `input` key, independent of zod version; drishti unpins zod at its next pin-bump.
- ✅ **Split-pane bug — FIXED + browser-verified** (`0dbf28e0a`): instrumented repro (local↔sincereintent) caught it in the act — on switch-back the sub-terminal's parent metadata arrives a beat late, `isExpanded` flickers false, Corvu Resizable collapses and its `onCollapse` **persisted** `collapsed:true`, latching the split shut permanently (the 'sometimes never'). Fix: don't persist a transient programmatic collapse (guard on `hasSubs`); controlled sizes re-expand when metadata lands. Verified in-browser: split survives A→B→A. **Deploy-ready.**
- ⬜ **PR body final** — review trail (3 review layers + the audits, ~130 verified findings), invariant table, closed-in-PR issues (#1715, #1716), the two srid-ratified opportunities (#1034-gated rail unification, #1580 scopeSibling brand), the ops-gate product ruling, dev-mode pool caveat, W6 pointer.
- ⬜ **Undraft** — srid merges (never the agent).

### Paired PRs

- ✅ **odu** — [juspay/odu#40](https://github.com/juspay/odu/pull/40) (draft): 4 imports → `@kolu/surface-remote`, nix pkg rename, gates green incl a real localhost-lane e2e. Needs only a **pin-bump + re-green at kolu's final SHA**.
- ✅ **drishti** — [srid/drishti#90](https://github.com/srid/drishti/pull/90) (draft): adoption-as-deletion done (hostRegistry 204→127, core files shrink; a hand-written `hostMapRegistry.ts` adapter avoids the unused ClockableSession primitive, clockOffset 0 documented-inert); green (143 tests, nix build). EntryStatus seam = `entryStatusTone.ts` (one file for Decision 1). **srid-verified live** (deployed, works well). Remaining: pin-bump at kolu's final SHA + unpin zod once kolu's fold hardening lands. — was building (hostRegistry + admin-surface → `@kolu/surface-map`; carries all wire ripples: fold envelope, channel namespacing, SessionState sum, `Subscription.complete`, KeyCodec, `buildRemotePool`). `EntryStatus` consumption seam kept thin pending Decision 1. No screenshot evidence — srid tests drishti live himself; the deletion diff is the acceptance artifact. Then pin-bump at kolu's final SHA.

### Done (the short version — the archaeology is in git and the PR's review trail)

- ✅ The five never-defer refactors (copying type-split · HostKey sum + codec · padi HostLocation · #1715 channel namespacing · daemon-rail collapse) — both host axes and the state vocabulary honest at the type level.
- ✅ Three review layers + two audits, all findings fixed or evidence-refuted: independent C1–C7 (31) · delta re-review (14, incl. the leak-fix regression behind the "unknown" dialog) · type audit 176 types × 6 lie-modes (3 + 27 triaged) · host-axis state audit 60 containers (4 broken → the canvas cluster; 17 declared) · plus ~44 from the perfection sweeps/gauntlet era.
- ✅ srid's live catches, each fixed + pinned + skill-encoded: the dedup crash · the 30s boot hang (env-divergence + fail-open) · the eternal-Connecting spinner · the "unknown" dialog (leak-fix regression + P4 escape hatch) · the localhost phantom chip · the fresh-connect race · the skew "kaval didn't start" card (fix pending, Decisions 1–2) · blank-canvas-on-switch + unrestored splits (pending, Decision 4) · **the production drain incident** (pending, P0).
- ✅ The boot contract: manifest-discovery adopt (2s, was 30s hang) · CLI fail-fast with remedy · honest canvas arms. Verified in the agent's browser *and* srid's deploys.
- ✅ Working-mode now: **push-often** — small green commits, srid deploys + tests; agent's bar = workspace-green + red-first pins; browser proof is srid's.
- ✅ Ops-measurement gate resolved by product ruling (persisted daemons + few-host scale); one ws-kill recovery check remains a user-pass leg.

## User-testing bugs

Remote-testing bugs are tracked in **[remote-bind-parity](remote-bind-parity.html)** (the standing ledger), not here. This PR's flow-testing pass completed with **zero new bugs**: split+switch verified across repeated A→B→A cycles incl. a live remote codex session (`0dbf28e0a` solid) · Code browser fully per-host · per-host canvas tiles at saved positions.

## Standing rules (the ones that keep binding)

Never-defer (findings fixed in the PR that surfaces them) · reproduce-first diagnosis · tree-verification over self-reports · one editor between fork waves · isolated state roots + teardown-reap for every spawned daemon · production/zest untouchable · srid merges everything.
