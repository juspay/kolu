---
title: Electricity — kolu's hidden infrastructure, and the method that finds it
description: "Not just a tracker — a generative method (Löwy volatility-based decomposition) for kolu's electricities; three families, a from-zero procedure that re-derives surface, and this pass's crop of twelve newly identified candidates with honest extract/defer/reject verdicts."
kind: analysis
maturity: evergreen
updated: 2026-06-05
---

import Pill from "../../components/Pill.astro";
import PrLink from "../../components/PrLink.astro";
import Callout from "../../components/Callout.astro";
import Cite from "../../components/Cite.astro";

<Callout kind="good" title="What counts as electricity">
Löwy's receptacle — hide the volatility behind a socket you plug into. Three tests at once: ① **domain-agnostic** — no terminal/canvas/git/xterm leaks into the interface; ② **hides a hard volatility** — transport, reconnect, GPU-context loss, persistence — not a 3-line `===`; ③ **graduates** — ships as `@kolu/*` and a *different* app plugs in. The proof of ③ is real, not aspirational. [`@kolu/surface`](https://kolu.dev/blog/surface-framework/) is **kolu's own** client↔server transport first — and it has since picked up a **second consumer** in [srid/drishti](https://github.com/srid/drishti) (a fleet process-monitor; browser ↔ Bun ↔ ssh-agent) that vendors it from this repo and declares its own per-host schema with none of kolu's domain. The second consumer isn't the *point* of the extraction — it's the *proof* it was real electricity: surface never noticed the domain changed.
</Callout>

<Callout kind="danger" title="How a session mis-identifies electricity — three traps, learned the hard way">
The live example: asked to add back/forward to the Code-tab preview, a session proposed extracting a **history stack**. Wrong three ways, each caught only by re-asking the test above — and the [solid-browser](solid-browser) plan is the corrected end-to-end.

- **Naming an organ, not the concept.** A history stack is a *transformer* — a working part *inside* a larger thing. The electricity was the **browser** (location + history + link-nav + render-host); history is one chamber of it. Test: *is the name self-describing standalone?* "history" → history **of what?**; "browser" → obvious. If you must append "…of X" to explain the name, you've named a part, not the receptacle.
- **A module in `client/src/` is not electricity.** Location *is* structure (Hickey): a generic-looking helper living inside the app is **part of the app**, however agnostic the code reads. Electricity is its **own package**, the dependency arrow pointing *out* — `client → @kolu/x`, never back. (`createSharedRoot` sits in `client/src` marked "publish?" — a leaf *awaiting* graduation, **not** a precedent for calling in-app modules electricity.)
- **Agnostic ≠ electricity — apply test ② honestly.** A clean, reusable, agnostic primitive that hides only a *bounded algorithm* (a stack, a debounce, an `===`) is a **leaf** (`nonempty`/`html-escape`-tier), not a receptacle. "I found a tidy generic module" is the feeling that *precedes* the mistake. Ask what **hard** volatility it hides — transport, GPU-context loss, sandboxing, reconnect — before reaching for `@kolu/`.
</Callout>

> "Decompose based on volatility." Functional decomposition splits a system by what it *does*; volatility-based decomposition isolates each area likely to *change* behind a stable interface — the way a body keeps blood-pressure, pulse, and salinity volatility "encapsulated behind the service called the heart." An *electricity*, here, is one such volatility pulled out into its own package. — Juval Löwy, [Volatility-Based Decomposition](https://www.informit.com/articles/article.aspx?p=2995357&seqNum=2) (excerpt from *Righting Software*).

This note does two jobs at once, and they pull in opposite directions. As an **inventory** it tracks what kolu has pulled out from under the app (the table). As a **method** it must *generate* those extractions, not merely record them — because an inventory that only recognises electricity in hindsight is a museum, not a tool. So the real test is **forward** (*"[does it generate, or only recognise?](#the-real-test-does-the-method-generate-or-only-recognise)"*): could it have produced `surface` from *"a client and a server talk over a wire"* before surface existed?

The lens that does both jobs is **three families of volatility** — every electricity below is exactly one of them, and the un-named ones hide in the *seams* between:

- **A · Boundary** — a live value across a place you don't control (network · process · host · origin · *and time*).
- **B · Invariant** — a rule run identically at every call-site, silent when forgotten.
- **C · Appliance** — a typed blob turned into a safe, addressable view.

Read the table as the **inventory**; read the sections after it as the **method**.

## The electricities

The **Family** column (A · Boundary, B · Invariant, C · Appliance, plus *leaf* and *comp*osition) is the spine made concrete — every row classified by [the three families below](#what-the-table-is-really-a-list-of--one-contract-four-boundaries).

| Electricity | Fam | Owns (volatility) | PR | Progress |
| --- | --- | --- | --- | --- |
| **@kolu/surface** | A | Live client↔server state — `Cell`/`Collection`/`Stream`/`Event` over oRPC, reconnect, fine-grained reconcile. | <PrLink pr={805} /> | <Pill variant="done">done</Pill> · 2nd consumer: [drishti](https://github.com/srid/drishti) |
| **[@kolu/surface-app](./surface-app.html)** | A | The app shell for surface apps — fresh delivery + server/build identity + the connection/update lifecycle model + desktop-install. *(the **Manager** half of the [Manager-vs-Engine](#the-lifecycle-manager-vs-the-transport-engine--is-surface-two-electricities--candidate) question.)* | <PrLink pr={1154} /> | <Pill variant="done">done</Pill> · 2nd consumer: [drishti](https://github.com/srid/drishti) |
| **@kolu/surface-nix-host** | A | `HostSession` — `nix copy` a closure to a host, realise, run `--stdio` over ssh. | <PrLink pr={984} /> | <Pill variant="done">done</Pill> · also in drishti |
| **@kolu/artifact-sdk** | A+C | Two volatilities under one roof — the agnostic **anchoring core** (C: W3C quote re-find; speaks only `Range`/`Document`/`ShadowRoot`) *and* the **sandboxed-iframe parent↔frame bridge** (A: opaque-origin postMessage, now carrying comments + in-frame navigation + back/forward input). See the re-eval note below. | <PrLink pr={922} /> | <Pill variant="done">done</Pill> <Pill variant="run">re-eval</Pill> |
| **@kolu/transcript-core** + **transcript-html** | C | Transcript model + rendering. | <PrLink pr={744} /> | <Pill variant="done">done</Pill> |
| **@kolu/solid-pierre** | C | SolidJS adapters for Pierre tree/diff. | <PrLink pr={823} /> | <Pill variant="done">done</Pill> |
| **@kolu/solid-markdown** | C | Untrusted-markdown → safe HTML — the marked → DOMPurify → Shiki pipeline + href policy. The *sanitiser* is the volatility (XSS surface moves with every CVE). | — | <Pill variant="done">done</Pill> · 2nd consumer: solid-fileview |
| **@kolu/pty-host** | A | PTY process lifecycle — `node-pty` child, `@xterm/headless` mirror, VT-derived taps (cwd/title/command/foreground). Agnostic to *what* runs in the shell; its contract schema still lives in `kolu-common` (③-caveat below). | — | <Pill variant="done">done</Pill> <Pill variant="run">③-caveat</Pill> |
| **persistedPref** | B | Validated localStorage (validate-on-read; closed the zoom-`NaN` + maximize bugs). Distinct vault from server [`migrate-on-load`](#migrate-on-load--schema-evolution-at-rest-family-b-server--candidate). | <PrLink pr={1089} /> | <Pill variant="done">done</Pill> |
| **@kolu/log** · **@kolu/html-escape** · **@kolu/url-shape** · **nonempty** · **memorable-names** · **terminal-themes** | leaf | Zero/near-zero-dep leaves every package imports without dragging the domain tree. `url-shape` (`hasOwnScheme`) is the textbook leaf — *carved out so solid-markdown's href policy and solid-browser's resolver share it with no arrow between them*. `terminal-themes` (okLab nearest-match over the iTerm2 catalog) is a data+algorithm leaf, not a receptacle. | <PrLink pr={1089} /> | <Pill variant="done">done</Pill> |
| **@kolu/solid-fileview** | comp | Source↔Rendered toggle + renderer dispatch. Honest verdict: a **composition**, not a receptacle — it *arranges* solid-markdown/solid-pierre, hides no hard volatility. Shipped as `@kolu/*` for reuse, but leaf-tier on test ②. | — | <Pill variant="bad">leaf (mislabeled by package scope)</Pill> |
| **dom/** + injectable `isMac` | B | `walkShadowRoots` relocated to a neutral wall; keybind core no longer reads the UA singleton. *(a face of [`createOwnedResource`](#createownedresource--reactive-owner-correctness-family-b--extract-now)'s owner-correctness axis.)* | <PrLink pr={1089} /> | <Pill variant="done">done</Pill> |
| **createSharedRoot** | B | Lazy app-scoped reactive singleton (7 consumers). 100% agnostic SolidJS — the *inverse* face of `createOwnedResource`. | <PrLink pr={974} /> | <Pill variant="done">done</Pill> <Pill variant="run">publish?</Pill> |
| **createOwnedResource** | B | **(new this pass — extract-now)** Reactive-owner correctness across `await`; registers cleanup at attach so `onCleanup` can't no-op. Absorbs `createSharedRoot` + `dom/`. [Crop §](#createownedresource--reactive-owner-correctness-family-b--extract-now). | — | <Pill variant="fix">to build</Pill> |
| **@kolu/solid-xterm** (createSolidXterm) | C | xterm.js lifecycle — 8-addon orchestration, WebGL single-owner + context-loss recovery, FitAddon resize, owner-correct async dispose across `await`. | <PrLink pr={1116} /> (plan) | <Pill variant="fix">to build</Pill> |
| **@kolu/solid-browser** | comp | The Code tab *is* a browser — location/address model, back-forward history, cross-content-type link interception, GitHub-relative resolution, sandboxed-iframe lifecycle. A **composition** (A link-intercept + C appliances + [addressing](#the-seam-the-families-share-addressing-a-reference-that-outlives-what-it-points-at)), not one receptacle. git is an injected resolver; the renderer packages plug in. [Plan](solid-browser). | <PrLink pr={1191} /> | <Pill variant="run">phase 1</Pill> primitives shipped · createBrowser+history (P2), ③ proof-pending |

## What the table is really a list of — one contract, four boundaries

Step back from the package names and most of the "transport" rows collapse into **one** volatility wearing a different boundary each time:

> A reactive value whose *source* sits on the far side of a boundary you don't control — where the universal contract for surviving the boundary is **snapshot, then deltas, then liveness** (reconnect · re-identify · resume).

| Boundary you can't reach across | What *changes* (the volatility) | Where kolu puts the socket |
| --- | --- | --- |
| **Network** (client ↔ server) | the link drops, reorders, reconnects | `@kolu/surface` — `Cell`/`Collection`/`Stream` over a retrying link |
| **Process** (server ↔ shell) | the PTY dies, restarts, flips build-id | `@kolu/pty-host` — *served over surface*; adds process-lifecycle on top |
| **Host** (local ↔ remote machine) | the box is unreachable until a closure is realised | `@kolu/surface-nix-host` — realises a daemon, then speaks *surface over stdio* |
| **Origin** (parent ↔ opaque-origin iframe) | the frame is sandboxed; `event.source` is the only trust | `artifact-sdk`'s bridge — hand-rolls snapshot/deltas/liveness *again* |

Read down the last column: three of the four are *surface, or surface stretched further*. The fourth — the sandboxed-frame bridge — is the **only** boundary surface's links can't cross (opaque origin = no shared transport, postMessage only), so it re-implements the same snapshot/deltas/liveness contract **by hand**. That hand-rolling isn't a coincidence; it's the *tell*. The real electricity was never "surface the package" — it is the **`Channel` contract itself (snapshot → deltas → liveness), made independent of transport.** Surface owns it for websocket/stdio/in-process links; the bridge is simply where it leaked, because no `postMessageLink` exists yet.

This is the cut the file/module view hides: the same socket is wired four times because four *boundaries* are volatile, not because four *modules* exist. It also sharpens the artifact-sdk re-eval (below): the live question isn't "anchoring vs bridge" — it's *"should the bridge be a **surface link** rather than a private transport?"* — i.e. does the origin boundary deserve a socket of the **same shape** as the network boundary?

## The table misses an entire *kind*: invariant electricities

Every row above is a **boundary** electricity — a socket over a place where bytes cross. But the codebase hides a second family the package-shaped view is blind to, because its members aren't packages — they're *rules enforced by remembering*:

> An **invariant electricity** is a correctness rule run as the same cassette of plumbing at N call-sites, where forgetting it produces a **silent** bug, not a loud one. The volatility isn't a transport — it's that *the set of call-sites keeps growing, and every new one is a latent failure.* (This is `.agency/lowy.md`'s Layer-0 prompt: *the pattern across N call-sites is itself the artifact.*)

| Invariant run everywhere by hand | The silent bug when you forget | Enforce-by-construction receptacle |
| --- | --- | --- |
| **snapshot → *then* deltas** on every streaming handler | client renders deltas onto an empty base; a reconnect shows nothing | **Already enforce-by-construction** — `Cell`/`Collection`/`Stream` handlers (and `pollOnEvent`) yield the snapshot *inside the framework*, where domain code can't skip it (<Cite file="packages/surface/src/server.ts" lines="146-149" />, <Cite file="packages/surface/src/server.ts" lines="314-335" />). The lone hand-roll left is the raw-oRPC `terminal.attach` escape hatch ([crop row below](#newly-identified-this-pass--the-engine-turned-on)). |
| **owner captured *before* `await`** in any async setup | `onCleanup` lands on a null owner → silent no-op → the subsystem leaks (~900 KB/component; #598/#600/#591) | a `withOwner`-shaped resource: cleanup registers at *attach*, not via a manual `detach()` handle |
| **validate on *read*** from any persisted store | a stale / `NaN` / `"false"` value loads as truthy and corrupts UI state | `persistedPref` (client) · the `state.ts` migration ladder (server) — *the same invariant in two unconnected homes* |
| **silence the *expected* abort, log the rest** | unmount/navigation spews fake errors that bury the real ones | `isExpectedCleanupError` — one predicate every stream consumer must run |

These never surface in a package audit because their *correct* form **deletes code rather than adding a folder**. That is the signature of family B: the receptacle is a *smaller surface*, not a new noun. Where family A asks *"what boundary is volatile?"*, family B asks *"what rule am I trusting myself to remember?"* — and both clear Löwy's bar (a volatility encapsulated so the blast radius of a change stays contained). Note the connection: snapshot→deltas appears in **both** families — surface (A) defines the contract *and* (correcting an earlier draft of this note) already **enforces** it by construction for every `Cell`/`Collection`/`Stream`. The family-B residue is now a single escape hatch — the raw-oRPC `terminal.attach` — which is exactly the kind of half-built remainder the crop below is for. The two families meet where an electricity is *almost* finished.

## A third family: appliance electricities (blob → safe view)

Families A and B miss the *largest* cluster of rows. solid-markdown, solid-pierre, transcript-html, the planned solid-xterm, the anchoring core — none is a boundary, none is an invariant. They are **appliances**: each takes a *typed blob* and returns a *safe, addressable, disposable view*, hiding the volatility of the rendering stack itself.

> An **appliance electricity** absorbs two churning things behind a stable `(blob, host) → view` interface: (1) the *rendering tech* — which parser, which sanitiser after the next CVE, GPU vs canvas, the next xterm major — and (2) the *growing set of content-types*. The domain ("render *this file* in the Code tab") is the appliance that plugs in; the receptacle never learns it.

| Appliance | Blob in | Volatility absorbed |
| --- | --- | --- |
| `@kolu/solid-markdown` | markdown text | marked → DOMPurify → Shiki; the *sanitiser* moves with every XSS CVE |
| `@kolu/solid-pierre` | file / unified diff | Pierre tree/diff internals, theme churn |
| `@kolu/solid-xterm` *(to build)* | PTY byte stream | xterm majors, 8-addon orchestration, WebGL context-loss |
| `@kolu/transcript-core`+`-html` | agent transcript | transcript schema + render |
| anchoring core *(in artifact-sdk)* | a `Range` in a rendered doc | W3C quote re-find as the DOM mutates |

Now the whole table has a shape — every electricity is exactly one of three kinds:

- **A · Boundary** — a live value across a place you don't control. *(surface · pty-host · nix-host · the bridge transport)*
- **B · Invariant** — a rule run the same way at every call-site, silent when forgotten. *(snapshot→deltas · owner-correctness · validate-on-read · expected-abort)*
- **C · Appliance** — a typed blob turned into a safe, addressable view. *(solid-markdown · solid-pierre · solid-xterm · transcript · anchoring)*

And the two **compositions** on the table — `solid-browser` and `solid-fileview` — are revealed for what they are: *not electricities but wiring* that routes a blob (A: link interception) to the right appliance (C) and owns *addressing into* the result (location/history). That is exactly why solid-fileview tested as a leaf in the table above: **a composition of electricities is not itself one.** It also tells you where to look for the *next* one — the seams *between* families, where addressing (anchoring, location) spans A and C, are where the un-named infrastructure hides.

## The seam the families share: addressing (a reference that outlives what it points at)

Walk into that A×C seam and a fourth thing is hiding — not a package, a *capability kolu builds twice and composes by hand*:

> **Addressing** — name a point in content and have the name *survive the content changing under it.* A full address has two levels: **which view** (location) × **where inside it** (anchor). A comment is just `(location, anchor, payload)`.

kolu already owns both halves, in different packages:

- **which view** → solid-browser's location/history (survives *navigation*: the view is replaced)
- **where inside** → artifact-sdk's anchoring core (survives *re-render*: the DOM mutates; re-find by W3C quote + context)

Nothing owns the *pair*. "Jump to this comment after a reload," "deep-link a diff line," "re-anchor a selection across a re-render *and* a navigation" each re-compose the two halves at the call-site — a family-B smell (a rule kept by remembering) riding on top of two family-A/C receptacles.

And here is the step that earns the word *infrastructure*. An anchor is not a string — it is **Hickey's identity over time.** The W3C quote is a stable *identity*; the concrete `Range` it resolves to is a *value* that changes every epoch as the document mutates. Re-finding that Range on each render is *exactly* surface re-delivering a `Cell` value after a delta — the same snapshot-then-deltas contract, except the boundary it crosses is **time**, not the network. Which means family A was never really about *spatial* boundaries: it is **"a live identity whose value is re-resolved across a boundary you don't control"** — and network, process, origin, *and time* are all just values of "which boundary." Addressing is family A with the boundary dial turned to **time**.

<Callout kind="warn" title="Over-merge check (the note's own trap ①)">
Is "addressing" one receptacle, or a topic-bundle of location + anchor smuggled together? Honest answer: **undecided — and that's the finding.** The two halves have genuinely *different* volatilities (navigation vs DOM-mutation) and may well stay two sockets. What is real and demonstrable is that the *pair is used together and owned by no one.* Logged as the strongest un-named candidate at the A×C seam — flagged, **not** declared electricity. (The discipline that catches a forced merge is the same one in the three-traps callout at the top.)
</Callout>

## The real test: does the method *generate*, or only *recognise*?

A tracker recognises electricity after it ships; a method *predicts* it. Surface sits in the "done" column — but surface only *proves* the method if the method would have **produced** it from "a client and a server talk over a wire," before anyone wrote a line. So the honest test of this whole note is **forward**, not backward: run the procedure on kolu *as it stands today* and write down the electricities it predicts that don't exist yet. If they later ship and a second consumer plugs in, the method generated them. If the predicted axis never actually moves, the method hallucinated one (see falsification, below). Three standing bets, in falsifiable form:

| The method predicts… | Derived from | Falsified if… |
| --- | --- | --- |
| **`postMessageLink`** — the iframe bridge becomes a *surface link*, so the origin boundary gets the same snapshot/deltas/liveness socket as the network boundary | family A: four boundaries, one contract; the bridge is the only hand-rolled one | the opaque-origin boundary genuinely needs a *different* contract than the network — it doesn't; both are drop-prone async channels |
| **`createOwnedResource`** — a `withOwner`-shaped primitive that registers cleanup at attach, so `onCleanup` can't land on a null owner | family B: owner-before-await, already seen 3× (#598/#600/#591) | the SolidJS owner model changes so the await-gap closes upstream, or the call-site set stops growing |
| **`@kolu/solid-xterm`** (already tabled) | the xterm lifecycle braid | a second SolidJS app never embeds xterm (③ never arrives) |

### The procedure — run it on any app, before any code

Six steps. Inputs are *interactions*, never features. The falsifiable claim: fed only *"a client and a server talk over a wire,"* step 6 must **emit surface**. If it can't, the method is wrong — and this note is then a catalog of packages wearing a method's clothes.

1. **Frame the interaction, not the feature.** Reject requirements-as-design (Löwy: *"any attempt at designing against the requirements will always guarantee pain, because when the requirements change, so will your design"*). The only legal input here: *a client and a server talk over a wire.*
2. **Sweep the two axes of change** (both *verbatim* Löwy): **① same customer over time** — what changes for *this* user as use evolves? (request/response today → server-pushed live state → deltas → reconnect-with-resume). **② all customers at one instant** — are they all using the wire the same way? (LAN vs flaky mobile vs proxies that kill long-lived sockets → poll / SSE / WS).
3. **Independence check.** Are ① and ② the *same* change in disguise? No: *push semantics over time* ≠ *transport across customers*. Two real axes → a real vault. If they collapse to one, you've found functional decomposition wearing a domain hat — **stop.**
4. **Screen the survivor** (three Löwy filters): *variable, not volatile?* — an if-else/flag can't span poll→SSE→WS, so it survives. *Solution masquerading as requirement?* — "add WebSockets" is the **Cooking**, not the **Feeding**; climb to the parent: *live-state delivery.* *Open-ended & expensive to contain unless encapsulated?* — retrofitting a transport swap across every feature is the loose grenade. Survives.
5. **Name it for the axis, not the tech.** `WebSocketClient` names an *activity* (it fractures on the first transport swap); **`LiveStateTransport` / surface** names the *volatility* (it doesn't). The naming test is the cheap proxy for the sweep test in [§the metaphor, made generative](#the-metaphor-made-generative-axes-in-not-decoration).
6. **The interface is the receptacle.** Verbs that don't change when the volatility behind them does — *snapshot → deltas → liveness*; a signal-shaped handle; no `AsyncIterable` in components. Domain-agnostic, a different app can plug in → **you have specified `@kolu/surface` without having written any kolu.**

Step 2 is where functional decomposition dies: the instant you write *what changes* instead of *what it does*, surface stops being a design choice and becomes **inevitable**. (Honest footnote, [§hindsight](#foresight-names-the-vault-pain-funds-it): the method names *where* the vault is and *what its verbs are* before code exists — but historically it's recurring same-shape **pain** that tells you it's time to pour it. Foresight names the vault; pain funds it.)

### Foresight names the vault; pain funds it

A caution against over-selling the method, kept honest by history. Prediction-by-method almost never *suffices alone* — recognition-after-pain is the historical rule. McIlroy's 1964 "garden hose" memo genuinely predicted Unix pipes ~nine years early (*"we should have some ways of coupling programs like garden hose"*) and then **sat unbuilt** until accumulated pain met Thompson's overnight rewrite. So the defensible claim is narrow and strong: the procedure tells you *where* the receptacle will sit and *what its verbs are* before any code exists — but **same-shape pain (or a contractually-certain axis) is the co-factor that says it's time to pour.** This is why the crop below pairs every *extract-now* with **dated, recurring** evidence — and why a clean axis whose volatility hasn't fired yet (`vt-events`) is filed *defer*, not *build*.

## The metaphor, made generative: axes in, not decoration

The reason it's called *electricity* isn't decoration — it's **the procedure read backwards.** Löwy lists the power volatilities first — *"power can be AC or DC; 110 volts or 220 volts; … 50 hertz or 60 hertz; produced by solar panels …, a generator …, or plain grid connectivity"* — and *then* the receptacle is whatever interface stays **bit-identical** across every value on every axis. The volatility list is the input; the socket's geometry is the output (its fixed point). Map the axes straight onto a software seam:

| Electricity axis | …is software's | surface's value of it |
| --- | --- | --- |
| Voltage (110/220) | wire format / payload shape | snapshot vs delta encoding |
| Frequency (50/60) | transport / push cadence | poll → SSE → WS → WebTransport |
| Source (solar/grid) | provider / origin | which server process authors the state |
| AC vs DC | ordering / consistency model | update sequencing, resume semantics |
| Phase (1/3) | locality / deployment | LAN vs mobile vs proxied client |

**The sweep test** turns taste into pass/fail: vary each axis end-to-end — grid→solar, poll→WS — and confirm the interface doesn't change *shape* at any point. "Needs only minor changes" means it is **still leaking**; a receptacle doesn't change *at all*. **The naming test** is the cheap proxy: if the best name you can find is a domain noun or an activity gerund (`WebSocketClient`, `BillingManager`), you grouped *function* and the interface will fracture on the first change you forgot to list. *(Enumerate-then-sweep is our generative distillation of the analogy — sharper than Löwy's literal text, which frames discovery as requirements "sweat," not a mechanical catalog.)*

**Contract vs carrier — the part that outlasts the metaphor.** The electrical *contract* (sine wave, nominal voltage, pin spacing) is universal; the *plug* fragmented into ~15 national shapes only because grids were poured before any standard had teeth. In software: spend the enumeration effort on the **contract** — the atomic verbs and wire invariants (snapshot→deltas) — and treat the **carrier** (the library, the framework convention) as disposable. surface is a contract; oRPC is a carrier. Confuse the two and you ship a `WebSocketClient` and rewrite every consumer on the next transport.

## Newly identified this pass — the engine, turned on

A method is only worth the name if running it *produces* electricities you hadn't tabled. So this section is the output of pointing the [procedure](#the-procedure--run-it-on-any-app-before-any-code) at kolu **as it stands today** — each candidate stated as its *axis* (what changes), the tell-tale sentence *"every time ___ changes, we hand-edit ___,"* the kolu evidence (`file:line` / issue), and an honest verdict. The honesty gate: a generator that only ever says "extract" is a tracker with extra steps, so the crop below **defers** some and **rejects** two — and one *corrects a claim made earlier in this very note*.

| # | Candidate | Fam | Verdict | Axis — *what changes* |
| --- | --- | --- | --- | --- |
| 1 | [`createOwnedResource`](#createownedresource--reactive-owner-correctness-family-b--extract-now) | B | <Pill variant="fix">extract-now</Pill> | the reactive scope owning a subscription across `await` |
| 2 | [`terminal.attach` → Stream](#terminalattach--surfaces-last-hand-rolled-snapshot--narrow) | B | <Pill variant="run">narrow</Pill> | surface's last hand-rolled snapshot |
| 3 | [`migrate-on-load`](#migrate-on-load--schema-evolution-at-rest-family-b-server--candidate) | B | <Pill variant="run">candidate</Pill> | persisted shape vs an evolving schema |
| 4 | [Manager vs Engine](#the-lifecycle-manager-vs-the-transport-engine--is-surface-two-electricities--candidate) | A | <Pill variant="run">candidate</Pill> | lifecycle *sequence* vs transport *activity* |
| 5 | [`view → artifact`](#view--portable-artifact--export-family-c-adjacent--candidate--defer) | C | <Pill variant="run">defer</Pill> | export format + sink set |
| 6 | [`vt-events`](#vt-events--escape-sequences--typed-shell-events--candidate--defer) | A | <Pill variant="run">defer</Pill> | escape-sequence → typed event mapping |
| 7 | [`chord-arbitration`](#chord-arbitration--who-owns-a-keychord-when-input-consumers-stack-family-b--candidate) | B | <Pill variant="run">candidate</Pill> | who owns a chord when consumers stack |
| 8 | [`capability-gate`](#capability-gate--degrade-when-a-browser-api-is-absent-family-b--extract-now) | B | <Pill variant="fix">extract-now</Pill> | degrade when a browser API is absent |
| 9 | [`terminal-probes`](#terminal-probes--read-volatile-library-internals-behind-null-safe-thunks-family-b--extract-now--partly-built) | B | <Pill variant="run">extract-now·partly</Pill> | reading volatile library internals safely |
| 10 | [`refcounted-fan-out`](#refcounted-fan-out--one-source--n-subscribers-refcount--backpressure--extract-now) | B | <Pill variant="fix">extract-now</Pill> | one source → N subscribers + backpressure |
| 11 | [`freshness`](#freshness--running-code-identity-vs-deployed-code-identity--candidate--partly-in-surface-app) | A | <Pill variant="run">candidate</Pill> | running-code id vs deployed-code id |
| 12 | [`ambient-injection`](#ambient-injection--deterministic-time--platform--rng-as-injected-sources--candidate) | B | <Pill variant="run">candidate</Pill> | injected time / platform / RNG |
| · | [addressing](#the-seam-the-families-share-addressing-a-reference-that-outlives-what-it-points-at) | A×C | <Pill variant="run">flagged</Pill> | a reference that outlives its target |
| ✗ | [identity / trust](#the-reject-pile--candidates-the-engine-refused) | — | <Pill variant="bad">reject</Pill> | who's allowed (single-user-local) |
| ✗ | [client fan-out](#the-reject-pile--candidates-the-engine-refused) | — | <Pill variant="bad">reject</Pill> | N tabs share one subscription |

Tally: **4 extract-now**, 1 narrow, **5 candidates**, 2 defer, 1 flagged, 2 reject. Nine of the twelve are **Family B** — the invariant kind a package audit is structurally blind to, which is the whole point. Detail rows follow.

**The ③ audit (the hardest gate — "a second consumer of *different shape*").** Rank the crop by how real its second consumer is, not how clean its code is. **Already proven internally:** `refcounted-fan-out` is strongest — *four* consumers of different shape (PTY/WAL/dir/git) pull the same lifecycle today; `freshness` is real too — drishti already reuses the `isStale`/`buildInfo` seam with its own identity. **Generic-but-credible:** `createOwnedResource`, `capability-gate`, `migrate-on-load` — any SolidJS-async / web / versioned-state app wants each unchanged, but no *concrete* second consumer has pulled them yet. **Kolu-shaped, ③ pending:** `chord-arbitration`, `terminal-probes`, `ambient-injection`, `vt-events` — real axes whose only consumer is kolu, so they wait for a second multiplexer / terminal-app / injected-clock to *prove* (not assume) the boundary. This ranking, not the prettiness of the interface, is what orders the build queue.

### `createOwnedResource` — reactive-owner correctness (Family B) · <Pill variant="fix">extract-now</Pill>

The strongest new find, and a textbook **invariant electricity**.

- **Axis (what changes):** the reactive *scope* that owns a subscription/effect/resource and guarantees its disposal. SolidJS's global `Owner` is `null` across any `await`, so an `onCleanup` registered after an await silently no-ops — and the subsystem leaks with *no runtime error*, visible only in a heap retainer walk.
- **The sentence:** *every time a setup grows an `await`, we hand-edit the `getOwner()` → `runWithOwner()` dance — or forget it and leak ~900 KB.*
- **Evidence (dated, recurring — not anticipated):** the manual dance at <Cite file="packages/client/src/terminal/Terminal.tsx" lines="463-471" />; the requirement spelled out at <Cite file="packages/client/src/scrollLock.ts" lines="63-64" />; and `createSharedRoot` is the *same axis solved inversely* (an app-scope owner that outlives any component). Leak history: #591 (190+ orphaned xterm trees), #598, #600, #606.
- **Why now:** three independent leaks already paid for it; the receptacle — register cleanup at *attach*, hold the owner, dispose explicitly — *deletes* the ceremony at every call-site (the family-B signature). It absorbs `createSharedRoot` as one configuration of itself.
- **③ prospect:** any SolidJS app that does async setup wants this unchanged — the cleanest second-consumer story of the whole crop.

This promotes the table's `dom/` + `createSharedRoot` rows from "agnostic helpers awaiting graduation" to **two faces of one un-built receptacle.**

### `terminal.attach` — surface's last hand-rolled snapshot · <Pill variant="run">narrow</Pill>

Found by *correcting this note*, not by adding to it.

- **Axis:** snapshot-then-deltas (Family B), already owned by surface for every `Cell`/`Collection`/`Stream`.
- **The sentence:** *every time we add a raw-oRPC stream outside surface, we hand-write `if (snapshot) yield snapshot; for await (…) yield delta` again.*
- **Evidence:** the one surviving hand-roll lives at <Cite file="packages/server/src/router.ts" lines="144-151" /> (`terminal.attach`) — a raw oRPC procedure, *not* a surface-managed descriptor, so the framework's guarantee doesn't reach it.
- **Verdict — narrow:** not a new package; the move is to model `terminal.attach` as a surface `Stream` (or route PTY bytes through the existing `pollOnEvent` shape) so the invariant becomes 100% enforce-by-construction and the escape hatch closes. The electricity here is *already built* — this is the last meter of wire.

### `migrate-on-load` — schema evolution at rest (Family B, server) · <Pill variant="run">candidate</Pill>

A real receptacle hiding in plain sight inside `state.ts` — un-named, un-extracted.

- **Axis:** the shape of state *at rest* versus an evolving schema. Persisted JSON written by build N must load cleanly into build N+k.
- **The sentence:** *every time a persisted field changes shape, we hand-write a migration rung (and bump `SCHEMA_VERSION`).*
- **Evidence:** the aggregate schema at <Cite file="packages/server/src/state.ts" lines="88-102" />, the **23-rung migration ladder** (`1.1.0 → 1.23.0`) at <Cite file="packages/server/src/state.ts" lines="128-442" />, boot-time validation that refuses to start on mismatch. This is a coherent *migrate-on-load* receptacle that domain modules read through without ever touching the on-disk layout.
- **③ prospect:** any app persisting versioned state wants exactly this (Conf gives the file; the *migration ladder + validate-at-boot + degrade rule* is the electricity).

<Callout kind="warn" title="…and the trap it would spring: persistedPref is NOT the same electricity">
The seductive move is "unify client `persistedPref` and server `state.ts` into one `@kolu/validated-store`." **Run the independence check — they fail it.** Different *clocks*: the client validates **per-entry, on every read, with no migration** (a stale value just degrades to its fallback); the server validates **once at boot, on an aggregate, through a migration ladder** (<Cite file="packages/client/src/persistedPref.ts" lines="56-68" /> vs <Cite file="packages/server/src/state.ts" lines="128-442" />). And the wire-validation clock (oRPC checks an incoming patch) is a *third*, independent one. Same *shape* (opaque → validate → degrade) ≠ same *axis*: they change for different reasons. Merging them is the **coincidental-duplication trap** (legs-on-a-dog = legs-on-a-chair = 4). So: client `persistedPref` = **done** (the receptacle, fully adopted — five call-sites, no stragglers); server `migrate-on-load` = a **separate** candidate. Two vaults, not one.
</Callout>

### The lifecycle **Manager** vs the transport **Engine** — is surface *two* electricities? · <Pill variant="run">candidate</Pill>

The boldest structural claim of the pass: apply Löwy's **sequence-vs-activity** split *inside* the headline electricity.

- **Axis:** the *order* of the connection lifecycle — connect → snapshot → deltas → reconnect-with-resume → teardown — changes **independently** of the *mechanism* that carries each step (websocket / stdio / postMessage link). Two axes ⇒ potentially two vaults: a **Manager** owning the sequence, an **Engine** owning the transport.
- **The sentence:** *every time the reconnect/restart story changes, we edit lifecycle logic that is wired straight to the socket.*
- **Evidence — half-separated:** the *Engine* is already a clean seam — `websocket` / `stdio` / `direct` links all return the same client interface (<Cite file="packages/surface/src/links/_wire.ts" lines="1-9" />). But the *Manager* is still braided to it: `createServerLifecycle({ ws, probe })` derives connecting/connected/reconnected/**restarted** by listening to the raw socket's `open`/`close` (<Cite file="packages/surface-app/src/solid/index.ts" lines="87-103" />), and the retry plugin re-snapshots on reconnect *separately* — two uncoordinated responders to one socket event.
- **Verdict — candidate (don't split yet):** the seam is real and named, but the axis hasn't *moved* enough to force the split (one transport family in anger so far). Flagged as the place surface will *cleave* if a second transport's lifecycle (e.g. stdio resume semantics) diverges from websocket's. The `postMessageLink` [forward bet](#the-real-test-does-the-method-generate-or-only-recognise) is the most likely trigger: a fourth boundary whose liveness differs is what turns one electricity into a Manager + an Engine.

### `view → portable artifact` — export (Family C-adjacent) · <Pill variant="run">candidate · defer</Pill>

The crop's clearest *"real axis, wrong time"* — and a lesson in not extracting on momentum.

- **Axis:** turn a live, rendered view into a standalone exportable artifact; the *set of formats and sinks* churns (HTML, PDF, PNG, JSON, WebM…).
- **The sentence:** *every time we add an export, we re-write `gather view state → serialize → package → hand to a sink`.*
- **Evidence — the shape is real and growing:** five formats in seven weeks — `exportSessionAsHtml`, `exportScrollbackAsPdf`, `screenshotTerminal` (PNG→clipboard), `sessionTransfer` (JSON), `recorder/` (WebM); the leaf `download.ts` (`triggerDownload`) is *already* shared by two of them.
- **Why defer (the falsifier bites):** each export *welds* its own source to its own sink with **different** readiness guards (popup-block fallback vs hard-error vs clipboard-absent) — the parameters-and-conditionals smell that says *the cases aren't the same thing yet*. No second consumer of a different shape. Verdict: **revisit at the 6th format or a non-UI (CLI) consumer**; until then it's a folder waiting to become wrong. The honest call here is *not* to extract — which is the point of having a method instead of an appetite.

### `vt-events` — escape-sequences → typed shell events · <Pill variant="run">candidate · defer</Pill>

A possible future **cleave of an existing electricity** (pty-host), not a greenfield one.

- **Axis:** the mapping from terminal escape sequences (OSC 7 cwd · OSC 633 command marks · OSC 0/2 title · `tcgetpgrp` foreground) to *typed semantic events*, which changes with shell/OSC conventions — **independently** of how the PTY process is spawned and reaped.
- **The sentence:** *every time a shell convention changes (Kitty, OSC 1337…), we edit parse rules tangled into process-lifecycle code.*
- **Evidence:** the ~50 lines of OSC handlers at <Cite file="packages/pty-host/src/ptyHost.ts" lines="434-481" />, sitting inside ~450 lines of node-pty lifecycle; one real entanglement (OSC 633 *drives* the foreground re-sample schedule).
- **Why defer:** the parse ruleset has been **inert since extraction** (no new OSC codes added; the churn has all been *process* concerns — exit tombstones, sample timing). No external consumer wants byte-stream-→-events without the rest of pty-host *today*. Verdict: **document the axis, monitor churn** — split to `@kolu/vt-events` only when a non-kolu consumer (tmux/Zellij control-mode) appears or the rules take 3+ independent edits. A clean Löwy axis whose *volatility hasn't fired yet*.

### `chord-arbitration` — who owns a keychord when input consumers stack (Family B) · <Pill variant="run">candidate</Pill>

The most *kolu-shaped* find: a volatility born of running tools **inside** the app's own terminals.

- **Axis:** which layer claims a key chord when consumers are stacked — the kolu app vs a tool running *inside* a kolu PTY. Claude Code's `Ctrl+B`/`Ctrl+J` must reach the terminal, not the app. The set of contested chords **grows as embedded tools are added.**
- **The sentence:** *every time a tool inside a PTY reserves a chord, we hand-edit `PROHIBITED_KEYBINDS` and manually shift the colliding app action (`Mod+J` → `Mod+Shift+J`).*
- **Evidence — dated & growing:** `Ctrl+J` reserved 2024, `Ctrl+B` added 2026 (`ee454bc3`); the reserved set at <Cite file="packages/client/src/input/prohibitedKeybinds.ts" lines="21-33" />, the manual action-shifts at <Cite file="packages/client/src/input/actions.ts" lines="224" /> / <Cite file="packages/client/src/input/actions.ts" lines="252" /> (#873/#957), and the collision is *unit-tested* (`PROHIBITED × ACTIONS`) at <Cite file="packages/client/src/input/keyboard.test.ts" lines="218-232" />.
- **Verdict — candidate:** the invariant is *tested* (good) but the resolution is still hand-shifted per collision. The receptacle is an arbiter that, given the reserved set, *derives* the app's chord remap instead of editing it by hand. Domain-agnostic: any terminal multiplexer hosting passthrough tools has this exact axis. Extract when a **fourth** reserved chord lands — that's the rule-of-three line.

### `capability-gate` — degrade when a browser API is absent (Family B) · <Pill variant="fix">extract-now</Pill>

A scattered invariant that wants one seam.

- **Axis:** the set of platform capabilities (clipboard, File-System-Access, WebGL, secure-context/HTTPS, PWA-install) that gate a feature, *with a degrade path when absent* — and that set churns as browsers move.
- **The sentence:** *every time a feature needs a capability, we re-write `if (!navigator.X) { toast.error(…) }` inline — each site with a different fallback.*
- **Evidence:** partial home at <Cite file="packages/client/src/capabilities.ts" lines="26-34" /> (form-factor gates only), but the *feature* gates are ad hoc at 5+ sites — `navigator.clipboard?.write` <Cite file="packages/client/src/screenshotTerminal.ts" lines="395" />, `showSaveFilePicker`/`getDisplayMedia` <Cite file="packages/client/src/recorder/useRecorder.ts" lines="103-111" />, the `execCommand` clipboard fallback <Cite file="packages/client/src/ui/clipboard.ts" lines="64" />, WebGL gating in `Terminal.tsx`. The plain-HTTP clipboard fallback (`168e6865`) shows the set growing.
- **Verdict — extract-now:** same shape (*probe → degrade → notify*) at every site, no app-domain in it. A `tryFeature(capability, { onAbsent })` seam collapses the five hand-rolls and makes "what do we do when the API is missing" answerable in one place. This is the export row's [welded guards](#view--portable-artifact--export-family-c-adjacent--candidate--defer) seen from the *capability* axis rather than the *format* axis — the two cross here.

### `terminal-probes` — read volatile library internals behind null-safe thunks (Family B) · <Pill variant="run">extract-now · partly built</Pill>

Already recognised in `.agency/lowy.md` — the crop just confirms it and finds the leaks.

- **Axis:** reading private/volatile library internals (xterm `_core`/`_bufferService`, the live WebGL-canvas set) behind a *stable thunk* so a library rename or a lost GPU context returns `null` instead of crashing the render path.
- **The sentence:** *every time xterm's private shape moves, code that reached through it crashes — unless the read went through a probe.*
- **Evidence — receptacle exists, but leaks at the edges:** `TerminalProbes` (`() => T | null`) at <Cite file="packages/client/src/terminal/terminalRefs.ts" lines="20-30" /> and the WebGL lifecycle snapshot at <Cite file="packages/client/src/terminal/webglTracker.ts" lines="71-101" /> are the good pattern; but `screenshotTerminal` still pokes `buffer.active.getLine` *directly* (<Cite file="packages/client/src/screenshotTerminal.ts" lines="207-226" />), bypassing the probe wall.
- **Verdict — extract-now (harden, don't invent):** the electricity is *already designed*; the work is to route the remaining reach-throughs through it and ship it (likely as the diagnostics face of `solid-xterm`, or its own `@kolu/terminal-probes`). Distinct from solid-xterm's *lifecycle* axis: this one is purely *measure-volatile-internals-safely*.

### `refcounted-fan-out` — one source → N subscribers, refcount + backpressure · <Pill variant="fix">extract-now</Pill>

The pass's **best-evidenced** find — rule-of-three already *quadrupled* — and it resolves the multi-tab reject into a positive.

- **Axis:** share one upstream stream/watcher across N local consumers, with refcount install-on-first / teardown-on-last, a bounded per-subscriber buffer (drop-slow), and fault isolation (one listener throwing doesn't skip peers).
- **The sentence:** *every time we add a fan-out primitive (PTY output, WAL watcher, dir watcher, git-event tap), we re-hand-roll `Set<Listener>` + refcount + drop-slow.*
- **Evidence — the same shape at four sites:** `Channel<T>` <Cite file="packages/pty-host/src/channel.ts" lines="51-166" />, the refcounted WAL watcher <Cite file="packages/shared/src/sqlite/wal-subscription.ts" lines="93-149" />, the refcounted dir-watcher <Cite file="packages/integrations/io/src/refcounted-dir-watcher.ts" lines="63-151" />, and the git repo-change compose <Cite file="packages/integrations/git/src/repo-change.ts" lines="53-80" /> — all `subscribe(onChange) → () => void`, install-on-first/retire-on-last, fault-isolated.
- **Verdict — extract-now:** four independent hand-rolls of one lifecycle is the strongest signal in the crop. A `createFanOut(subscribe, { bound })` receptacle deletes the refcount/backpressure bookkeeping at every site. *This is the server-side fan-out the [reject pile](#the-reject-pile--candidates-the-engine-refused) said was real* — the axis the client lacks, the server has four times over.

<Callout kind="warn" title="This revises an earlier reject — @kolu/fs-watch was judged at the wrong altitude">
The [Considered table](#considered--not-electricity) rejects `@kolu/fs-watch` as "a refcount/debounce *lifecycle*, not a streaming transport." True — but that compared it to the *wrong* electricity. fs-watch isn't a transport; it is **one of the four instances of `refcounted-fan-out` above.** The reject answered "is this a *surface*-shaped transport?" (no) and missed "is the refcount-fan-out *lifecycle* itself the artifact?" (yes — `.agency/lowy.md`'s Layer-0 prompt). The helper was real; we'd been measuring it against the wrong receptacle. Corrected below.
</Callout>

### `freshness` — running-code identity vs deployed-code identity · <Pill variant="run">candidate · partly in surface-app</Pill>

- **Axis:** the identity of the code currently *running* vs the code *deployed/available*, and deriving "you are stale — reload/restart" from the drift — with **no transport knowledge** in it.
- **The sentence:** *every time a new identity axis appears (pty-host build-id, client commit, server commit), we hand-edit an `isStale` predicate.*
- **Evidence:** the nix-baked `currentBuildId()` <Cite file="packages/pty-host/src/buildId.ts" lines="1-29" />, the extensible `isStale` in <Cite file="packages/surface-app/src/surface.ts" lines="37-60" />, the stale badge reading `staleKey` in `IdentityRail.tsx`. Landed incrementally (`606c7a24` build-id intro → `5cc41968` surface-app extraction).
- **Verdict — candidate (mostly housed):** surface-app *already* owns the lifecycle plumbing, so this isn't greenfield — but naming the **axis** (identity-drift → update-prompt) separates it from surface's *transport* axis, which is why the same app can change its update story without touching its wire. The `③` is real: drishti reuses the `isStale`/`buildInfo` seam with its own identity.

### `ambient-injection` — deterministic time / platform / RNG as injected sources · <Pill variant="run">candidate</Pill>

The most *outside-the-box* find: the volatility isn't a boundary or a renderer — it's the **ambient globals the code reads without asking.**

- **Axis:** non-deterministic ambient singletons — the wall clock (`Date.now`/`setInterval`), the platform (`navigator.userAgent`), randomness — read directly at call-sites, which makes them *untestable and non-injectable*. The volatility is "this value comes from the environment, and the environment varies (and must be faked in tests)."
- **The sentence:** *every time setup needs the time/platform/RNG, we read a global directly — then can't test it, and re-roll the same `setInterval(…); onCleanup(clearInterval)` ticker.*
- **Evidence:** `isMac` is the **proven first instance** — already injected (the `dom/` row) precisely so the keybind core stops reading the UA singleton. The unowned siblings: the same shared-ticker shape at <Cite file="packages/client/src/staleness.ts" lines="54-56" />, <Cite file="packages/client/src/settings/useTips.ts" lines="41-44" />, `recorder/useRecorder.ts`, `useThemeManager.ts`. (Telling corroboration: the agent harness *forbids* `Date.now()`/`Math.random()` in some contexts for exactly this reason.)
- **Verdict — candidate, honestly hedged:** the per-use ticker is **leaf-tier** on its own (three lines). But the *axis* — deterministic injected ambient — is real and already paid off once (`isMac`). The receptacle is a `createClock()` / injected-providers seam (one owned ticker, fake-able in tests) that `createOwnedResource` would host. Worth naming because it reframes a scatter of "small helpers" as one testability electricity; not worth a package until a second ambient (the clock) is actually injected.

### The reject pile — candidates the engine *refused*

Included on purpose: a method that only ever says "extract" is rationalising, not generating. These looked like electricity and aren't (yet).

| Refused candidate | Why it's a phantom (the falsifier that fired) | Extract-if |
| --- | --- | --- |
| **Identity / trust across the wire** — "who may author/read state across a boundary" | The axis **doesn't vary**: kolu is single-user-local (no login, no per-user state; multi-user is *documented future* in `auth.md`). Only two trust sites exist, with *different* mechanisms (iframe `event.source`, daemon token), and each **collapses into its transport** — ssh delegates to ssh keys, the iframe check is *forced* by opaque origin. Fails the independence check; no second consumer; the daemon token was a one-off race-fix, not a recurring volatility. | multi-user lands, *or* a second app embeds the bridge with a different auth model. Then "who's allowed" finally varies independently of "how bytes ship." |
| **Client multi-tab / fan-out** — "N tabs share one upstream subscription" | **Zero client evidence**: one `PartySocket` per tab, no `BroadcastChannel`/`SharedWorker`/refcount, no issue or PR asking for it. Pure speculative generality (more hooks than things plugged in). *Telling nuance:* fan-out **is** real infrastructure on the **server** (refcounted WAL watchers, drop-slow-subscriber channels) — so the axis exists in kolu, just not at the boundary we were tempted to extract. | a feature needs cross-tab state sync; the *second tab pulling one subscription unmodified* would be the ③ proof — until then, it's a fantasy. |

The second row is the sharper lesson: the same axis (fan-out) is **genuine on one side of the system and speculative on the other.** "Is this volatile?" is never answered in the abstract — only at a specific boundary, with the git log open.

### The reverse audit — run the engine *backwards* on what's already shipped

A generator should also be a *detector*: point the procedure at each existing `@kolu/*` row and try to **re-derive** it — name its one axis, write its "every time ___ changes" sentence, find a second consumer of different shape. What *can't* be re-derived is a suspected functional decomposition wearing a package hat.

- **Re-derive cleanly** (one nameable axis, real ③): surface, surface-app, surface-nix-host, pty-host (boundary axes); solid-markdown, solid-pierre, transcript (appliance axes). The audit *reproduces* the inventory — reassuring.
- **Correctly *fail* to re-derive a hard axis** — and that's the point, not a flaw: the leaves (`url-shape`, `nonempty`, `memorable-names`, `log`, `html-escape`, `terminal-themes`). Asked "what *hard* volatility?", each answers "a bounded algorithm." The engine sorting them *out* is it working.
- **Flagged — can't name a single axis:** `solid-fileview` and `solid-browser`. Both re-derive only as *"arranges A + C"* — no own volatility. The reverse audit lands on the **same** composition verdict reached three other ways (the Family column, test ②, the seams analysis). When four independent checks converge on "composition, not receptacle," that's not a coincidence — it's the boundary being real.

The reverse audit is the method's self-test: it must re-derive the good rows, reject the leaves, and flag the compositions — *without being told which is which.* It does. That, more than any single new candidate, is the evidence that the note is a generator and not a flattering list.

<Callout kind="warn" title="Re-evaluating @kolu/artifact-sdk — its electricity framing shifted (open; resolve outside this PR)">
"Anchoring" (the original <PrLink pr={922} /> framing) now under-describes the
package. It holds **two** volatilities: (1) the **anchoring core** (`core/` — W3C
quote extract/re-find/highlight; pure and agnostic, speaks only
`Range`/`Document`/`ShadowRoot`), and (2) the **sandboxed-iframe parent↔frame
bridge** (`client/bridge.ts` + `iframe/` — opaque-origin postMessage with
`event.source` identity as the only trust boundary). The bridge is the *harder*
volatility, and it has quietly accreted clients: comments (selection →
`SelectMsg` → highlights), then **in-frame navigation** (`observeIframeNavigation`),
then **back/forward input** (`observeIframeHistory`, <PrLink pr={1191} />). The
lens-debate in <PrLink pr={1191} /> already reframed the package's *stated*
identity to "the in-iframe sandbox bridge, with comments as one client" — but
that was a docs fix, not a decomposition.

**The open question (work it outside this PR):** is "talk to code inside an
opaque-origin sandboxed frame" one sound receptacle — a transport-grade
volatility with comments / navigation / input as interchangeable clients — or a
topic-bundle that should split the agnostic **anchoring core** from the **bridge
transport**? Note the smell that prompted this: the back/forward client landed in
this package only by *physical necessity* (the one script inside the sandbox is
this one), which is exactly the "easy to put it here" the three traps above warn
against. Flagged, not decided.
</Callout>

Plus external electricity correctly leaned on instead of hand-rolled: **Corvu** (dialog/drawer/focus-trap/scroll-lock), **@solid-primitives/\*** (resize-observer, media). Origin: a 41-agent Hickey/Lowy audit (16 candidates) → its "ship now" tier landed in <PrLink pr={1089} />; a framework-scale re-test + adversarial skeptic surfaced the missed `solid-xterm`.

## Next: extract @kolu/solid-xterm

<Callout kind="accent">
The most *scoped* of several not-yet-built electricities — the crop above now lists `createOwnedResource`, `capability-gate`, and `refcounted-fan-out` as fellow *extract-now* finds, so solid-xterm is no longer "the one," just the one with a written interface. Mis-filed at first as "terminal domain," but a SolidJS adapter for xterm.js is agnostic (the `.agency/lowy.md` §6.5 *"what library is this?"* test). It owns two declared volatility rows — async-init cleanup (the `#598`/`#600` ~900 KB leaks — *the same owner-correctness axis `createOwnedResource` would host*) and canvas/WebGL lifecycle (the `#591` zombie-context hunt, `#575`/`#239`/`#595`).

**Inline today** in one `Terminal.tsx` `onMount` (972 lines): `new XTerm` <Cite file="packages/client/src/terminal/Terminal.tsx" lines="472" />, 8× `loadAddon` <Cite file="packages/client/src/terminal/Terminal.tsx" lines="489-509" />, the WebGL single-owner dance <Cite file="packages/client/src/terminal/Terminal.tsx" lines="222-285" />, owner-correct async init <Cite file="packages/client/src/terminal/Terminal.tsx" lines="463-471" />, fit/resize <Cite file="packages/client/src/terminal/Terminal.tsx" lines="179" /> / <Cite file="packages/client/src/terminal/Terminal.tsx" lines="750" />, touch-scroll <Cite file="packages/client/src/terminal/Terminal.tsx" lines="549-596" />.
</Callout>

```ts
// one primitive (not a topic-bundle, per §6.5):
createSolidXterm({ container, theme, fontSize, canUseWebgl?, enabledAddons?,
  onData?, onResize?, onLinkActivate?, onCustomKeyEvent?, plugins? })
  -> { terminal, searchAddon, hasWebgl, renderer, dispose }
```

Owns the agnostic core; kolu bits (link provider, soft-keyboard, diagnostics probes, the PTY `terminal.attach` stream) stay consumer-injected via callbacks. **Dissolves ~240–314 lines** of `Terminal.tsx`. Survived the skeptic ("the domain braid runs too deep") on the interface — those are orchestration concerns, not xterm-lifecycle concerns. Graduates the way surface did: any SolidJS app embedding xterm can plug in.

## Considered — not electricity

Looked framework-sized (the audit's biggest braids), but fail the bar — recorded so they don't get re-proposed.

| Candidate | Verdict | Why |
| --- | --- | --- |
| **defineMutation** (optimisticOverlay) | <Pill variant="bad">helper</Pill> | One module, one domain (canvas layouts); the write echo is already owned by `surface`'s metadata subscription. Pending is a UI cache, not a transport axis. |
| **@kolu/fs-watch** | <Pill variant="run">re-judged</Pill> | *Original verdict: helper — "a refcount/debounce lifecycle, not a streaming transport."* **Re-judged this pass:** that measured it against the wrong receptacle. It is not a transport, but the refcount/fan-out **lifecycle** it owns *is* the artifact — fs-watch is one of four instances of [`refcounted-fan-out`](#refcounted-fan-out--one-source--n-subscribers-refcount--backpressure--extract-now) (extract-now). The reject wasn't wrong about "not a socket"; it stopped one altitude too early. |
| **@kolu/geometry** | <Pill variant="bad">domain-coupled</Pill> | `TileLayout` binds it to a server-persisted domain type; a `Rect` package re-adapts at every boundary — moves the braid, doesn't dissolve it. |
| **@kolu/surface-publish** | <Pill variant="bad">already-covered</Pill> | Surface's `cellHandlers`/`collectionHandlers`/`pollOnEvent` already own server snapshot+delta. Only `terminal.attach` is a raw escape hatch. |
| **@kolu/commands** (palette) | <Pill variant="bad">domain-coupled</Pill> | Palette engine is already agnostic; all coupling is isolated in `createCommands`, which must stay in the app. Extracting deletes ~0 kolu lines. |
| God-procedure split | <Pill variant="run">refactor</Pill> | Its biggest dissolvable chunk *is* `solid-xterm` (above); the rest is kolu orchestration — a refactor, not a receptacle. |

---

*Bar from Juval Löwy, _Righting Software_ (receptacle/volatility) + Rich Hickey, _Simple Made Easy_ (complecting); one-socket-not-a-topic-bundle is `.agency/lowy.md` §6.5. `file:line` verified against the codebase; ③ demonstrated by [drishti](https://github.com/srid/drishti) — a second consumer (kolu is the first) that vendors `@kolu/surface` via npins.*

*Sourcing honesty: **verbatim Löwy** — the two axes of change (same-customer-over-time × all-customers-at-once), the Cooking/Feeding requirement-vs-solution distinction, and "open-ended and… very expensive to contain unless encapsulated." **This note's distillations** (sharper than Löwy's literal text, which frames discovery as requirements "sweat") — the enumerate-then-sweep test, the "second consumer of different shape" gate, the three-family taxonomy (A · Boundary / B · Invariant / C · Appliance), and "foresight names the vault; pain funds it." The Parnas lineage ("begin with a list of… decisions… likely to change") and the falsification side (Metz's "wrong abstraction," YAGNI, the rule of three) are cited where used. The newly-identified crop's evidence was ground out against the codebase this pass; verdicts are falsifiable, with extract-if conditions stated.*
